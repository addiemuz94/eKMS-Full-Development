# CLAUDE_WEB.md

_Part of the split CLAUDE.md documentation set — `web/`-specific. See [CLAUDE.md](CLAUDE.md) for the index, the "Production is already live" short warning, and the non-negotiable architectural boundaries list. See [CLAUDE_SHARED.md](CLAUDE_SHARED.md) for cross-cutting info, and [CLAUDE_BACKEND.md](CLAUDE_BACKEND.md) for the full API endpoint table/auth model this app talks to._

**Web portal note:** the Super Admin web portal is being migrated from the Kotlin/Wasm Compose `webApp` module to a React+Vite app at `web/`. `webApp` is now excluded from the Gradle build (`settings.gradle.kts` has `include(":webApp")` commented out — "Kotlin/Wasm webApp is frozen") and should be treated as a reference/legacy implementation, not a build target. New Super Admin portal work happens in `web/`. See `CLAUDE_SHARED.md`'s Module architecture table for the full module comparison.

## Web Portal — Registration Workflow

**Status (Jul 2026 rework): implemented in `web/`; pairing backend + portal must be redeployed together on the VPS (see `backend/DEPLOY.md`).**

Portal paths:
- `/registration` — **continuous unit-scoped wizard** (7 steps): Register Unit → Key Cabinet → **Cabinet Settings** → Personnel → Keys → Permissions → Generate Pairing Code. Unit chosen in step 1 is carried through all steps automatically; pairing code is always generated last.
- `/terminals` — **Key Cabinet Registration** (full `TerminalUpsertRequest` fields, one-time pairing code banner, regenerate with revoke warning, `paired` badge, **Settings** action for behavioral timers/toggles)
- `/personnel` — includes optional `staffId`; credentials column states **terminal-local only**

**Cabinet behavioral settings (portal + DB + sync):** Take Warning Time, Door-Close Warning Time, Key Return Certification, Return/Retrieval video toggles live in `terminal_cabinet_settings` (`GET`/`PATCH /v1/admin/terminals/:id/cabinet-settings`, `expectedRevision`-guarded). Defaults are created with each terminal. Bootstrap/download snapshot includes `cabinetSettings`; `TerminalAdminStore.applyServerSnapshot` merges them (server wins — see `CLAUDE_TERMINAL.md` for the terminal-side merge). NFC / fingerprint / face remain terminal-local (policy A — see `CLAUDE_BACKEND.md`).

**Appointment Authorization** was removed from the portal nav/routes and unmounted from the live API (`/v1/admin/appointments*`) — not required by the client.

#### What terminalApp must do for pairing to work end-to-end

See `CLAUDE_TERMINAL.md`'s "Web Portal — Registration Workflow: terminalApp's side of pairing" section for the terminal-side implementation checklist and operator checklist — kept there since it's terminalApp code, not `web/` code.

## Project Status

### How to read this file (status truth — filtered to `web/` conflicts)

| Topic | Conflicting claims that used to confuse agents | Current truth |
|---|---|---|
| **Web PATCH / in-place edit** | Early spot-check and older boundary notes said `web/` has create+delete only, no update. Later Completed says PATCH wired for ~10 resources. Intro used to say portal still "hardcoded/in-memory for others." One authoring note said "no Node" as if the API were unreachable. | **Code exists** in `web/src/api/client.ts` + edit UIs for Units/Terminals/Personnel/Keys/Permissions/Events/Schedules/Groups/Multi-Auth. Production API is **already at `https://kms-cvt.com/v1`** (see `CLAUDE_BACKEND.md`) — verification is "rebuild/redeploy `web/` and click Edit," not "stand up a backend." A machine without local Node cannot `npm run build` there; it can still hit the live portal. Treat as **implemented, end-to-end edit verify pending**. Frozen `webApp` still has no edit UI (irrelevant — do not build it). |
| **Personnel on web** | Next steps said "rebuild Personnel management properly on web/". | Core list/create/delete/update + card-enrollment status already exist on `web/` Personnel page. Remaining work is UX/audit polish and standing-alert features — **not** a greenfield rebuild. |
| **`web/` Phase 9-web-C+D deploy** | Known issues said icon+label buttons and the dark-mode toggle were shipped but never addressed their deployment status — implicitly local-only, build/lint-verified. | **Deployed to `kms-cvt.com`, confirmed by the user.** Phase 9-web-A+B's tokens/`ConfirmDialog`/outlier-page cleanup ship in the same `web/` build, so that code is live too, not just C+D. Deployment is **not** the same as browser-verified — no real click-through against the live site has happened yet; see Known issues. |

**Agent rule:** When deciding whether a bug is open, check **Known issues / not yet resolved** and **Next steps** first. Only use older Completed Phase notes for history.

### Completed

- **Post-pull reconciliation (web audit note)** — full entry (Kotlin gotcha fix) lives in `CLAUDE_TERMINAL.md`. Ran a `webApp`-vs-`web/` portal audit as part of that pass; findings are now superseded — see `CLAUDE_SHARED.md`'s Module architecture table for current state, not the old audit text.
- **Personnel/credential sync + theme iteration — `web/`'s own theme** — full entry lives in `CLAUDE_TERMINAL.md`. `web/` separately shipped its own MD3 "Cavotec" theme (primary `#0055a5`, Outfit + IBM Plex Mono, `components/ui/` kit) — same brand direction as terminalApp's theme rework, not a literally shared token source.
- **PATCH/update support across `web/`** (closes boundary #4's gap — see `CLAUDE.md`). 11 backend resources have real double-guarded `expectedRevision` enforcement (backend detail: see `CLAUDE_BACKEND.md`). `client.ts` gained explicit `update*` methods for the first group and a generic `updatePath()` helper (mirroring `createPath`/`deletePath`) for the `softDeleteRouter` group; `ResourcePage.tsx` gained one generic `update` prop covering Events/Schedules/User Groups/Key Groups/Appointment Reasons. Every edit path reads `expectedRevision` off the already-loaded row (no extra fetch) and on `409` shows an explicit "changed by someone else, reloading" message rather than retrying/overwriting. Appointments/Key Slots intentionally have no edit UI (see boundary #4). Also fixed a missed `/v1/debug/agent-log` call site in `PersonnelPage.tsx` (full incident: see `CLAUDE_BACKEND.md`). Known unfixed dead code: `client.ts`'s `listAppointmentPermissions`/`createAppointmentPermission`/`deleteAppointmentPermission` call routes that don't fully exist — harmless, unused.
- **Key Cabinet + Personnel registration, pairing code — Part 4 (`web/` registration-workflow handoff docs)** — documented the registration-workflow contract (now folded into the "Web Portal — Registration Workflow" section above) for the web developer; no `web/` code written this pass. (Parts 1-3 — backend/shared, terminalApp UI, mobileApp check — live in `CLAUDE_BACKEND.md`/`CLAUDE_TERMINAL.md`/`CLAUDE_MOBILE.md`.)
- **Portal-managed cabinet settings** — backend/terminal halves (migration 007, GET/PATCH route, bootstrap merge) live in `CLAUDE_BACKEND.md`/`CLAUDE_TERMINAL.md`. Portal side: registration wizard's Cabinet Settings step and `/terminals`' Settings action, per the "Web Portal — Registration Workflow" section above. Phase 1 extras (Regional Admin, checkout sync, office hours, vendor passkey) were deferred at that point — now delivered, see `CLAUDE_BACKEND.md`/`CLAUDE_TERMINAL.md` for those entries.
- **Basic Settings sort/filter/search** (`web/`, Jul 2026): Units — state/province filter, Name/State/City column sort; Terminals — text search (cabinet/unit name), unit filter, paired-status filter; Personnel — text search (name/email/staff ID), role filter, unit filter, Name/Role/Units sort; Keys — text search, unit filter, enrollment-status filter, Name sort; Permissions — text search (personnel/unit/key names), unit filter, personnel filter, switched from cards to a sortable table; `ResourcePage` (Events/Schedules/User Groups/Key Groups) — unit filter + Name sort, inherited by all four automatically. (This pass also shipped a 10-step registration wizard that was superseded by the 6/7-step continuous flow below — don't build toward 10 steps.)
- **Dialogs never close on backdrop click** across the whole portal (Registration/Units/Terminals/Personnel/Keys/Permissions/MultiAuth/`ResourcePage`) — only Cancel/Save closes a dialog, preventing accidental data loss from a mis-click. **"Start another unit"** button on the registration wizard's final step resets to step 1 for a clean next registration. (Wizard step count/flow itself superseded by the 7-step Cabinet-Settings version — see "Web Portal — Registration Workflow" above for the current spec.)
- **`web/` Regional Admin role UI + Units map clustering fix** (commit `60cb589`, Jul 2026). Personnel page's role filter/create/edit `<select>`s and the registration wizard's Add Personnel step gained a `REGIONAL_ADMIN` option — closes the "no `web/` this pass" gap explicitly flagged in the Phase 1 (Revised) entry (`CLAUDE_BACKEND.md`); client-side option only, no new API methods added (`client.ts` already sent `role` as a free string). `MalaysiaUnitsMap.tsx` replaced its plain `L.layerGroup` with `leaflet.markercluster`'s `L.markerClusterGroup` (`showCoverageOnHover: false`, `maxClusterRadius: 50`; new deps `leaflet.markercluster`/`@types/leaflet.markercluster` in `web/package.json`) so overlapping unit pins group into a cluster instead of stacking illegibly; selecting a unit now `flyTo`s to at least zoom 11 (`Math.max(map.getZoom(), 11)`) instead of `panTo` at whatever zoom the map was already at. The commit message also says "fix delete" but the diff touches only these 4 files with no delete-logic change visible — if a delete bug still reproduces on Units/Terminals/Personnel/Keys, it wasn't addressed by this commit. Verify: not run against a dev server this session (no local Node); **picking `REGIONAL_ADMIN` here will fail against `https://kms-cvt.com` until the backend's Regional Admin support (migration `008`, `users.js` role enum) is deployed** — see `CLAUDE_BACKEND.md` Known issues/Next steps, this is a UI-only addition ahead of that deploy.
- **Phase 9-web-A+B: `web/` dark-mode token foundation + themed confirm dialog + outlier-page cleanup.** Added a full `:root[data-theme='dark']` mirror of every MD3 color role in `styles.css` (additive only, no light-mode var removed/renamed), plus two roles neither mode had before — `--md-sys-color-warning*`/`--md-sys-color-info*`, both light and dark, same four-role shape as error/success. Fixed the 6 flagged hardcoded-hex spots (`.search`/`.field input`/`.empty-state` backgrounds, `.map-side-list`/`.map-canvas-wrap` backgrounds, focus-ring `rgba()`, `.pairing-code-banner` fallback hex) plus several found along the way: `.wizard-stepper`/`.wizard-step`/`.wizard-step-num`/`.toolbar-row select` were permanently stuck on their hardcoded fallback regardless of theme, because the CSS vars they referenced (`--surface-variant`/`--on-surface-variant`/`--surface`) were never actually defined anywhere in `:root`; `.brand-mark`'s hardcoded white text and `.layout-map-detail`'s hardcoded white background would both have broken in dark mode (fixed via new `--nav-link-text`/`--map-detail-bg` tokens). `MalaysiaUnitsMap.tsx` gained a dark Leaflet tile layer (CartoDB's free `dark_all`, URL reachability verified by fetching a real tile before wiring it in) with a `MutationObserver` on `<html data-theme>` that swaps tiles live — dormant until Phase 9-web-D's toggle existed to set that attribute. New `components/ui/ConfirmDialog.tsx` + `useConfirm()` hook (drop-in `confirm()` replacement resolving from themed Confirm/Cancel buttons, same "never closes on backdrop click" rule as every other dialog in the app) replaced **every** native `confirm()` call site — 10 files (Units/Terminals×2/Registration/RecycleBin/Personnel/MultiAuth/Permissions/ResourcePage) — each site's existing gating logic preserved exactly, no behavior added beyond the UI swap. `MultiAuthPage`/`RecycleBinPage` (previously the only two pages not importing `components/ui/`) were brought onto the shared `Button` component; `MultiAuthPage`'s card-based list was also converted to the `data-table` pattern every other list page uses (no new sort/filter invented — it never had any). Incidental, not caused by this session's own changes: `npm install` (first real install in this environment) also synced a pre-existing `leaflet.markercluster`/`@types/leaflet.markercluster` lockfile drift that had been declared in `package.json` since commit `60cb589` but never actually installed. Verify: `npm run build` (tsc + vite) and `npm run lint` (oxlint) both pass clean — **no dark mode has ever been seen in a real browser**, since nothing set `data-theme` yet at the time this shipped (that's Phase 9-web-D, next entry).
- **Phase 9-web-C+D: icon+label buttons (`lucide-react`) + dark-mode toggle.** `components/ui/Button.tsx` gained an `icon?: LucideIcon` prop, rendered before the label — never icon-only, swapped for the existing `CircularProgress` spinner while `loading`. Rolled out across every cancel/accept/add-shaped action app-wide: `Plus` on every "Add X"/"Register X" button (Units/Terminals/Keys/Personnel/Permissions/MultiAuth/ResourcePage's shared `addLabel`, plus 6 more inside the Registration wizard — stripped the wizard's old manual `'+ Create new user'` text prefix now that the icon carries that meaning), `Check`/`X` on every dialog's Save/Cancel pair app-wide including the `ConfirmDialog` from Phase 9-web-A+B (explicitly deferred there). Deliberately **not** touched, flagged rather than guessed: Edit/Recycle/Restore/Purge/Settings/pairing-code row-link actions (not Add/Save/Cancel-shaped, and Recycle/Purge are exactly the "don't touch delete/purge unless obvious fit" case); the Registration wizard's own ← Previous/Next → pagination (plain arrows already read correctly; forcing accept/cancel icons onto pagination would misrepresent it); `DataSyncPage`'s conflict-resolution choices (Keep server/Keep terminal change/Merge manually — not add/save/cancel shaped); `LoginPage`'s standalone "Sign in" CTA (not a dialog Save). New `theme/ThemeContext.tsx` (`ThemeProvider`/`useTheme`, same provider-hook shape as `AuthContext.tsx`): `ThemeMode` is `system`/`light`/`dark`, mirroring terminalApp's `TerminalThemeMode` three-state pattern (see `CLAUDE_TERMINAL.md`) — persisted to `localStorage` (`ekms_web_theme`, this app's correct equivalent of terminalApp's SharedPreferences, not a violation of anything), applies `data-theme` to `document.documentElement`, and live-follows OS `prefers-color-scheme` changes while in `system` mode. Toggle UI is a `SegmentedControl` (System/Light/Dark) in `AppShell`'s topbar `session-pill`, next to Sign out — reused the app's existing segmented-toggle pattern rather than building a new switch. `index.html` gained a small inline pre-paint `<script>` (reads the same `localStorage` key + `matchMedia`, sets `data-theme` before first paint) plus a dark variant of the boot-screen's inline `<style>` background/text colors, so there's no flash-of-wrong-theme on the loading screen either — kept intentionally tiny per instruction, not a general SSR-style fix. Verify: `npm run build`/`npm run lint` both pass clean. **Still not visually confirmed in a real browser** — see Known issues.
- **Jul 2026 portal + pairing rework** shipped policy A (see `CLAUDE_BACKEND.md`) + `web/` registration UI + pairing backend together — see "Web Portal — Registration Workflow" above and `CLAUDE_BACKEND.md`'s "Credential policy (policy A)" section for the full current spec, not repeated here. Appointments removed from portal nav/routes and unmounted from `backend/src/index.js`.
- **Key node binding (Key Attachment feature) — `web/` half.** Closes the previously-flagged gap: web-registered keys had no physical cabinet node binding at all. Full entry (discovery, backend/terminal halves) lives in `CLAUDE_BACKEND.md`/`CLAUDE_TERMINAL.md`.
  - **New `client.ts` methods** (there were zero before this pass): `listKeySlots(terminalId?)`, `createKeySlot`, `updateKeySlot` — plus a new `KeySlotDto` type in `types.ts` (`id`/`terminalId`/`nodeAddress`/`managedKeyId`/`revision`, matching `key-slots.js`'s `mapSlot()`).
  - **New shared helper `api/keySlotAssignment.ts`** (`assignKeyToNextAvailableNode`, `countAvailableNodes`) used by both the Registration wizard's Keys step and the standalone Keys page, so the auto-assign algorithm exists exactly once. **Real refinement beyond a literal reading of the task**: lowest-unused-integer must check for an existing-but-unassigned `KeySlot` row at each node and PATCH (re-link) it rather than always POSTing a new row — `key-slots.js`'s own duplicate-node-per-terminal check rejects a second ACTIVE row at an already-occupied node number, so after the first delete+re-add cycle a create-only implementation would always 400. This is also what makes "delete frees the node" actually work end to end, given the backend's DELETE unlinks rather than removes the slot row (see `CLAUDE_BACKEND.md`).
  - **Multi-cabinet-per-site handling (confirmed design decision, not in the original brief)**: `ManagedKey` only has `siteId`, not `terminalId`, and a site can have more than one `Terminal`. Auto-picks silently if the site has exactly one terminal; shows a small cabinet `<select>` in the Add-key dialog if it has more than one; creates the key without a slot (clear notice, matches pre-existing behavior) if it has none yet.
  - **Capacity handling**: `StepKeys` preemptively disables "Add key for this unit" once the (single-terminal) site's cabinet is full, showing "N of M nodes free"; `KeysPage`'s dialog disables Save the same way once a cabinet is selected and full. Both also handle the live `POST /key-slots` 400 gracefully (clear message, key itself still created) as a defensive backstop for the race/multi-terminal cases that can't be precomputed as cleanly.
  - **New "Cabinet node" column** on both the wizard's Keys step table and the standalone Keys page's table (`Node N (Cabinet Name)` / "Not assigned") — visibility only, no new interaction.
  - **Known, explicitly out-of-scope gap**: `KeysPage.tsx`'s existing Edit action lets a Super Admin change an existing key's `siteId` (move it to a different unit) — this does **not** unlink/reassign its `KeySlot`, since only "add" and "delete" were in scope this pass. Editing a key's unit today leaves its old cabinet's node assignment pointing at a key that's nominally now at a different site — flagged, not fixed.
  - Verify: `npm run build` (tsc + vite) and `npm run lint` (oxlint) both pass clean — only pre-existing-pattern warnings (the same "missing `reload` dependency" `useEffect` warning already present on every other page in this app). **Not run against a dev server or the live API this pass** — no live click-through of the new auto-assign/capacity/multi-cabinet UI.
- **Key Access page grant/revoke live-verified (Haikal, Jul 2026).** Portal `/key-access` Approve / Reject / Revoke work against production. Only B mobile Apply → RA Approve path confirmed in the same session; terminal Key Attachment still open — see `CLAUDE_MOBILE.md` / `CLAUDE_TERMINAL.md`.

### Known issues / not yet resolved

- **`web/` PATCH/update rollout is now built and redeployed, but still not click-verified live.** `npm run build` passes, the VPS now serves the rebuilt `web/dist`, and production remains `https://kms-cvt.com`. Remaining work is manual live verification of at least one edit path (for example Units edit + `409` conflict handling) against the live API.
- **`web/` has not had a full section-by-section UX audit** against the vendor manual — only API-client / spot checks. Page-level UX and shared-type-equivalent checks still outstanding.
- **Appointment Authorization removed from portal + API mounts** (Jul 2026) — full backend-side detail (dead `phase4.js` routers) lives in `CLAUDE_BACKEND.md`. Portal-side: nav/routes removed, not re-added unless product restores appointments.
- **Key Slots:** backend CRUD exists (see `CLAUDE_BACKEND.md`) and is now wired into the Keys step/page for auto-assign-on-add (see the Key node binding entry above) — but there's still no standalone Key Slots admin page for directly viewing/editing/moving a slot outside of the key-creation flow (e.g. manually reassigning a node without deleting+recreating a key). Optional, see Next steps.
- **Key node binding (Key Attachment) — `web/` half is build/lint-verified only, never run against a dev server or the live API.** The auto-assign-next-node algorithm (including the PATCH-vs-POST reuse logic for a freed node), the multi-cabinet-per-site terminal picker, and the capacity-disable UI have never been clicked through — see `CLAUDE_BACKEND.md`/`CLAUDE_TERMINAL.md` for the much larger backend/terminal-side live-verification gap this pass explicitly left open. The known "editing a key's unit doesn't reassign its slot" gap (see Completed entry) is also unaddressed.
- **Regional Admin role**: `web/`'s `REGIONAL_ADMIN` option in Personnel/Registration role selects (commit `60cb589`) should now work against production since the backend is deployed (see `CLAUDE_BACKEND.md`) — but this has not been click-verified either.
- Personnel management in frozen `webApp` was a shallow form — **moot** (module frozen). Live work is `web/PersonnelPage.tsx` (already has real backend CRUD + enrollment column); remaining gaps are audit/polish, not a missing page.
- **`web/` dark-mode work (Phase 9-web-A through D — CSS tokens, `ConfirmDialog`, icon+label buttons, the System/Light/Dark toggle) is deployed to `kms-cvt.com`, confirmed by the user, but still only `npm run build`/`npm run lint`-verified — nobody has clicked through the live site with it yet.** A clean typecheck/lint proves the code is well-formed, not that it looks right, and being deployed proves the bits are live, not that anyone has looked at them. Specifically unconfirmed: the dark token values actually read as a coherent dark theme rather than just "not broken"; the CartoDB dark tile layer's contrast against `MalaysiaUnitsMap.tsx`'s markers/detail card; every icon+label `Button` rendering correctly (a clean compile only proves the `lucide-react` icon names resolve, not that spacing/alignment looks right); the `SegmentedControl` theme toggle's own affordance and the System/Light/Dark states actually switching live; and the `index.html` pre-paint script actually preventing a flash-of-wrong-theme in practice. Needs the user to toggle dark mode and click through the **live production site** before any of this is called done.
- **Standing Super Admin / mobile alerts** for take door-left-open and return abandoned-return are not built — no portal delivery UI exists yet (event source: see `CLAUDE_TERMINAL.md`).

### Next steps (in order)

- **Browser-verify `web/`'s Phase 9-web-A through D dark-mode work now that it's live on `kms-cvt.com`** (tokens, `ConfirmDialog`, icon+label buttons, System/Light/Dark toggle) — this affects the live production site right now, not just unshipped code; build/lint-verified only so far, never opened in a real browser. See Known issues for the specific unconfirmed pieces.
- Full section-by-section audit of `web/` against the vendor manual (appointments removed — skip those sections)
- Standing-alert UI on `web/` for door-left-open / abandoned-return (later)
- **Live-verify the Key node binding UI** (add key → auto-assign node, multi-cabinet picker, capacity disable, delete → node freed for reuse) against the real backend and a real terminal running the Key Attachment screen — nothing about this pass has been click-tested yet.
- Optional: Key Slots admin page if product needs it (direct view/edit/move of a slot outside the key-creation flow)
