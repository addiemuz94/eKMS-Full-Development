# CLAUDE_MOBILE.md

_Part of the split CLAUDE.md documentation set — `mobileApp`-specific. See [CLAUDE.md](CLAUDE.md) for the index, the "Production is already live" short warning, and the non-negotiable architectural boundaries list. See [CLAUDE_SHARED.md](CLAUDE_SHARED.md) for cross-cutting info, including the Digital Key hardware-feasibility investigation (spans terminal + mobile HCE, kept whole there) and Web/Mobile App UX Consistency rules mobileApp must follow. See [CLAUDE_BACKEND.md](CLAUDE_BACKEND.md) for the full API endpoint table/auth model this app talks to._

`mobileApp` is the Super Admin companion app: a thin UI layer today, no hardware access, no network code for most of this project's history — see Project Status below for where real backend wiring has since landed.

Living product requirements (Only B exception access, Vendor docs/PIC, dashboard/map): see repo-root [`eKMS_MobileRequirement.md`](eKMS_MobileRequirement.md).

## Ownership (Haikal / Adi)

Flexible — either may cover the other when timing requires; prefer the tagged owner and leave a short note if you cross over.

| Tag | Who | Focus |
|---|---|---|
| **Haikal's part** | Haikal (remote) | Docs, backend/`shared`, `web/` portal, `mobileApp`, VPS deploy, phone/emulator verification |
| **Adi's part** | Adi (office) | Physical F7G18P: pairing, Key Attachment, take/return, passkey on terminal, beep/boot/biometrics as time allows |

## Hardware Feature Findings pointer

`CLAUDE_SHARED.md`'s "Hardware Feature Findings" section documents the Digital Key (NFC phone-as-card) investigation in full — it spans terminal (reader hardware) and mobile (HCE) test tooling, so it's kept whole there rather than duplicated/split here. Read it before re-proposing a phone-as-key-card feature.

## Project Status

### How to read this file (status truth — filtered to mobileApp conflicts)

| Topic | Older / conflicting claim | Current truth |
|---|---|---|
| **Key Access / passkey Apply model** | Completed "Passkey feature" shipped grant-scoped key picker + duration slider bounded by Region ceiling. | **Superseded by product decision Only B (Jul 2026).** Apply must become **exception access**: location outside standing site assignments, calendar pickup/return, reason, RA approve → 4-digit PIN until return. Do **not** extend the grant-scoped duration UI as the long-term product. Spec: `eKMS_MobileRequirement.md`. Region `max_key_access_duration_minutes` is **not** enforced for this flow for now (any calendar range allowed). |
| **`mobileApp` network** | Known issues still said "100% local demo data, zero network — hold". | **Live-verified (Jul 2026):** login + Access (Only B request/approval) + Terminals list + Overview counts + Alerts (request status) talk to production `https://kms-cvt.com`. **Map** (OSM pins + native Directions) landed. **Real FCM SDK wiring landed (Aug 2026)** — see Completed entry below; replaces the old "needs `FCM_SERVER_KEY`" note (backend is now on `firebase-admin` HTTP v1 + a service-account JSON, not a server key — see `CLAUDE_BACKEND.md`). **Zero device verification yet** — needs a real APK install + manual test push. **Activity Report (Logs tab)** — Super Admin only: same filters/summary/PDF as web via `/v1/reports/activity-*` + `ACTIVITY_LOGS` export. |
| **Bottom nav Overview** | — | Fixed Jul 2026: do not nest `NavHost` in a scrollable parent. |
| **Default server URL** | Admin Menu `serverAddress` defaulted to `""` while API client used production. | Both `mobileApp` and `terminalApp` default to **`https://kms-cvt.com`**. Blank prefs / legacy empty `serverAddress` migrate to production; Admin Menu / pairing Advanced may still override for LAN. |

Treat the newest Completed entry below as current for any other mobileApp topic.

### Completed

- **`mobileApp` check (Part 3 of Key Cabinet + Personnel registration pairing code work)** — no changes needed, confirmed via grep: zero references to any new Phase 1 pairing-code field/concept, zero network dependencies, still 100% local demo data. (Parts 1/2/4 — backend/shared, terminalApp UI, `web/` docs — live in `CLAUDE_BACKEND.md`/`CLAUDE_TERMINAL.md`/`CLAUDE_WEB.md`.)
- **Digital Key hardware-feasibility test tooling (mobileApp only, deliberately experimental)** — a throwaway "Experimental" tab (`SuperAdminCompanionApp.kt`) hosting `DigitalKeyTestScreen.kt`, which deliberately and temporarily crosses boundaries #1/#2 (see `CLAUDE.md`) at the user's explicit request, purely to evaluate two candidate phone-as-key designs on real hardware before any decision is made — **not** a settled architecture change, see the Known issues entry below for the exact framing to use. **Option A** (`nfc/OptionACardUidHostApduService.kt`): `NfcAdapter` reader mode captures a real card's raw UID (`Tag.getId()`, held in-memory only via `CapturedCardUidStore` — no persistence, no encryption), replayed by a `HostApduService` as application-layer APDU response data after AID selection. Checked current Android docs before building, not assumed: standard HCE has no public API to control the low-level ISO 14443-A anti-collision UID — it's randomized per-tap by the phone's NFC controller by design (Android's own HCE guide: "NFC readers should not depend on the UID of HCE devices as a form of authentication or identification"); Option A can only ever affect the APDU-layer response, never the anti-collision UID itself, and this is surfaced as an explicit on-screen warning rather than silently assumed to work. **Option B** (`nfc/OptionBDigitalKeyHostApduService.kt`): a locally-generated UUID (`DigitalKeyIdentityStore`, plain SharedPreferences, not derived from any physical card) replayed the same way, on a separate AID from Option A's. Also checked, not assumed: AID-conflict/default-app resolution only applies when two services register the *same* AID — since A and B use different AIDs under `category="other"`, both can be registered and enabled simultaneously with no forced system default; the in-screen enable/disable toggles are a testing convenience (`PackageManager.setComponentEnabledSetting`) rather than something the platform requires. Added `android.permission.NFC` + `<uses-feature android:name="android.hardware.nfc">` to `AndroidManifest.xml` — mobileApp had zero permissions declared before this. `shared`/`terminalApp`/`web`/`backend` untouched. Pairs with a separate, not-yet-built terminalApp-side raw-reader-frame diagnostic screen (see `CLAUDE_TERMINAL.md`) for the actual test. Verify: `:mobileApp:assembleDebug` passes; installed and launched on a real device (Samsung SM-A176B) for the user's hands-on test. **This fundamentally requires that real-hardware test to mean anything — code-level verification alone cannot confirm either option actually works.**
- **mobileApp clean-start foundation + Digital Key temp-tooling revert.** Full revert of the Digital Key hardware-test tooling (mobileApp's Option A/B prototype + HCE services, terminalApp's raw serial command prober — see `CLAUDE_TERMINAL.md`) confirmed clean via `git diff` — nothing left outside the reverted files. mobileApp rebuilt from its old single-file ~213-line mockup onto Navigation-Compose with real per-screen files (`ui/dashboard/`, `ui/terminals/`, `ui/access/`, `ui/alerts/`, `ui/common/CompanionCard.kt`). Cavotec theme duplicated into mobileApp's own `ui/theme/` rather than extracted into a shared module — flagged: the pass's own scope ("terminalApp revert-only") ruled out restructuring terminalApp's existing theme files at the time. `INTERNET` permission added — the first permission mobileApp has ever declared. Terminals screen fixed to read real terminal/site names from `KeySlotDemoData`/`SuperAdminDemoData` instead of the old mismatched hardcoded strings; connection state honestly shown as `UNKNOWN` rather than fabricated, pending real network wiring (see two entries below). Digital Key is a bottom-nav entry opening a neutral "Coming Soon" dialog — no hardware-limitation detail exposed in end-user UI copy (that context stays in `CLAUDE_SHARED.md`'s Hardware Feature Findings section only). terminalApp audited read-only, no changes made: no natural Digital Key UI slot exists or is needed there; if ever built it would arrive via the existing NFC Card login tile, not a new one.
- **mobileApp real backend client.** Ktor client (core + OkHttp + content-negotiation + kotlinx-json) — mirrors terminalApp's stack choice, since mobileApp is Android-only like terminalApp with no Wasm constraint. New `MobileApiClient`: login for all 4 roles via `clientType: MOBILE` (already supported by the existing backend client-type enum — see `CLAUDE_BACKEND.md`), refresh, token/profile storage — deliberately minimal at the time, no regions/request-fetching methods yet (added in the next entry). New `LoginScreen` gates `SuperAdminCompanionApp`; added a Sign-out control since nothing else could clear a session. Closes mobileApp's "zero network code" gap for the first time — see `CLAUDE_SHARED.md`'s Hardware Feature Findings section for why NFC-specific network/hardware capability remains separately out of scope.
- **Passkey feature — mobileApp request/approval flow + real terminalApp wiring.** Backend-side audit/fixes and the Region schema this depends on live in `CLAUDE_BACKEND.md` (migration 009 + the Part 0 audit's 4 fixes) — not repeated here. mobileApp: new `KeyAccessRequestScreen` (Technician/Vendor) — key picker scoped via the same validFrom/validUntil + AccessGrant filter terminalApp's Key Menu already uses (see `CLAUDE_TERMINAL.md`), duration slider bounded by the site's Region ceiling, approved passkey rendered as the most prominent element on screen (large monospace readout, primary-tinted card); new `KeyAccessApprovalScreen` (Regional Admin/Super Admin) — equal-weight Approve/Reject, all scoping server-side; both wired into the existing Access bottom-nav slot, dispatched by role. terminalApp side (`TerminalPasskeyLoginScreen` real wiring, `DueDateSource.PASSKEY_REQUEST`): see `CLAUDE_TERMINAL.md`'s Passkey entry — confirmed via diff: zero behavior change to Key Menu itself from this pass. Verify: backend booted locally and smoke-tested — every new/modified route correctly gated; shared 59/59 tests pass; terminalApp/mobileApp both compile+assemble clean. Not deployed at the time — this is the combined push landing now (see Next steps). **Product supersession (Jul 2026):** this grant-scoped + duration-slider Apply is **not** the long-term Key Access model — see status-truth **Only B** and `eKMS_MobileRequirement.md`. Keep the PIN display / approval shell patterns where useful; replace site/key/time/reason behavior in Phase 1.
- **Only B decisions locked + docs Phase 0 (Haikal, Jul 2026).** Product confirmations recorded in `eKMS_MobileRequirement.md` and this file: access model = exception-only (Only B); push = new FCM work; Vendor docs max 5 MB stored in MySQL `MEDIUMBLOB`; PIC reject still forwards to Regional Admin with note; calendar pickup/return with **any range allowed for now** (explicitly **not** the 24h / region ceiling). Ownership tags: **Haikal** = remote build/deploy/phone; **Adi** = office F7G18P hardware checklist. No feature code in this Phase 0 pass.
- **Only B Phase 1 Technician Apply (Haikal, Jul 2026).** Migration `010_key_access_exception_calendar.sql` (`reason`, `pickup_at_epoch_ms`, `return_at_epoch_ms`). Create rule inverted for TECHNICIAN/VENDOR: only sites **outside** standing `user_site_assignments`. New `GET .../exception-sites` and `.../exception-sites/:siteId/keys`. Approve sets PIN expiry to **return** datetime (no region duration clamp). Mobile `KeyAccessRequestScreen` rewritten (site → keys → calendar text fields → reason); approval shows reason + window. PIN still shown to requester after approve.
- **mobileApp login “Expected string, received null” (Haikal, Jul 2026).** Login **was already wired** to `POST /v1/auth/login` with `clientType: MOBILE`. Failure was Zod rejecting `"deviceId": null` (kotlinx `encodeDefaults` + nullable field; Zod `.optional()` ≠ null). Fixed: backend `deviceId` is `z.string().nullish()`; mobile sends `mobile-android` and sets `explicitNulls = false`. Redeploy API before relying on old APKs that still send null.
- **mobileApp nav + tab wire-up (Haikal, Jul 2026).** Bottom-nav could not return to Overview because `NavHost` lived inside a parent `verticalScroll`. Fixed: `NavHost` fills the scaffold body; each tab scrolls itself; Overview deep-links use the same `goToTab` as the bottom bar. Terminals + Overview counts call live `GET /sites` + `GET /terminals` (RA/Technician/Vendor allowlisted + site-scoped). Alerts lists key-access request status from the API (interim until FCM Phase 3). Access was already API-backed; profile is kept in shell state so the tab always receives it. UI copy standardized (short sentences, role subtitle, View/Open labels).
- **mobileApp live server connection verified (Haikal, Jul 2026).** App talks to production API; Overview / Terminals / Access / Alerts load against the server. Compile fixes: `AlertsScreen` / `TerminalsScreen` used lowercase `modifier` instead of Compose `Modifier` inside `Column` — corrected.
- **Default server = production (Haikal, Jul 2026).** `MobileApiClient.baseUrl` treats blank prefs as `https://kms-cvt.com` (same constant as terminal). Reinstall APK to pick up the blank-guard. Admin override not required for production.
- **mobileApp Idempotency-Key on mutations (Haikal, Jul 2026).** Apply/Approve/Reject failed with `Mutating requests require Idempotency-Key header` because `MobileApiClient.send` never sent the header (web + terminal already do). Fixed: every non-GET request sends a fresh UUID `Idempotency-Key`. Rebuild/reinstall mobile APK before retesting Only B Apply.
- **Loopback server URL → production (Haikal, Jul 2026).** Blank prefs already defaulted to `https://kms-cvt.com`, but a previously saved `http://127.0.0.1` / `localhost` still won and broke emulator/device connects. `resolveProductionBaseUrl` now remaps blank + loopback hosts to production on terminal and mobile.
- **Technician approved PIN picker (Haikal, Jul 2026).** Approved requests are a dropdown; PIN is hidden until selected. API adds `siteName` + `cabinetNames` so the PIN card shows location and which key cabinet to use. Redeploy API + rebuild mobile APK.

- **Passkey login session from API profile (Haikal, Jul 2026).** Only B exception techs are not in a cabinet's bootstrap user list (sync only downloads SUPER_ADMIN + site-assigned users). Passkey login used to call `authenticateByUserId` and failed with "personnel record no longer exists" after a valid PIN. Terminal now builds `TerminalSession` from `TerminalPasskeyLoginResponse` profile fields; API returns `requesterDisplayName`/`requesterEmail`/`requesterRole`. Redeploy API + rebuild terminal APK.

- **Cabinet login site scoping + Key Access revoke on web (Haikal, Jul 2026).** Technician/Vendor standing login (password/NFC/fingerprint/face) is blocked unless the cabinet's unit is in their `assignedSiteIds`; Super Admin / Regional Admin unchanged. Passkey/Only B still works via server-validated PIN. New `POST .../key-access-requests/:id/revoke` clears an approved PIN (`REVOKED`). Portal **Key Access** page (`/key-access`) lists requests with Approve / Reject / Revoke. Rebuild terminal + mobile APKs; API + `web/dist` redeployed.

- **Passkey KEY_MENU empty keys + pickup window + mobile date/time pickers (Haikal, Jul 2026).** Passkey sessions now keep approved `keyIds` for KEY_MENU (exception techs have no AccessGrant). Take also falls back to local TerminalKey node mapping. `passkey-login` rejects PIN before `pickupAt`. Mobile Apply uses Material date→time pickers. Redeploy API; rebuild terminal + mobile APKs. If keys still missing after PIN, attach them to cabinet slots (Key Attachment) and Download/sync.
- **Only B smoke + compile fixes (Haikal, Jul 2026).** Live test: Technician Apply → RA Approve → Technician can use approved access (PIN / Access tab). Portal **Key Access** grant + revoke work. **Still broken:** Key Attachment / key enrollment on the cabinet — approved keys are not usable for take until nodes are attached and synced (see Known issues). Compile fixes this push: `KeyAccessApprovalScreen` wrong `import androidx.compose.ui.Modifier.modifier`; `TerminalAdminApp` local helpers (`managedKeyAndSlotFor` / `refreshSnapshot` / `acceptStandingLogin`) moved above first use (Kotlin locals are not hoisted).

- **Activity Report on mobile (Logs tab)** — Super Admin bottom-nav **Logs**: same category/unit/cabinet/period filters + summary + list as web; **Export PDF** downloads via authenticated `/v1/reports/exports` (`ACTIVITY_LOGS`) and opens with FileProvider. Non-Super Admin roles do not see the tab (reports API is `requireSuperAdmin`). Terminal contributes take/return via sync push; registration events come from the portal/API. **Blocked on API redeploy** for live use (same as web — see `CLAUDE_BACKEND.md`). Kotlin compile not verified on the VPS agent host (no Android SDK / `local.properties`).
- **Real Firebase SDK wiring (checkout-deadline push), mobile-only pass (Aug 2026).** Backend side (`deadlineMonitor.js`, `firebase-admin` HTTP v1 `fcm.js`, `notifyRegionalAdminsForSite`) already landed and was not touched here — see `CLAUDE_BACKEND.md`. This pass: `mobileApp/google-services.json` placed at module root (Firebase project `kms-cvt`) with **two** registered Android clients — `com.ekms.terminal` (pre-existing) and `com.ekms.mobile` (added mid-pass after an initial content mismatch was caught and flagged to the user before proceeding — the file originally only had the terminal client). Added `**/google-services.json` to root `.gitignore` (file was already untracked, so this was a safe, non-destructive addition, not a history rewrite). Gradle: `com.google.gms.google-services` plugin **4.5.0** (project-level classpath + mobileApp `apply`) + `firebase-bom` **34.17.0** + `firebase-messaging`, all version-checked against current stable as of this pass (not guessed) and confirmed compatible with this repo's pinned AGP 8.11.1 / Kotlin 2.2.20 / compileSdk 36 / minSdk 26 — note `androidx.core`/`core-ktx` **1.19.0** requires compileSdk 37 + AGP 9.1.0 and fails `checkDebugAarMetadata`; pinned `core-ktx` to **1.16.0** instead (needs only compileSdk ≥35 / AGP ≥8.6.0, satisfied here). `PushTokenSync.kt`'s old `Class.forName` reflection stub replaced with a real `FirebaseMessaging.getInstance().token.await()` call (`kotlinx-coroutines-play-services`); still wrapped in try/catch → null on any failure (no Play Services, no network, bad config), so Alerts polling remains the fallback — same fail-safe shape as the old stub, just real now. New `EkmsFirebaseMessagingService` (`push/EkmsFirebaseMessagingService.kt`): `onNewToken` re-registers via `MobileApiClient.registerPushToken`; `onMessageReceived` shows a real notification through new `push/PushNotifications.kt` (creates an `IMPORTANCE_HIGH` `NotificationChannel` idempotently, `NotificationCompat` + a plain-circle `drawable/ic_stat_notify.xml` placeholder icon — no branded icon existed anywhere in mobileApp's `res/` before this). Manifest: registered the service with the `MESSAGING_EVENT` intent-filter, added `POST_NOTIFICATIONS` permission, `MainActivity` now `launchMode="singleTop"`. Tapping a notification opens `MainActivity` with an `open_tab=alerts` extra; `MainActivity` (now handling `onNewIntent` too) hoists this into a Compose state passed into `SuperAdminCompanionApp(openTabRequest, onOpenTabConsumed)`, which `goToTab`s to Alerts once `authenticated` is true (handles both warm-tap and cold-start-then-login) — reuses the existing bottom-nav `goToTab`, no new deep-link system. `POST_NOTIFICATIONS` (API 33+, this app's targetSdk is 36) requested once at `onCreate` via `ActivityResultContracts.RequestPermission`; no existing request for it was found before this pass. Unrelated one-line fix needed to get `:mobileApp:assembleDebug` green at all: `LogsScreen.kt`'s `SummaryChip` had a pre-existing `Column(Modifier = ...)` typo (capital-M named arg that doesn't exist) — corrected to `modifier =` (same class of bug the Jul 2026 "live server connection verified" entry above already fixed once in `AlertsScreen`/`TerminalsScreen`; this is a third instance in `LogsScreen`). Verify: `:mobileApp:assembleDebug` clean (only pre-existing/expected deprecation warnings, e.g. `FirebaseMessagingService.onNewToken`/`getToken()` being marked deprecated-in-Java upstream — not errors). **Zero device/emulator verification** — needs a real APK install on a device with Play Services, login, and a manually-triggered test push once the backend's `FCM_SERVICE_ACCOUNT_PATH` is actually configured on the VPS, before this is trusted end-to-end.
  - **Aug 2026 handoff update: this is code-complete and its contract has been cross-checked against the backend's actual `deadlineMonitor.js` push call (`sendPushToUser`) and web's SSE client.** **Still not deployed** — per explicit user instruction, deployment (including building the release APK this pass never had a signing pipeline for — see the doc below) is being handed to a separate dev team via a GitHub-push-triggered VPS deploy. Full deploy sequence and the release-signing/distribution gap: `docs/DEPLOY_HANDOVER_checkout_notifications.md`.

### Known issues / not yet resolved

- **Activity Report (Logs tab) needs live API** — until `/v1/reports/activity-*` is deployed, the Logs tab will 404/fail against production.
- **Key Attachment / enrollment still broken for Only B take** — Technician Apply → RA Approve → access works, but cabinet **Key Attachment** (dashboard "Key enrollment" / Key Attachment) is not reliably binding approved keys to physical nodes, so passkey→take still fails or shows no slot. Needs dedicated fix + hardware re-verify (also tracked in `CLAUDE_TERMINAL.md`).
- **Alerts push notifications not device-verified** — real FCM SDK wiring landed (see Completed entry above), but has had zero real-device/emulator test. Needs an APK install + a manually-triggered test push once the backend's `FCM_SERVICE_ACCOUNT_PATH` is configured on the VPS. Alerts tab itself remains request-status polling in the meantime. Door-left-open / abandoned-return standing alerts (the actual checkout-deadline triggers `deadlineMonitor.js` computes) still need an end-to-end live test once push is confirmed working.
- **Dashboard map + directions** not built — Phase 4. Overview is counts/cards only.
- **Vendor docs / PIC / two-stage approval** not built — Phase 2 (Haikal).
- **Digital Key** — infeasible on current reader hardware for production; "Coming Soon" in app. Do not reopen without hardware change — see `CLAUDE_SHARED.md` Hardware Feature Findings.
- **Passkey → real F7G18P take with due time** — blocked on Key Attachment above; then **Adi's part** on office cabinet for full take/return. Broader terminal Known issues: see `CLAUDE_TERMINAL.md`.

### Next steps (in order)

1. **[Haikal / Adi] Fix Key Attachment** — approved Only B keys must bind to cabinet slots and survive Download/sync so passkey→take works. Re-verify on F7G18P.
2. **[Adi] Passkey → take** — after Attachment: enter PIN → take key → due time = return datetime. Also pairing e2e, Return Flow / beep / boot as time allows.
3. **[Haikal] Phase 2** — Vendor docs (5 MB DB BLOB) + PIC + two-stage approval.
4. **[Haikal] Phase 3 device verification** — real Firebase SDK wiring landed (see Completed); still needs an actual APK install + manual test push against the VPS (configure `FCM_SERVICE_ACCOUNT_PATH` there first) before trusting it. Richer Alerts (standing door-left-open / abandoned-return, not just request-status) still to build on top once push itself is confirmed.
5. **[Haikal] Phase 4** — dashboard map + directions.

