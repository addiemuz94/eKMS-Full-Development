services:
  mysql:
    image: mysql:8.4
    container_name: ekms-mysql
    restart: unless-stopped
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: root_dev_password
      MYSQL_DATABASE: ekms
      MYSQL_USER: ekms
      MYSQL_PASSWORD: ekms_dev_password
    volumes:
      - ekms_mysql_data:/var/lib/mysql
      - ./sql/001_init.sql:/docker-entrypoint-initdb.d/001_init.sql:ro
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "127.0.0.1", "-uroot", "-proot_dev_password"]
      interval: 5s
      timeout: 5s
      retries: 20

volumes:
  ekms_mysql_data:
