# CLAUDE_TERMINAL.md

_Part of the split CLAUDE.md documentation set — `terminalApp`-specific. See [CLAUDE.md](CLAUDE.md) for the index, the "Production is already live" short warning, and the non-negotiable architectural boundaries list. See [CLAUDE_SHARED.md](CLAUDE_SHARED.md) for cross-cutting info, and [CLAUDE_BACKEND.md](CLAUDE_BACKEND.md) for the full API endpoint table/auth model this app talks to._

## Terminal → live API: wire-up on the device (Admin Menu)

The terminal does **not** talk to the React UI. It talks to the same backend the portal uses (`https://kms-cvt.com/v1/...`) — see `CLAUDE_BACKEND.md` for the endpoint table, auth model, and what shows on the portal after terminal actions.

**Manual Admin Menu pairing (fallback path):**

1. Sign in locally if needed (first Super Admin bootstrap), open **Admin Menu**.
2. **Set server address** → defaults to `https://kms-cvt.com` (no trailing slash). Only change for LAN / non-production.
3. **Key Cabinet ID** → UUID of this cabinet's terminal row from portal **Terminal Settings** (same id used in sync paths).
4. Sign out, then sign in with a **portal** Super Admin email/password so `TerminalApiClient` stores JWT access/refresh tokens.
5. Use **Bootstrap** / **Download** / **Push** (Admin Menu) for snapshot sync. Personnel create still uses admin user APIs when server-linked. **Do not** call credential complete/revoke APIs for NFC/fingerprint/face (policy A — terminal-local only, see `CLAUDE_BACKEND.md`).

**Client code:** `terminalApp/.../data/TerminalApiClient.kt` builds requests as `{baseUrl}{ApiPaths.*}`. Canonical path strings live in `shared/.../api/ApiContracts.kt` (`object ApiPaths`).

## Web Portal — Registration Workflow: terminalApp's side of pairing

The full portal-side registration workflow (wizard steps, `/terminals` page, cabinet behavioral settings) lives in `CLAUDE_WEB.md`. This section covers what terminalApp itself must do for pairing to work end-to-end.

Already in `terminalApp` (verify after `git pull` + rebuild):

1. Fresh/unpaired device shows `TerminalPairingScreen` (gate on non-blank Key Cabinet / `cabinetId`).
2. Default API base URL **and** Admin Menu `serverAddress` are `https://kms-cvt.com` (blank legacy prefs migrate to production; Advanced / Admin override still allowed for LAN).
3. `TerminalApiClient.pairWithCode(code)` → `POST /v1/terminal/pair-with-code`.
4. On success: store TERMINAL_DEVICE tokens, persist cabinet id/name, **best-effort** `syncCoordinator.bootstrap()` so unit data **and cabinetSettings** download from the DB.
5. Do **not** clear pairing when operators sign out (gate is cabinet id, not access token).
6. After bootstrap, local `TerminalCabinetSettings` timers/toggles match the portal (server wins on those five fields; `serverAddress` / activation stay device-local).
7. **Default server = production (Haikal, Jul 2026):** `TerminalCabinetSettings.serverAddress` defaults to `TerminalApiClient.DEFAULT_BASE_URL`; `ensureDefaultServerAddress()` + blank-pref guards migrate legacy `""` installs.

Operator checklist after Super Admin registers a Johor (or any) cabinet:

1. Complete registration wizard through **Cabinet Settings** (or set Settings on `/terminals`), then note the **6-digit code** from the final pairing step (shown once; regenerate if lost — regenerating revokes the old device session).
2. On site: open terminal → enter code (and server URL only if not using production default).
3. Confirm bootstrap/download succeeds; Personnel/Keys/grants **and** Take/Return warning timers match the portal for that cabinet.
4. Enroll NFC / fingerprint / face **on the terminal only** — those enrollments are **not** uploaded to MySQL (policy A).

Manual Admin Menu pairing (server address + Key Cabinet UUID + Super Admin login) remains a fallback if pairing is unavailable.

## Terminal App UX Baseline (Production — baseline + defined enhancements)

terminalApp's core screens, flow order, and terminology continue to follow
the Smart Key Cabinet User Manual V2.1 as the baseline. This is no longer
a strict exact-clone requirement — production behavior may extend beyond
the manual, but only via enhancements explicitly documented in this
section. Undocumented deviation from the manual is still a bug; a
documented enhancement below is not.

Baseline (unchanged from the manual):
- Standby -> tap-to-wake -> login screen
- Login: personnel card swipe, key card swipe (returns directly),
  account/password, Face Recognition, Fingerprint Recognition
- Key retrieval: Layout Display / List Display, user-toggleable
- Return flow: swipe -> door/blue light -> insert -> done
- Admin Menu: all 10 items from the manual (terminal name, Key Cabinet ID,
  password change, server address, activation code, key node setting, MAC
  address, Key Return Certification, return video toggle, retrieval video
  toggle)
- Color theme and first-start screensaver remain the only purely cosmetic
  differences, as before.

Documented production enhancements (beyond the manual):

1. **Key Take Flow — active feedback and timed door-close enforcement**
   (supersedes the manual's bare "door opens, insert key, done" return
   description for the TAKE side specifically):
   - Continuous beep from unlock through door-close confirmation
   - Take Warning Time: a Super Admin-configurable setting, in seconds,
     stored per-terminal (add alongside the existing Key Return
     Certification / video toggles in the Admin Menu's terminal settings)
   - Timer starts only on confirmed key-fob removal (0x16 bolt-removed),
     never on unlock or door-eject alone
   - If the timer expires before the door is reclosed, play a "please
     close the door" voice line
   - A door-left-open state (voice line played, door still not closed) is
     logged as a distinct event from the take itself, and may surface as a
     standing Super Admin alert
   - A secondary, separate timeout governs the case where the door is
     ejected but the key fob is never removed at all (open, no removal) --
     see open implementation question in the Key Take Flow spec; do not
     leave this case unbounded

   All new admin-configurable timing values follow the same settings
   storage/sync pattern as existing terminal settings (Key Return
   Certification, video toggles) -- do not introduce a separate ad-hoc
   config mechanism.

2. **Key Return Flow — active feedback and timed door-close enforcement,
   direction-reversed from the Key Take Flow** (supersedes the manual's
   bare "door opens, insert key, done" description for the RETURN side,
   layered on top of the existing Phase 3/9 return flow: swipe -> door/
   blue light -> insert):
   - The target node is identified via key-card UID swipe, resolved
     through `CardUidResolver` (shared with the login flow's personnel-
     card path -- one resolver, never re-derived per flow)
   - From the moment of that swipe, a 20-second no-insert abandonment
     ceiling starts, concurrently with the optional Key Return
     Certification login gate if that setting is enabled -- the clock is
     not paused for login, so a slow certification login eats into the
     same 20s window
   - Continuous beep from confirmed door-open through door-close
     confirmation
   - Insertion detected within 5s of door-open cancels the escalation;
     not detected by 5s plays a "please insert the key" voice line once
     and raises beep volume (same 1s interval) until insertion or
     abandonment
   - Not inserted by the 20s ceiling (measured from the original swipe,
     not from door-open): locks the fob slot, turns the light off, logs a
     distinct abandoned-return event, and alerts both the terminal user
     and Super Admin (mobile app) -- deliberately not the same as Take
     Flow's abandonment, which only re-locks an already-unlocked fob with
     no two-party alert, since nothing was ever removed
   - On confirmed insertion, the fob is locked immediately; the light
     stays on through the Door-Close Warning Time countdown -- a NEW,
     separate Super Admin-configurable setting from Take Warning Time
   - If the door is still open when that countdown expires, play a
     "please close the door" voice line and log a door-left-open event --
     distinct from both the return record and the abandoned-return
     notification, since never-inserted / inserted-but-door-left-open /
     successful-close are three different failure modes, never collapsed
     into one
   - Every exit path (success, door-open hardware fault, abandoned-
     return) leaves the light off and releases the concurrency guard

   Door-Close Warning Time follows the same settings storage/sync pattern
   as Take Warning Time and the other terminal settings -- same rule as
   Take Flow, no separate config mechanism.

   **Return Flow rework additions** (layered on top of the above, not a replacement of it):
   - **Wrong-slot detection**: once the door is open, every poll cycle also sweeps every other
     configured `KeySlot` node (not the full 1-127 address space) via Test Micro Switch and
     Card (0x17), not bare Test Micro Switch (0x16) -- a readable card UID is resolved through
     `CardUidResolver`/the key-fob registry (same mechanism the login/return-trigger swipe
     already uses) to confirm a genuinely enrolled key, not inferred from presence alone. A
     wrong-slot node's light turns red, an on-screen warning shows, and the beep forces full
     volume until corrected -- deliberately still triggered by *unresolved* presence too (bolt
     present with no readable card, or a UID that matches nothing enrolled), since a real key
     should always carry a readable card and an unidentified object in an open cabinet during a
     return is exactly as worth flagging as a confirmed wrong key
   - **Continuous multi-key return session**: after a key return completes -- success, failure,
     or abandonment all count -- the terminal stays in a session-idle state instead of dropping
     to standby, so the next fob scan starts immediately with no re-login. Ends via an explicit
     Done button or `SESSION_IDLE_TIMEOUT_MILLIS` (20s default) of no new scan, either way
     falling back to standby
   - **Checkout record**: a local-only, deliberately minimal stand-in (`TerminalCheckoutStore`)
     records who took a key and when at Take Flow success, so Return Flow can show "Checked out
     by X * Ym ago" and close the record out on return. No overdue/emergency/extension fields,
     no backend sync -- that is a separate, not-yet-built phase's job, not this store's

3. **Key Menu & multi-key sequential Take Flow** (Technician/Vendor post-login landing
   screen, supersedes the manual's implicit "everyone sees the same cabinet grid" for
   non-Super-Admin personnel specifically -- Super Admin's own unfiltered grid, reached from
   the dashboard, is untouched):
   - After a Technician/Vendor logs in (server or local session), they land on `KEY_MENU`
     instead of the old unfiltered `KEY_RETRIEVAL` grid -- Super Admin still reaches the
     latter via the dashboard's "Take a key" button, unchanged
   - Shows only the keys that user's `AccessGrant.keyIds` cover (a validity window on the
     grant, if set, is honored) as medium selection boxes with a Layout Display / List
     Display toggle -- the same toggle/enum `TerminalKeyRetrievalScreen` already used,
     reused rather than rebuilt
   - Multi-select: confirming a selection of N keys immediately lights every selected
     node's red indicator individually (one `redLightOn` command per node, never a
     batch/group command -- the light latches on its own, no keep-alive loop holds it)
   - The queue is then processed one node at a time: red -> blue ("your turn") -> the
     existing single-key take sequence (engage electromagnet -> eject door -> wait for
     confirmed removal) -> light off and lock -> automatically advances to the next queued
     node, until every selected key has been taken
   - Every per-key safeguard from enhancement #1 above (Take Warning Time, door-left-open
     logging, abandonment) applies unchanged to whichever node is currently active in the
     queue, since the queue reuses the exact same `TerminalKeyTakeScreen`/callbacks per node
     rather than a parallel implementation
   - One-electromagnet-at-a-time is structural, not an added check: the next node's take is
     only ever started from the current node's completion callback, by which point the
     guard has already been released -- two nodes can never be mid-take at once by
     construction of the loop

Any future enhancement beyond the manual must be added to this list
explicitly before being implemented -- this file is the single record of
where terminalApp intentionally diverges from the supplier manual and why.

Architecture note (unchanged): business logic stays in `shared`, hardware-
specific code stays Android-only in terminalApp.

## Project Status

### How to read this file (status truth — filtered to terminalApp conflicts)

| Topic | Conflicting claims that used to confuse agents | Current truth |
|---|---|---|
| **Door eject on key take** | Phase 10 says `releaseKeyForPickup` never calls `ejectDoor()` and is still deferred. Later text says the bug is already fixed / method deleted. | Production take path is **`beginKeyTake`** (blue light → unlock → **ejectDoor** → confirm open). `releaseKeyForPickup` is **deleted dead code**. Door-eject on take is implemented in code **and hardware-verified** on a real F7G18P (see Completed "Combined hardware verification session" below) — sequence, 20s abandonment ceiling, and removal-triggered countdown all confirmed correct. Two bugs found in the same session: beep-continuity is **still open, unrelated to Return Flow, lives in `AudioFeedbackController`'s audio-driver interaction**; Return-flow double-abandonment is **believed structurally fixed by the Return Flow rework (`attemptId`-keyed abandonment race) but not yet hardware-reverified** — see Known issues for both. |
| **Fingerprint/face enrollment hardware status** | Earlier Known issues said hardware unverified; later said credential-sync zod bug blocked portal sync. | **Device-local enroll/revoke is hardware-verified.** Server credential sync is **intentionally disabled (policy A, Jul 2026 — see `CLAUDE_BACKEND.md`)** — do not treat missing portal credential rows as a bug. |
| **Key-card swipe → return** | Phase 10 says real enrolled keys never resolve a node (`matchedKey` always null). Later Known issues says fixed via `managedKeyAndSlotFor()`. | **Fixed** during Key Return Flow. Phase 10 "NOT fixed" text is historical only. |
| **Theme / fonts (terminal)** | Cavotec rewrite paragraph mentions Inter and older hex tokens, then says a later pull changed Outfit / hex. | Trust **`terminalApp/.../ui/theme/Color.kt` and `Typography.kt`** in the tree, not hex values quoted in older diary paragraphs. |
| **Take/Return Flow audio** | Take/Return Flow Completed bullets above say `AudioFeedbackController` is a no-op stub. Later text says beep uses `SoundPool` and voice lines use `MediaPlayer`. | **Both claims are stale.** Real audio shipped (superseding the no-op stub), but beep was later switched from `SoundPool` to `MediaPlayer` as well — see Completed "Beep-continuity investigation" below. Both beep and voice lines are now one-shot `MediaPlayer` instances, kept as fully separate players with distinct `AudioAttributes`. Beep-continuity itself (the audible-dropout bug) has two code-only fixes applied but is **not yet hardware-reverified** — see Known issues. |
| **Terminal pairing** | See `CLAUDE_BACKEND.md`'s status-truth table for the deploy-status framing. | Terminal-side: pairing screen + best-effort bootstrap are implemented (see "Web Portal — Registration Workflow: terminalApp's side of pairing" above). Live e2e on a fresh install still required. |

**Agent rule:** When deciding whether a bug is open, check **Known issues / not yet resolved** and **Next steps** first. Only use older Completed Phase notes for history. Do not re-open struck-through or superseded items.

### Completed

- terminalApp UI/UX rebuilt to match Smart Key Cabinet User Manual V2.1 (see Terminal App UX Baseline section above): Phase 1 login (all 4 methods), Phase 2 key retrieval (Layout/List toggle), Phase 3 return flow, Phase 4 Admin Menu (10 items), Phase 5 settings wired to behavior.
- Phase 6: `shared/.../protocol/` frame layer (split-nibble encode/decode, CRC8, frame assembly), unit-tested against vendor spec worked examples. Pure protocol, no serial I/O.
- Phase 7: `shared/.../protocol/` gained `SerialTransport` (transport interface) and `KeyCabinetLink` (full node command set, 500ms/3-attempt timeout+retry, §10.4 one-electromagnet-at-a-time guard — `engageElectromagnet` throws `ElectromagnetConcurrencyException`), tested via `FakeSerialTransport`. terminalApp's `AndroidSerialTransport` implements the real `/dev/ttyS1` @ 19200 8N1 side; `CabinetHardwareController` drives `KeyCabinetLink`. Old `KeyCabinetProtocol.kt`/`CabinetSerialPort.kt` deleted (superseded).
- Phase 8: public card-swipe reader — confirmed genuinely separate protocol from node-level reads: `PublicM1CardReader` on `/dev/ttyS2` @ 9600 8N1 (ASCII poll `02 AF DD`), vs `KeyCabinetLink` on `/dev/ttyS1` @ 19200 (split-nibble/CRC8). Vendor AAR's `Device` defaults to 8N1 with just path+speed set. `PublicCardReaderController` auto-starts/stops at login idle; detected card feeds the Phase 3 key-card-swipe return trigger. Personnel-card swipe wiring landed later (card-UID disambiguation fix below).
- **Phase 9**: real hardware wired into retrieval/return. **Electromagnet direction confirmed: 0x13=unlock, 0x14=lock** (reconfirmed against an initially-backwards request — do not invert). Two-phase return: `beginKeyReturn` (0x11 Blue Light On + 0x23 Eject Door) / `waitForKeyInserted` (polls 0x16 until bolt-present, then 0x14 + 0x12 Blue Light Off), both auto-connecting the cabinet if not already open. `releaseKeyForPickup` from this phase was later deleted as dead code once Key Take Flow shipped `beginKeyTake` — see status-truth table.
- **Card-UID disambiguation fix**: added `shared/.../domain/CardUidResolution.kt` (`CardUidResolver`/`CardUidMatch` — pure decision over two already-resolved nullable record IDs, no raw UID/Android dependency; double-enrollment surfaces as `Ambiguous`, never silently picks a side) — implements the permanent NFC UID Resolution Rule (`CLAUDE_SHARED.md`). Generalized `EncryptedFobEnrollmentStore` → `EncryptedUidEnrollmentStore` (namespaced by `storeName`, so personnel-card and key-card enrollments are two separate Keystore-backed stores). Added `CardEnrollmentScreen`. Fixed the runtime bug: public-reader `onCardDetected` used to assume every scan was a key-card return trigger; now resolves via `CardUidResolver` and branches (personnel match → `authenticateByUserId`, key match → return flow, no/ambiguous match → explicit error). Verify: compile/test only at the time.
- **Phase 10: first physical F7G18P hardware verification** (interactive ADB-driven session). Confirmed correct on real hardware: cabinet connection, 0x13/0x14 electromagnet direction, bolt detection (0x16/0x17), full return sequence, one-electromagnet concurrency guard, public reader with real M1 cards, guided key-enrolment cycle, personnel-card swipe login end-to-end. Two bugs found and fixed: (1) `CardEnrollmentScreen`'s `PublicCardReaderController` was built inside a keyless `remember{}`, so its `onCardDetected` closure permanently captured the first-composition user/key selection, ignoring later changes — a real Compose gotcha, fixed via `rememberUpdatedState`; also added a "Revoke this record's card" button. (2) Key list was wired to demo `retrievalKeys` fixtures instead of `snapshot.keys` (real `TerminalKey` records) — fixed. Two further bugs found this session (door-eject on `releaseKeyForPickup`; key-schema ID mismatch on card-swipe return) are resolved/superseded by later work — see status-truth table, not repeated here.
- **Key Take Flow** — implements UX Baseline §1 above (full behavior spec there, not repeated here). `CabinetHardwareController.beginKeyTake`/`pollForKeyRemoval`/`waitForDoorCloseAfterTake`; new `TerminalKeyTakeScreen`. Field-verified 0x22 door-status byte mapping: **0x00 = open/ejected, 0xFF = closed**. Local event outbox added (`TerminalAdminStore.logEvent`/`eventOutbox`) — `KEY_TAKEN` + `KEY_TAKE_FAILED`/`KEY_TAKE_ABANDONED`/`KEY_TAKE_DOOR_LEFT_OPEN`; nothing drains it yet (no sync transport at the time). `AudioFeedbackController` was a no-op stub at this point (superseded — see Real audio feedback below). Verify: compile/assemble only at the time (later hardware-verified — see Combined hardware verification session).
- **Key Return Flow** — implements UX Baseline §2 above (full behavior spec there). `TerminalKeyReturnScreen` rewritten with the same stage-machine architecture as Take; the pre-existing null-key manual-tap fallback (`resolveReturningKey`, hardware-free testing convenience) kept untouched. `CabinetHardwareController` gained `beginKeyReturnFlow`/`pollForKeyInsertion`/`waitForDoorCloseAfterReturn`. Added `managedKeyAndSlotFor()` in `TerminalAdminApp` — bridges a real `TerminalKey` (what card enrollment binds to) into a synthetic `ManagedKey`/`KeySlot` at the one call site that needed it (see `docs/Backend_Integration_Handover.md` for the underlying two-schema gap; this is a targeted bridge, not a full schema unification). A top-level `LaunchedEffect` races the swipe-time abandonment deadline concurrently with an optional certification login. `takenKeyIds` bookkeeping on success is scoped to the demo retrieval grid only — a real card-swipe return was never reflected there (separate demo-vs-real data sources). Verify: compile/assemble only at the time.
- **Post-pull reconciliation**: merged a diverged pull safely after confirming zero file overlap between local/remote changes. Fixed a Kotlin gotcha: `retrievalTerminal` mixed `Terminal` (server snapshot) and `ManagedTerminalOption` (demo fallback) across two assignment sites — Kotlin silently widened the type to `Any`, no compile error until every `.field`/`.copy()` access broke. Fixed via a `Terminal.toManagedTerminalOption()` adapter. Ran a `webApp`-vs-`web/` portal audit; findings are now superseded — see `CLAUDE_SHARED.md`'s Module architecture table for current state, not this entry.
- **Cavotec theme rework (v1)**: `ui/theme/{Color,Typography,Theme}.kt`, reusable `StatusRingCard` applied across every hardware/lifecycle status indicator. Superseded by a later Outfit/hex iteration (below) — trust `Color.kt`/`Typography.kt` in the tree over any hex values quoted in older diary text. Red/alarm tone was never confirmed live on hardware (see Known issues).
- **Personnel/credential sync + theme iteration (terminalApp half, external pull)** — backend-side half (route + migration) lives in `CLAUDE_BACKEND.md`. `TerminalApiClient` gained user/site/credential list+create+complete+revoke methods (credential complete/revoke for NFC/fingerprint/face were later disabled under policy A — see `CLAUDE_BACKEND.md`, not a bug if unused now). Theme: Inter → Outfit font, hex retuned (e.g. primary `#3966B1` → `#5B7FC4`); new `CabinetConnectionHints.kt` (`HintSeverity` LIKELY/CHECK/OK), `HardwareStatusPage.kt`, `SoftComponents.kt`. `web/` separately shipped its own MD3 "Cavotec" theme (see `CLAUDE_WEB.md`; primary `#0055a5`, Outfit + IBM Plex Mono, `components/ui/` kit) — same brand direction, not a literally shared token source. Fixed a build break from this pull: `TerminalApiException.statusCode` → the real property `.status`.
- **`releaseKeyForPickup` deleted as confirmed-dead code** — zero call sites once Key Take Flow shipped `beginKeyTake` (which already does the correct `blueLightOn → engageElectromagnet → ejectDoor() → checkDoorStatus` sequence). Deleted the method + its dangling KDoc cross-reference. Does **not** mean the door-eject sequence is hardware-verified by itself — see status-truth table / Known issues for that.
- **Real audio feedback (beep + voice lines) v1**: `AudioFeedbackController` — `SoundPool` beep (same clip, volume-only `loud` param, caller owns the 1s repeat interval) + `MediaPlayer` voice lines (PLEASE_TAKE_THE_KEY / PLEASE_INSERT_THE_KEY / PLEASE_CLOSE_THE_DOOR), placeholder WAVs in `res/raw/`. Deliberately skips `AudioManager.requestAudioFocus()` — dedicated kiosk device, beep and voice-line must play concurrently, never duck/pause each other. Bug: `MediaPlayer.create(context, resId)` calls `prepare()` internally before returning, so a later `setAudioAttributes()` was too late on this device's custom `awplayer` pipeline — silent playback. Fixed: construct `MediaPlayer()` bare → `setAudioAttributes()` first → `setDataSource()` via `AssetFileDescriptor` → `prepareAsync()`. Confirmed audible on real hardware, concurrent, no glitches (beep engine itself later switched to `MediaPlayer` too — see Sound test tooling entry below).
- **Pairing Part 2 (terminalApp UI)**: `TerminalPairingScreen` (6-digit entry) gates the whole app until redeemed; default API base URL `https://kms-cvt.com`, with a collapsed "Advanced: server address" override on the pairing screen itself (a 6-digit code can't encode a URL). `pairWithCode()` stores TERMINAL_DEVICE tokens in the same slots `login()` uses; on success, best-effort `syncCoordinator.bootstrap()`. **Gate is keyed on `cabinetId.isNotBlank()`, deliberately not `apiClient.isAuthenticated`** — `signOut()` already clears tokens on every personnel logout, so gating on token presence would re-show pairing after the very first logout. Scope confirmed via grep to be exactly device-to-account pairing — NFC/password/fingerprint/face login, `CardUidResolver`, Take/Return Flow, hardware protocol all untouched. Verify: compile-verified only; pairing endpoint itself wasn't deployed yet at the time (backend Part 1: see `CLAUDE_BACKEND.md`).
- **Fingerprint + face enrollment (real implementations, terminalApp half)** — backend raw-material guard half lives in `CLAUDE_BACKEND.md`. Audit first found: NFC card enrollment was already real; fingerprint/face login chips were static labels with no integration. R503 port confirmed against real wiring: `/dev/ttyS0` @ 57,600 8N1 (same vendor AAR terminalApp already uses; ported from sibling repo `../eKMSHardwareTester`, not built from a spec).
  - **Fingerprint**: `shared/.../protocol/R503FingerprintProtocol.kt` (mirrors `KeyCabinetLink`'s `SerialTransport` split). Sleep-based finger-polling deliberately stays in terminalApp's `FingerprintHardwareController` (Kotlin Multiplatform has no cross-platform sleep). One shared per-packet read timeout (not a fresh timeout per sub-read). 15 unit tests (`handshake`/`checkSensor` frames hand-computed byte-for-byte). The R503 owns the template on-device (0-199 slot) — no separate opaque-reference design needed: `FingerprintTemplateStore` reports `fptemplate_<id>` (the real on-device slot) as the `enrollmentReference` directly. Local `userId↔templateId` map is plain SharedPreferences, not Keystore-encrypted (an integer slot isn't credential material). Revoke deletes the sensor slot (0x0C) before clearing the local mapping.
  - **Face**: ported `OpenCvFaceEngine` (YuNet+SFace), `MediaPipeFaceLandmarkerEngine` (468-landmark+blendshape), `FaceTemplateEnrollmentSession` (5-sample averaging), `SFaceEmbeddingExtractor`, `FaceModelStore`. Added `org.opencv:opencv:5.0.0.1` + `com.google.mediapipe:tasks-vision:0.10.35` — empirically confirmed compatible with the pinned AGP/compileSdk/Kotlin baseline via a real `assembleDebug` (native `.so`s present for arm64-v8a/armeabi-v7a/x86/x86_64). Model assets ~41MB: `face_detection_yunet_2023mar.onnx` (227KB), `face_recognition_sface_2021dec.onnx` (37MB), `face_landmarker.task` (3.6MB) — see `CLAUDE_SHARED.md` Toolchain / Known issues below for the Git LFS/history-bloat story. `FaceProfileStore.enrollmentReference` = `faceref_<uuid>`, alongside the existing AES-256-GCM Keystore-encrypted embedding (raw embedding never reaches the backend — boundary #2 in `CLAUDE.md`).
  - **RGB-only active liveness, v1 — user-confirmed tradeoff**: RGB-only reuses working code but is a real (not hypothetical) weaker anti-spoof tier, vulnerable to a convincing video/photo replay. RGB+IR matches the vendor manual (§4.8.3/4.8.4, not present in this repo) but no IR-liveness algorithm exists anywhere (tester, this repo, or any dependency) — building toward it now would mean guessing both the fusion architecture and the anti-spoof algorithm. **Decision: RGB-only for v1, explicit plan to upgrade later** — an intentional, temporary, lower-assurance choice, not a placeholder to silently forget. The original liveness challenge (blink+head-turn) was later replaced by head-turn-only — see Combined hardware verification session below, not repeated here. `FaceCameraController` (Camera2, RGB camera `"1"`), `FaceEnrollmentScreen` (`AndroidView`-wrapped `TextureView`+overlay). `frameBusy` `AtomicBoolean` backpressure guard — skips a frame rather than queuing an unbounded backlog if inference falls behind capture cadence. `CAMERA` permission + `<uses-feature>` added. Not ported: `FaceVerificationSession` (login/matching — enrollment only, same precedent as fingerprint).
  - Verify (at the time): 15/15 new R503 unit tests + full shared suite, compile/assemble pass. No physical R503/camera hardware this session — see Combined hardware verification session below for the later hardware run.
- **Git LFS configured** for face-model assets, forward-only, no history rewrite. `.gitattributes` (repo root) tracks `*.onnx`/`*.task`/`*.tflite`/`*.pb`/`*.bin` under `terminalApp/src/main/assets/models/`; the three existing model files re-added as LFS pointers (working-tree bytes unchanged, verified before/after). History rewrite to strip the pre-LFS ~41MB blob was deliberately **not** done — see Known issues for the reasoning. `git lfs install` is a required one-time local setup step (see `CLAUDE_SHARED.md` Toolchain).
- **Combined hardware verification session** (Take/Return, fingerprint, face — real F7G18P + R503 + camera; user's rule: stop and report on any failure).
  - **Part 1 (Take/Return) — stopped early by explicit user request, not fully completed.** Take Flow confirmed on real hardware: full door sequence, 20s abandonment ceiling measured at **20.09s** (~100ms of spec), abandonment correctly re-locks + light-off, Take Warning Time countdown starts on confirmed fob **removal** (not door-open), success path, door-left-open logged distinctly. Return: a real card-swipe correctly resolved a node via `CardUidResolver`. Two bugs found, left open — see Known issues: (1) duplicate `KEY_RETURN_ABANDONED` events ~28s apart from one swipe (cause unconfirmed at the time); (2) continuous beep audibly stops a few seconds after door-open with no 5s escalation (unconfirmed hypothesis: the `MediaPlayer` voice-line start interrupts the concurrent `SoundPool` beep loop on this device's `awplayer`/CedarX stack). Return's success/insertion path never attempted this session. User's exact words ending Part 1: *"please stop part 1. will look into this later on. the process is too chunky. i will remake the process after this."* — read as intent to redesign Take/Return, not just patch these two bugs; the Return Flow rework (below) is that redesign.
  - **Part 2 (fingerprint) — hardware behavior confirmed working; backend sync issue is now moot under policy A.** Real 6-scan `AutoEnroll` → `fptemplate_0` confirmed in `FingerprintTemplateStore`'s SharedPreferences. Re-scanning an already-enrolled finger correctly hit the R503's own duplicate-detection (0x27). Revoke (0x0C) confirmed via successful re-enrollment.
  - **Part 3 (face) — three real hardware/runtime bugs found and fixed, then full local pipeline confirmed working.**
    1. Camera preview blocked: `TextureView.SurfaceTextureListener` was attached inside a `LaunchedEffect`, but `AndroidView`-hosted `TextureView` can fire its one-shot `onSurfaceTextureAvailable` before that effect runs. Fixed: check `textureView.isAvailable`/`surfaceTexture` up front and attach directly if already available (keep the listener for the normal case too).
    2. Liveness unreliable (user: *"rework this liveness method. its ineffecient"*): root cause was sampling-rate/aliasing — the ~350ms frame-pump interval is too coarse for a natural ~100-400ms blink, compounded by a fragile sequential multi-gate state machine with no partial credit. Per the user's explicit choice (simplify the challenge, not fix the sampling), deleted `ActiveBlinkLivenessChallenge.kt`, replaced with `ActiveHeadTurnLivenessChallenge.kt` (single gesture, random left/right head-turn, 15s timeout — a sustained geometric state tolerates coarse polling; a blink doesn't).
    3. Crash on the very next capture after the liveness fix: `FaceProfileStore.encrypt()` pre-generated an IV via `SecureRandom` and passed it into `Cipher.init(ENCRYPT_MODE, key, GCMParameterSpec(...))` — AndroidKeyStore's default `setRandomizedEncryptionRequired(true)` rejects a caller-supplied IV for `ENCRYPT_MODE` (`InvalidAlgorithmParameterException`), a hardware-only failure no compile check could catch. Fixed: `cipher.init(ENCRYPT_MODE, key)` with no IV, read the Keystore-generated IV back via `cipher.iv` post-init (`decrypt()` unaffected — it legitimately needs the known IV). **The same bug pattern also existed in the original `../eKMSHardwareTester` code this was ported from** — its save path was apparently never exercised against a real AndroidKeyStore either.
    After all three fixes: full enrollment (liveness → 5-sample capture → encrypted save) confirmed end-to-end on real hardware; a held-up photo correctly **timed out** against the head-turn challenge (not an adversarial spoof test — no deliberate photo/screen-tilt attempt was made). Verify: hardware-confirmed — the first entry in this file where that's literal, not just compile/assemble.
- **Auto-launch on boot, startup self-diagnostic, network bring-up** (terminalApp-only). `BootCompletedReceiver` (`BOOT_COMPLETED`, launches `MainActivity` with `FLAG_ACTIVITY_NEW_TASK`) + `RECEIVE_BOOT_COMPLETED`/`ACCESS_NETWORK_STATE`/`ACCESS_WIFI_STATE` permissions. `NetworkStatusController`: `ConnectivityManager`-based (not `EthernetManager`, which needs a system-level permission most OEM images won't grant), prefers Ethernet over Wi-Fi when both present; Wi-Fi bring-up is a best-effort `WifiManager.reconnect()` nudge only (modern Android has no API to force-connect a specific saved SSID; explicit "no new pairing UI" instruction). `StartupDiagnosticsScreen`: runs on every cold launch/crash-recovery relaunch (`rememberSaveable`, resets on process restart not background/foreground); one line per subsystem (cabinet/fingerprint/camera-presence/NFC/network), reuses `HintSeverity` (+new `FAIL` case). Camera check is presence-only (`CameraManager.cameraIdList` contains the RGB camera id) — a full `FaceCameraController` instantiation would load OpenCV/MediaPipe on every boot. NFC line reuses the same `PublicCardReaderController` instance `TerminalAdminApp` already owns (`TerminalNfcReaderController`/`PublicM1CardReader` both claim `/dev/ttyS2` exclusively — can't have two owners). The screen actively triggers `hardwareController.connect()`/`fingerprintHardwareController.connect()` if not already connected — a real behavior change, these serial ports now open proactively at every launch instead of lazily on first action. Verify: hardware-confirmed for the diagnostics screen itself (5/5 checks resolve live, Continue dismisses correctly, background/foreground doesn't re-show it). Boot receiver itself, Ethernet-preferred path, and fully-offline state remain untested — see Known issues.
- **Sound/voice-line audit** (no code changed): confirmed via grep that WAV files are referenced only from `AudioFeedbackController` (safe to drop-in replace by filename). Checked two candidate new-sound flows (wrong-slot return warning, continuous-session "Done") against actual code — neither existed yet at the time, so no placeholder `VoiceLine` entries or raw resources were added.
- **Key Menu & multi-key sequential Take Flow** — implements UX Baseline §3 above (full behavior spec there). `beginKeyTake` refactored into `beginKeyTakeInternal` + new `beginQueuedKeyTake` (adds the red→blue "your turn" transition) + new `beginMultiKeyRedLightSequence` (per-node `redLightOn`, one worker-thread operation, no batch command). New `TerminalKeyMenuScreen.kt`, `SuperAdminRoute.KEY_MENU`, `MultiKeyTakeQueue`. **Spec/code discrepancy caught**: the one-electromagnet guard lives in `shared/.../protocol/KeyCabinetLink.kt` (`engagedNodeAddress`/`ElectromagnetConcurrencyException`, §10.4), not `CabinetHardwareController` — the latter only adds its own app-level `takeMonitoring` guard on top. Verify: compile/assemble only — see Known issues for what's still unverified (no Technician/Vendor account existed on the live backend to test through — declined to create a throwaway one).
- **Return Flow rework** — implements UX Baseline §2's "Return Flow rework additions" above (full behavior spec there: wrong-slot detection, continuous session, checkout record). New `TerminalCheckoutStore.kt` (local-only stand-in per the baseline spec). `ReturnFlow` gained `SessionIdle` + an `attemptId` (`System.nanoTime()`) field. **Duplicate-abandonment root cause**: the old abandonment-race `LaunchedEffect` was keyed on the *entire* `ReturnFlow` value, so a failed certification-login retry (same attempt, only `loginError` differs) produced a structurally-different value and relaunched the coroutine — a real TOCTOU gap where an already-completed `delay()` and a fresh login result could both decide to log the same event. Fixed two ways, both required: key on `attemptId` alone (stable across login retries), and re-confirm the live state is still that same `attemptId` immediately before logging/clearing. Believed structurally fixed, not yet hardware-reverified (Known issues). **Beep-continuity bug explicitly NOT touched by this rework** — it's an `AudioFeedbackController` audio-driver issue; a state-machine change can't fix it, and claiming otherwise would be the exact "assume the rewrite fixes it" mistake to avoid. Verify: compile/assemble only, not hardware-verified at all (Known issues).
- **Wrong-slot detection revised**: uses `testMicroSwitchAndCard` (0x17), not bare `testMicroSwitch` (0x16) — identity via card UID, not presence alone. Byte semantics (protocol §5.4, reused from `waitForReturnedKeyFob`'s existing encoding): a readable 4-byte value is a card UID, all-`0x00` is "bolt present, no card," all-`0xFF` is "nothing present." `pollForKeyInsertion` gained a `resolveKeyFobUid` callback (worker-thread, must stay fast/side-effect-free) checking **both** personnel-card and key-card stores via `CardUidResolver`, returning true only for `CardUidMatch.Key` — per the permanent NFC UID Resolution Rule (`CLAUDE_SHARED.md`). **Deliberate design choice**: unresolved presence (no card, or an unenrolled UID) still triggers the same alarm as a confirmed wrong key, differentiated only by wording ("Wrong key" vs "Unrecognized item") — a real enrolled key should always carry a readable card, so anything else is equally anomalous. Sweep scope unchanged (configured `KeySlot`s only, not the full 1-127 range). Verify: compile/assemble only.
- **Beep-continuity investigation** (code-only). Two fixes applied: (1) both Take/Return beep-loop `LaunchedEffect`s were keyed on `beeping` **and** `beepLoud` (Return's also on `wrongSlotNodeAddress`) — the moment the 5s-escalation callback flips `beepLoud` and fires the voice line, Compose cancel-relaunched the beep coroutine (real, likely jitter not full stoppage). Fixed: key on `beeping` alone, read the rest via `rememberUpdatedState`. (2) Judged the more likely actual root cause: beep (`SoundPool` at the time) and every voice line (`MediaPlayer`) shared one `AudioAttributes` instance — **theory**: this device's confirmed non-stock `awplayer`/CedarX HAL groups/preempts streams sharing usage/content-type, plausibly killing the beep stream when a voice line with matching attributes starts. Fixed: split into `beepAudioAttributes` (`USAGE_NOTIFICATION_EVENT`/`CONTENT_TYPE_SONIFICATION`) and `voiceLineAudioAttributes` (`USAGE_ASSISTANCE_ACCESSIBILITY`/`CONTENT_TYPE_SPEECH`) — preserved through the later beep-engine switch below. `MAX_CONCURRENT_BEEPS=2` audited and ruled out as a factor (moot now that beep no longer uses `SoundPool`). Verify: compile/assemble only — not hardware-reverified against the original concurrent scenario (Known issues).
- **Boot auto-launch diagnosis**: a real reboot test showed the app did not auto-launch (not the "stopped app" `BOOT_COMPLETED` exemption — the app had been manually opened before). Re-verified `BootCompletedReceiver`/manifest line-by-line: intent-filter action string, `enabled`/`exported=true`, `RECEIVE_BOOT_COMPLETED` placement, `onReceive()` logic, `FLAG_ACTIVITY_NEW_TASK`, target class/package all confirmed correct. **No bug found.** Added unconditional `BootAutoLaunch`-tagged diagnostic logging at 4 points (`onReceive()` entry, action-mismatch branch, pre-`startActivity()`, try/catch around `startActivity()`) so the next reboot test's logcat pinpoints the failing stage. Deliberately no OEM/vendor autostart-permission workaround built (unverified whether even needed on this hardware). Verify: logging in place, not yet exercised by an actual reboot (Known issues).
- **Sound test tooling (temporary, fully removed) + beep engine switch + chirp root-cause**. Built two throwaway pieces (`TerminalSoundTestScreen`, standalone `SoundTestActivity` launched via adb component name, bypassing the pairing gate) to verify WAV drop-ins while the device was mid-pairing-gate. A 15-rep/1s beep loop + all 3 voice lines ran clean on real hardware across several runs — but the tool runs the beep loop to completion then plays voice lines separately, so it **never reproduced beep-loop-concurrent-with-voice-line** and does not hardware-reverify the beep-continuity fixes. Both tools fully removed after use (grep-confirmed zero remaining references). Found/fixed along the way: the very first beep of a fresh `AudioFeedbackController` could silently no-op because `SoundPool.load()` is async and a beep ~280ms after construction could race ahead of `beepLoaded` — added a `Log.w` for the skip case. **At the user's explicit request** (told in advance this would not fix distortion), switched `beep()` from `SoundPool` to a one-shot `MediaPlayer`, matching `playVoiceLine()`'s construction pattern; kept the split `beepAudioAttributes`/`voiceLineAudioAttributes` from the fix above. Real tradeoff: `MediaPlayer` prepares fresh every call (~30-50ms observed) vs `SoundPool`'s zero-latency after one-time load — judged imperceptible at a 1s repeat cadence. **Chirp root-caused, not fixed**: PCM analysis of a user-replaced `beep.wav` showed a steady ~1300-1350Hz tone (not a real frequency sweep) with 43.9% of samples clipped at/near the 16-bit digital ceiling — harmonic distortion from a too-hot export, confirmed independent of playback engine or software volume scaling. Recommended re-export with ~-6dB headroom; not done as of writing.
- **Phase 1 (Revised) terminalApp half**: full entry (schema, migration, tables) lives in `CLAUDE_BACKEND.md`. terminalApp side: `UserRole` gained `REGIONAL_ADMIN`; every exhaustive `when` over the enum fixed; Vendor role hard-excluded from fingerprint/face enrollment at the call site. Verify: shared/terminalApp compile-verified.
- **Phase 3: Login Screen Rework**. `TerminalLoginScreen` is now a 5-tile method chooser (`LoginMethod`: `PASSWORD`/`FINGERPRINT`/`FACE`/`NFC_CARD`/`PASSKEY`), each routing to its own dedicated screen. Password and NFC Card are presentation-only changes — underlying logic untouched. **Fingerprint and Face login are genuinely new** (previously static, non-functional chips): fingerprint wires `FingerprintHardwareController.identifyFingerprint()` — the first-ever caller of `R503FingerprintProtocol.autoIdentify(startTemplateId=0, templateCount=FINGERPRINT_LIBRARY_SIZE=200)` — resolving a matched template to a user via new `FingerprintTemplateStore.userIdForTemplate()`. Face login captures a live 2-sample embedding (`FaceCameraController.startLogin()`, `LOGIN_REQUIRED_SAMPLES=2` vs. enrollment's 5, reusing the same `FaceTemplateEnrollmentSession` class) and cosine-matches it against every locally-enrolled profile; **`FACE_MATCH_SIMILARITY_THRESHOLD = 0.40f`** (`FaceCameraController.kt`) is an explicitly flagged, uncalibrated placeholder — needs real hardware tuning before this number means anything. Passkey login is a UI shell only, not functional. Role-based post-login routing consolidated into one `postLoginRoute()` helper (used by all 4 login methods) and a new `TerminalSession.isAdminTier` (`role == SUPER_ADMIN || REGIONAL_ADMIN`) replacing 7 separate `isSuperAdmin` checks across `TerminalAdminApp.kt`. **Kotlin gotcha hit while wiring this**: local `fun` declarations resolve by *source order*, not execution order — a closure stored in an earlier `remember{}` block cannot reference a local `fun` declared later in the same composable even though the closure only runs later; fixed by moving `postLoginRoute` near the top of the function. Verify: compile/assemble only — no hardware run of fingerprint/face login matching at all.
- **Phase 7: Admin Menu 17-item permission matrix**. New `AdminFieldMode` (`EDIT`/`VIEW`/`HIDDEN`) framework, resolved once per field via `adminMenuFieldMode(role, field)` against a private 12-item `AdminMenuField` enum (items 1-12 of the matrix) — replaces scattered `if (role == ...)` checks. Super Admin: everything `EDIT` (unchanged). Regional Admin: server address, activation code, MAC address, and password change are `HIDDEN` (genuinely absent, not greyed out); everything else is `VIEW` (value shown, no input/save). `AccessGrantsScreen` made role-aware (item 13). Item 14 (Regional Admin's own site assignments) required threading `assignedSiteIds: Set<String>` end-to-end through `TerminalUser`/`TerminalSession`, both local caches, `UserDto.toTerminalUser()`, and `TerminalSyncCoordinator`/`authenticate*` — it never existed on `TerminalSession` before. New `TerminalOfficeHoursScreen` (item 15): Super Admin edits, Regional Admin view-only **at the terminal** even though they edit it on the web portal — a deliberate "terminal is always view-only for Regional Admin" rule. New `TerminalVendorPasskeyScreen` (item 16): **flagged judgment call** — the matrix frames this as Regional Admin's job on web, but this screen also gives Super Admin full approve/reject (matching the "full parity with web" rule used for every other item); confirm if this item is meant to be Regional-Admin-exclusive by design. Investigated (per instruction, not fixed) whether Admin Menu items 6/8-12 push to the backend — confirmed all local-only. Verify: compile/assemble only — no real Regional Admin/Technician account existed on the live backend at the time to test the matrix through an actual login.
- **Phase 7 follow-up: Site names for Regional Admin + Backend Sync card visibility (terminalApp half)** — backend allowlist half lives in `CLAUDE_BACKEND.md`. Reused terminalApp's existing `listSites()` rather than adding a new client method so item 14 can resolve `assignedSiteIds` to real site **names** instead of raw UUIDs. `TerminalAdminMenuScreen` gained an `onLoadSites` param + a `LaunchedEffect` resolving names for display. Backend Synchronization card restructured from one `if (role == SUPER_ADMIN)` block covering the whole card + Save into two independent blocks: Bootstrap/Download (read/pull) stay unconditional for Regional Admin, Push/Read (write/mutating) stay Super-Admin-only. Verify: compile/assemble only; not deployed.
- **Phase 5: Post-Take Logging, Real Checkout Sync, Overdue/Emergency/Close-to-Deadline Logic (terminalApp half)** — backend allowlist fix half lives in `CLAUDE_BACKEND.md`. Wires real backend sync into Take/Return, replacing `TerminalCheckoutStore`'s local-only stand-in with a local-cache-plus-backend-sync model (the store itself is kept, not removed — see its own doc comment). New `CheckoutDeadlinePolicy.kt` (terminalApp-only, `java.time`-based since `shared` also targets Wasm): `CLOSE_TO_DEADLINE_THRESHOLD_MINUTES = 60`, `EMERGENCY_WINDOW_HOURS = 3` — both flagged reasonable-default constants, not spec-derived. New `TerminalCloseToDeadlineScreen.kt` — blocking manual-entry (24-hour `HH:MM` clock time, rolls to tomorrow if already past — **flagged input-style judgment call**, a duration-based picker was the alternative) or Emergency choice, resolved **once per take session** (single-key or multi-key), before the physical hardware sequence starts; wired via extending `TakeFlow`/`MultiKeyTakeQueue` with a `checkoutDeadline` field and a new cross-cutting `when` branch (same pattern as the `ReturnFlow` states) intercepting ahead of `route`. An office-hours fetch failure is deliberately treated the same as "needs a decision," not a hard block — Emergency remains a working offline escape hatch even if the backend is unreachable. `TerminalCheckoutRecord` gained nullable `backendCheckoutId`/`backendRevision`/`backendDueAtEpochMillis`/`backendIsEmergency`/`backendEmergencyWindowEndsAtEpochMillis`, populated via `attachBackendInfo()` once the create-sync succeeds — the return-time close-out `PATCH` echoes these back unchanged (no `GET` route needed or allowlisted). Both the take-side `POST` and return-side `PATCH` calls are fired non-blocking from `handleTakeFlowOutcome`/`handleReturnFlowOutcome` — a failure never blocks or undoes the physical take/return, only logs a new `KEY_CHECKOUT_SYNC_FAILED` audit event locally. Verify: `:shared:compileDebugKotlinAndroid`, `:terminalApp:compileDebugKotlin`, `:terminalApp:assembleDebug`, `:shared:testDebugUnitTest` all pass; not deployed; no hardware run at all.
- **Phase 9 design-system rework — foundation + pilot only, not a full screen sweep** (terminalApp-only). Audited every terminalApp `ui/` file for hardcoded `Color(0x...)` outside `theme/`: **none found** — `Color.kt` was already the sole source of raw hex. Unified the two existing brand blues into named tokens rather than dropping either: `CavotecBlue` (`#0055A5`, matching `web/`'s primary) is now the system PRIMARY; the prior terminal-only primary, `CavotecBlueLight` (`#5B7FC4`), is the lighter ACCENT/secondary. Added a fourth semantic token, `info` (`#3B8FD1` light / `#64AEE0` dark), alongside the existing success/warning/danger trio — reused `HintSeverity`'s existing LIKELY→warning/OK→success/FAIL→danger mapping (`ConnectionHintCard` in `HardwareStatusPage.kt`) as the starting point rather than inventing a new scheme; `info` itself isn't wired into that mapping yet (CHECK still renders as neutral surface) — left for a future sweep, not silently changed. `Theme.kt`'s `EkmsTerminalTheme` now takes a `darkTheme: Boolean` param and builds either a `lightColorScheme()` or `darkColorScheme()` from one of two token sets (`LightEkmsColors`/`DarkEkmsColors`), both derived from the same `Color.kt` tokens rather than independently picked. Outfit stays the typeface unchanged (no dark-mode font issue found). New `TerminalThemePreferences` (`data/`, own `SharedPreferences`, same one-class-per-concern pattern as `TerminalApiClient`'s base-URL storage): `TerminalThemeMode` (`SYSTEM`/`LIGHT`/`DARK`), `SYSTEM` is the first-launch default (follows `isSystemInDarkTheme()`), explicit toggle sets `LIGHT`/`DARK` from then on — device-local, never backend-synced, same footing as `serverAddress`/`activationCode`. **Toggle placement (flagged, reconsider if wrong)**: a plain `Switch`-based `ThemeModeToggle` in `TerminalLoginScreen`'s top-right corner, reachable pre-authentication since it's a cosmetic device preference, not routed through the Admin Menu's 17-item permission matrix. New reusable `IconActionButton` (`ui/IconActionButton.kt`), parameterized by `ActionButtonType` (`CANCEL`/`ACCEPT`/`ADD` today, enum documented as extensible for `EDIT`/`DELETE`/`DONE` later without a redesign) — icon+label together, never icon-only. **New dependency, flagged**: `androidx.compose.material:material-icons-core` (small official Compose artifact, the base ~20-icon set only — not the much larger `material-icons-extended`), added because the project had zero icon library before this pass; version `1.7.6` picked to match `material3 1.3.2`'s Compose UI 1.7.x lineage. Because core (not extended) was the deliberate choice, the login screen's dark/light toggle uses a `Switch` + text label instead of sun/moon glyphs, to avoid pulling in extended for one control. Wired into exactly two pilot spots per scope: `TerminalLoginScreen` (tokens apply automatically via `MaterialTheme`/`LocalEkmsColors`, already indirected everywhere in that screen — no per-screen token changes needed) and the Admin Menu Controls page's physical-action confirm/cancel `AlertDialog` (`pendingPhysicalAction`, reached from door/electromagnet engage/release controls) now uses `IconActionButton(ACCEPT)`/`IconActionButton(CANCEL)` in place of raw `Button`/`TextButton`. Take/Return Flow, Key Menu, Office Hours, and Vendor Passkey screens deliberately untouched this pass. **Follow-up flagged, not fixed here**: `SoftAssistChip` (`SoftComponents.kt`) and `ConnectionHintCard` (`HardwareStatusPage.kt`) reference `SoftSuccessContainer`/`SoftWarningContainer` as fixed light-mode-only `Color` constants (not scheme-aware) — harmless before dark mode existed, but will look inconsistent (light chip fills on a dark screen) now that dark mode is a real, user-facing toggle; needs theme-aware container tokens in the next sweep, not a problem this pass introduced elsewhere. Verify: `:terminalApp:compileDebugKotlin` and `:terminalApp:assembleDebug` both pass; `:shared:testDebugUnitTest` unaffected/still passes. **No hardware run this pass** — dark mode contrast/legibility on the real F7G18P display, and the corner toggle's touch target, are both unverified on device.
- **Phase 9A hardware-bug fix, `TerminalLoginScreen` only** — two visual bugs confirmed on-device from the Phase 9 pilot's real hardware screenshot pass, both root-caused and code-fixed, **neither yet re-verified on hardware** (compile/assemble only; the user is re-screenshotting to confirm — do not treat as done until that lands). (1) **Method icons not rendering**: `SoftScanTile` never actually drew an icon — it rendered a single letter (`title.take(1)`) inside a badge Box hardcoded to `Color.White.copy(alpha = 0.75f))`, which ignored dark mode entirely and read as a stray white square once dark mode existed. Fixed by giving `SoftScanTile` a real `icon: ImageVector` param and swapping the badge's hardcoded white for `MaterialTheme.colorScheme.primaryContainer` (already theme-correct per-mode via `Theme.kt`). The five tiles now pass `Icons.Filled.Password`/`Fingerprint`/`Face`/`Nfc`/`VpnKey` — none of which exist in `material-icons-core` (only in `material-icons-extended`), so the dependency swapped from core to extended (`libs.versions.toml`/`terminalApp/build.gradle.kts`, same `1.7.6` version) — **flagged size tradeoff**: extended bundles the full classic Material icon set (several MB unshrunk vs. core's near-nothing); accepted because the specific glyphs needed live only there. Re-checked `IconActionButton`'s `Close`/`Check`/`Add` still resolve under extended (confirmed via a clean `compileDebugKotlin`, not assumed) — extended is a superset of core so no change was needed there. (2) **Light-mode cards invisible**: `SoftScanTile`'s non-listening `containerColor` (`MaterialTheme.colorScheme.surface`, i.e. `CavotecPanelLight` `#FFFFFF`) sat almost exactly on the screen's own background (`CavotecSurfaceLight` `#F5F6FA`) with `SoftCard`'s hardcoded `0.dp` elevation and no border — near-zero contrast, card boundary effectively invisible. Fixed by giving `SoftCard` optional `elevation`/`border` params (both default to the prior `0.dp`/`null` so every other `SoftCard` call site — `HeaderCard` and everything else built on it — is visually unaffected) and having `SoftScanTile` specifically pass `elevation = 3.dp` and `border = BorderStroke(1.dp, MaterialTheme.colorScheme.outlineVariant)`, which is visibly distinct from the fill in both light and dark (checked dark wasn't regressed: dark's `surface`/`background` pair, `CavotecPanelDark`/`CavotecSurfaceDark`, already had a real gap, and the added border/elevation are additive on top, not a replacement). Verify: `:terminalApp:compileDebugKotlin` and `:terminalApp:assembleDebug` both pass. **Still needs the user's on-device re-screenshot before either bug is considered actually closed.**
- **Phase 9A follow-up polish, light-mode-only, `SoftScanTile` fill** — dark mode confirmed good on hardware from the prior fix and deliberately untouched here. Root cause: the resting (non-`listening`) fill used the literal `MaterialTheme.colorScheme.surface` value, which Material3's `Card` auto-tints toward `surfaceTint` (`primary`) based on `elevation` (the tonal-elevation overlay) — a real, documented M3 behavior that gives visible depth in dark theme but is barely perceptible in light theme at the same `3.dp`. New `EkmsColors.isDark` field (`Theme.kt`, `false` on `LightEkmsColors`, `true` on `DarkEkmsColors`) lets `SoftScanTile` branch on the *actually active* theme (a local override, not necessarily the system setting) without threading a `darkTheme` param through every call site. New `restingContainerColorFor(colors, scheme)` in `SoftComponents.kt`: dark mode still returns the literal `scheme.surface` (byte-for-byte unchanged, so M3's existing tonal overlay there is untouched); light mode now returns an explicit computed tint, `colors.primary.copy(alpha = 0.08f).compositeOver(colors.panel)` — **exact value: `#EBF1F8`** (checked against the real light `ColorScheme`, not guessed: `CavotecBlue #0055A5` blended at 8% over `CavotecPanelLight #FFFFFF`), deliberately a *lower* alpha than the icon badge/`listening` state's `primaryContainer` (`0.12f` → `#E0EBF4`) so the badge stays a visually distinct accent chip inside the card rather than resolving to the identical color. Border (`outlineVariant`) and `elevation = 3.dp` are unchanged from the prior fix. **Contrast checked, not assumed**: the description text (`onSurfaceVariant` / `CavotecTextSecondaryLight #6B7380`, already explicit, left unchanged) drops from ≈4.78:1 against the old pure-white fill to ≈4.21:1 against the new `#EBF1F8` tint — technically just under the 4.5:1 WCAG AA guideline for small text, comfortably clears the 3:1 large-text/UI-component bar, and reads as a minor, flagged tradeoff rather than a real legibility problem; not further corrected this pass (narrowing the tint to chase 4.5:1 exactly would make the fill collapse back toward indistinguishable-from-background, defeating the fix). Separately, the **title text's color is now explicit** (`color = MaterialTheme.colorScheme.onSurface`, added to the `Text` call) — it previously had no explicit color and relied on `Card`'s `contentColorFor(containerColor)` auto-resolving via an exact-role match (`surface → onSurface`), which broke once light mode's fill became a custom blended `Color` that doesn't match any named `ColorScheme` role; `onSurface` resolves to the same `textPrimary` token it already rendered with in both modes, so this is a no-visual-change safety fix, not a design change. Verify: `:terminalApp:compileDebugKotlin` and `:terminalApp:assembleDebug` both pass. **Not yet hardware-reverified** — compile/assemble only, per instruction; needs the user's next on-device screenshot to confirm the light-mode depth actually reads as intended and dark mode truly didn't move.
- **Phase 9B: fixed `SoftAssistChip`/`ConnectionHintCard` theme-unawareness (flagged in the original Phase 9A audit) + applied the Phase 9A pattern to `TerminalKeyMenuScreen`.** No hardware run — compile/assemble only, per instruction.
  - **Root cause, both components**: they referenced `Color.kt`'s `SoftSuccessContainer`/`SoftWarningContainer`/`SoftSuccessOnContainer`/`SoftWarningOnContainer` literals directly — light-mode-only hex with no dark-mode counterpart at all (unlike `MaterialTheme.colorScheme.errorContainer`, which Theme.kt already derives correctly per-mode, which is why FAIL/danger never needed fixing).
  - **New tokens** (`Color.kt`): dark-mode success/warning container+onContainer pairs (`SoftSuccessContainerDark #243732`/reuses `CavotecSuccessOnDark`; `SoftWarningContainerDark #3A362D`/reuses `CavotecWarningOnDark` — both containers computed the same way Theme.kt's own `tertiaryContainer`/`errorContainer` are, tone composited at 14% over the mode's panel), plus a brand-new info pair for **both** modes (light: `SoftInfoContainer #E4EFF9` / `SoftInfoOnContainer #1E5A8A` — a hand-picked dark navy, not `CavotecBlueDark`, so info reads distinct from primary-styled elements; dark: `SoftInfoContainerDark #283540` / reuses `CavotecInfoOnDark`). Light success/warning values are **unchanged** — the Phase 9A audit found light already correct, only dark was ever missing.
  - **Contrast checked for every new pair, not assumed**: computed contrast of each onContainer against its own container — success light ≈5.78:1, success dark ≈5.22:1, info light ≈6.25:1, info dark ≈5.19:1 — all clear the 4.5:1 AA target with room. (Warning pairs reuse the pre-existing, already-shipped light values and the same-recipe dark values — not independently re-derived, since the light half was never flagged as a problem.)
  - **New `SoftTone` enum + `softToneColors()` in `Theme.kt`**: resolves a `SUCCESS`/`WARNING`/`INFO` tone to its container/onContainer pair for the *actually active* theme (via the `EkmsColors.isDark` flag from the Phase 9A follow-up, not `isSystemInDarkTheme()`). Both `SoftAssistChip` (`SoftComponents.kt`) and `ConnectionHintCard` (`HardwareStatusPage.kt`) now call this instead of touching the raw Color.kt constants — one fix point for any future tone, not two independent hardcodes.
  - **`info` wired into `HintSeverity`**: `ConnectionHintCard`'s `CHECK` arm ("worth verifying") now uses `softToneColors(SoftTone.INFO)` instead of falling back to a plain neutral `surfaceContainerLow`/`onSurface` — the token existed since Phase 9A but was never connected anywhere until now. `LIKELY`→warning, `OK`→success, `FAIL`→M3's own `errorContainer` are unchanged in behavior, just re-routed through the same helper (FAIL stays on `errorContainer` directly, untouched).
  - **`SoftAssistChip` call sites found via grep, every one checked for continued visual sense (not just compile)**: `HardwareStatusPage.kt` (hardware connected/connecting/disconnected chip), `TerminalAdminApp.kt` (5 sites — hardware status chip, server-linked/local-only unit chips, no-units-loaded warning, short-password warning, hardware Controls page connected chip), `StartupDiagnosticsScreen.kt` (2 — hardware/network health chips), `TerminalPasskeyLoginScreen.kt` (1 — "Not yet available" chip), plus `SoftComponents.kt`'s own internal use inside `SoftWaitPanel`. All are `success`/`attention` (green/amber) usages — none use a hypothetical neutral/info variant through `SoftAssistChip`'s public API (it only exposes `success`/`attention` booleans, unchanged), so every site's semantics are identical, only the underlying color source changed.
  - **`TerminalKeyMenuScreen` rework**: `SoftScanTile` gained a `selected: Boolean` param (extended, not forked, per instruction) — `selected` gives a stronger `primary`-tinted border (`2.dp` vs the resting `1.dp` `outlineVariant`) + `primaryContainer` fill, independent of and never combined with `listening`'s pulse animation (Key Menu never sets `listening`; the login screen never sets `selected`). `KeyMenuBox` (the Layout Display grid tile) is rebuilt on `SoftScanTile` instead of the plain `StatusRingCard` it used before — `taken` isn't a new SoftScanTile param, it's handled at the call site (`onClick = null` + `Modifier.alpha(0.55f)`), since it's a state specific to this one screen, not worth a third state on the shared component. `KeyMenuList` (the List Display alternative) is **deliberately untouched** — confirmed by reading it that it already used the theme-aware `StatusRingCard`, and list-style rows are meant to look different from tiles by design (same as `TerminalKeyRetrievalScreen`'s own list view). `SoftSegmented` (the Layout/List toggle itself) needed **no changes** — confirmed by reading it that every color already resolved via `MaterialTheme.colorScheme`. New `KeyMenuEmptyState` (icon badge + title + message, all `MaterialTheme.colorScheme` tokens) replaces the old bare `Text("No keys are currently assigned...")`. The confirm button is now `IconActionButton(ActionButtonType.ACCEPT, label = <dynamic "Select a key..."/"Take N keys">)` in place of the old `SoftPrimaryButton` — a genuine ACCEPT-shaped action (confirming a pending multi-select), so it fit the existing type without forcing it; the top "Sign out"/back `TextButton` was left alone — it's page-level navigation, not a modal confirm/cancel pairing, so CANCEL wasn't forced onto it. Key tiles use `Icons.Filled.VpnKey` (same icon already used for Passkey login — reused rather than guessing at an unverified icon name). Take/Return Flow, Admin Menu (beyond the existing Phase 9A dialog), Office Hours, and Vendor Passkey are untouched, per instruction.
  - Verify: `:terminalApp:compileDebugKotlin`, `:terminalApp:assembleDebug`, and `:shared:testDebugUnitTest` all pass. **Needs the user's on-device screenshot in both light and dark** to confirm `TerminalKeyMenuScreen`'s new tiles/empty-state/confirm-button actually read as intended, and that the `SoftAssistChip`/`ConnectionHintCard` dark-mode fix looks right on real hardware (no prior dark-mode screenshot of either component exists).
- **Phase 9C: applied the Phase 9 pattern to `TerminalOfficeHoursScreen` (Admin Menu item 15) and `TerminalVendorPasskeyScreen` (item 16), visual/theme only.** `AdminFieldMode` resolution, the Regional-Admin-view-only rule, and the Super-Admin-full-parity-on-vendor-passkey logic are all unchanged — no product-decision code touched, per instruction. No hardware run — compile/assemble only.
  - **Audit result for both screens**: neither had any hardcoded `Color(...)` — both already built entirely on `MaterialTheme.colorScheme`/`typography`, unlike the `SoftAssistChip`/`ConnectionHintCard` case in Phase 9B. Nothing to fix there; the work this pass was component/layout choices, not color-token bugs.
  - **`TerminalOfficeHoursScreen`**: edit-mode's 3 `OutlinedTextField`s are now grouped in one `SoftCard` (was three bare fields floating directly on the page) — a spacing/depth consistency change matching the rest of the sweep (e.g. `HardwareStatusPage`'s connect-form fields), not a functional change; 12dp inter-field spacing is preserved explicitly inside the card (previously inherited for free from `TerminalPage`'s own Column, which no longer applies once the fields are one card instead of three separate top-level items). The Save button is now `IconActionButton(ActionButtonType.ACCEPT, label = "Save office hours")` in place of the old `SoftPrimaryButton`. **No Cancel button added** — the screen never had a distinct Cancel action (only Save + the shared page-level Back button), and inventing one would be a functional addition, out of scope for a styling pass.
  - **Office Hours' view-only vs. edit-mode distinction — audited, judged already adequate, left unchanged.** Edit mode: bordered/labeled `OutlinedTextField`s (Material's canonical "this is editable" affordance) + a filled `IconActionButton`. View-only mode: plain label-caption-over-value rows with zero input chrome, inside a card — the standard Material read-only pattern. This contrast (visible input borders + save action vs. flat text-only rows) was already a clear, textbook edit-vs-readonly signal before this pass; no additional badge/treatment was added, per the instruction's own "if adequate, leave it and say so" escape hatch.
  - **`TerminalVendorPasskeyScreen`**: request status (`PENDING`/`APPROVED`/`REJECTED`) now renders via `SoftAssistChip` ("Pending" attention / "Approved" success / "Rejected" neutral) instead of plain `Text("Status: ...")` — reuses the same tone language `HardwareStatusPage` already uses, theme-correct in both modes via Phase 9B's `softToneColors` fix (not re-verified here, not re-litigated). Approve/Reject are now `IconActionButton(ACCEPT)`/`IconActionButton(CANCEL)` in place of the old `Button` (filled) + `TextButton` (plain-text) pair — a real, fixed visual-weight imbalance: the old pairing made Reject look like a lesser, second-class action purely from component choice, despite both being equally final, equally real actions a Super Admin can take; both are now the same size/shape (`Modifier.weight(1f)` each), differing only in ACCEPT (filled, success tone) vs. CANCEL (outlined, neutral tone) styling. New `VendorPasskeyEmptyState` (icon + title + message, same shape as `TerminalKeyMenuScreen`'s `KeyMenuEmptyState` from Phase 9B) replaces the old bare `Text("No vendor passkey requests...")`.
  - **Flagged, not silently assumed**: the task's instruction to "confirm both roles see the same approve/reject UI" is read here as **both actions** (Approve vs. Reject) needing equal visual weight — not literal Super-Admin/Regional-Admin parity, since that would directly contradict the same instruction's "do not touch the Regional-Admin-view-only rule." Regional Admin still sees the list only, with zero approve/reject controls, exactly as before — that gating (`canEdit`) was not touched. If "both roles" was meant literally, this pass did not implement it — flag back if that reading is wrong.
  - Verify: `:terminalApp:compileDebugKotlin`, `:terminalApp:assembleDebug`, and `:shared:testDebugUnitTest` all pass. **Needs the user's on-device screenshot of both screens, in both light and dark, and for both Super Admin and Regional Admin roles on Office Hours** (its view-only vs. edit-mode contrast has never been seen on real hardware in either mode).
- **Phase 9D: applied the Phase 9 pattern to the remaining Admin Menu surface — `TerminalAdminMenuScreen`'s 12 field items (`AdminMenuField`) and its Backend Sync card.** Visual/theme only — `adminMenuFieldMode`'s resolution, which items are `EDIT`/`VIEW`/`HIDDEN` per role, and the Backend Sync card's Bootstrap/Download-vs-Push/Read role split are all byte-for-byte unchanged. No hardware run — compile/assemble only.
  - **HIDDEN fields confirmed still genuinely absent, not just visually suppressed** — verified by reading the actual code after every edit, not assumed: `if (modeOf(AdminMenuField.PASSWORD_CHANGE) != AdminFieldMode.HIDDEN) { ... }` (and the same pattern for `SERVER_ADDRESS`/`ACTIVATION_CODE`/`MAC_ADDRESS`) still wraps the composable call itself — a HIDDEN field emits zero composables, not a disabled/greyed row. `AdminMenuToggle`'s `if (mode == AdminFieldMode.HIDDEN) return` early-return is untouched. The Backend Sync card's `if (role == TerminalUserRole.SUPER_ADMIN) { Push; Read }` guard is untouched — Regional Admin still gets zero Push/Read buttons, not disabled ones.
  - **Tile-grid vs. list-row decision (the task's "your call")**: kept the existing row shapes (`AdminMenuStringField`/`AdminMenuReadOnlyField`/`AdminMenuToggle`) rather than rebuilding on `SoftScanTile`. Reasoning: `SoftScanTile` is a tappable *selection* tile (icon badge, `onClick`, `listening`/`selected` pulse) meant for choosing between a handful of options (Login method, Key Menu) — this screen is a single scrolling settings *form* where every row is a labeled input or a plain value, never a "pick one" choice. Forcing tile semantics onto form fields would be a mismatch, so the fix here is a re-skin (plain M3 `Card` → `SoftCard`) of the existing row shapes, not a component swap.
  - **What actually changed**: `AdminMenuReadOnlyField`, `AdminMenuToggle`, the MAC address block, and the Backend Sync card all swapped their plain M3 `Card` for `SoftCard` (same row-visual-language fix as everywhere else in the sweep). The "Save Admin Menu settings" button is now `IconActionButton(ActionButtonType.ACCEPT)` — the same "confirm a pending settings edit" case Office Hours' Save button fit in Phase 9C; no Cancel button was added (none existed before — Save + the shared Back button is this screen's whole action set, same as Office Hours). Bootstrap/Push/Read/Download and "Modify administrator password" stay plain `OutlinedButton` — already theme-correct via M3 defaults (nothing hardcoded to fix), and none of them is an ACCEPT/CANCEL/ADD-shaped action (standalone sync triggers / screen navigation, not a pending edit to confirm or dismiss), so `IconActionButton` wasn't forced onto them, per the task's own scoping (Section 3 asked only for "theme tokens" on these four, not a component swap).
  - **`OutlinedTextField`/`Switch` audit**: no custom widgets beyond the standard `OutlinedTextField` (Key Node Setting, Take/Door-Close Warning Time — all plain numeric-filtered text fields, no stepper or other custom control actually exists in this codebase despite the task's "e.g. numeric stepper" hypothetical) and `Switch` (the three toggle fields) — both already theme-correct via M3 defaults, nothing to change beyond the container swap above.
  - **Scope note, flagged**: item 14's "Your assigned site(s)" row (rendered inline on this screen, not a separate file) shares `AdminMenuReadOnlyField` with items 1-12 and so incidentally also picked up the `SoftCard` re-skin — its content/logic was not touched, only the shared visual shell it was always going to inherit from. Read this as in-scope (it's the same screen, same shared helper) rather than a violation of "do not touch item 14."
  - Verify: `:terminalApp:compileDebugKotlin`, `:terminalApp:assembleDebug`, and `:shared:testDebugUnitTest` all pass. **Needs the user's on-device screenshot in both light and dark, for both Super Admin (full edit) and Regional Admin (partial view-only + hidden fields) roles** — this screen has never had a hardware pass under either role in either theme.
- **Phase 9E: applied the Phase 9 pattern to `TerminalKeyTakeScreen` (single-key + the Key Menu multi-key queue's per-node view, confirmed to be the same composable, not two) and `TerminalCloseToDeadlineScreen`.** Visual/theme only. **No timer/countdown logic, timing constants, state-transition conditions, `AudioFeedbackController`/beep-voice-line call path, `CabinetHardwareController`'s take/removal/door-close hardware sequencing, or `CheckoutDeadlinePolicy`/office-hours-gate code was touched** — confirmed via `git diff` after every edit, not just by intent: `TerminalKeyTakeScreen`'s diff is exactly two additions (a KDoc block and one new line reading an already-computed `stage` value into a presentational parameter); zero lines changed inside any `LaunchedEffect`, the `onBeginTake`/`onPollRemoval`/`onWaitForDoorClose` callback chain, or the beep-loop `LaunchedEffect`. `TerminalKeyReturnScreen.kt` (Return Flow) was not opened for editing at all this pass — confirmed via `git status`. No hardware run — compile/assemble only, per instruction.
  - **`TerminalKeyTakeScreen` audit findings**: this screen already had zero hardcoded colors — it's built entirely on `SoftWaitPanel`/`StatusTone` (already theme-correct in both modes since Phase 9A). No numeric countdown/timer display exists anywhere in this screen — confirmed by reading the code, not assumed — so none was invented, per the "if no such escalation exists today, do NOT invent one" instruction. No manual/fallback demo control exists here either — that's `TerminalKeyReturnScreen`'s `resolveReturningKey` null-key tap convenience, a Return Flow concept that doesn't apply to this file; the task's mention of it didn't match this screen's actual contents.
  - **Door-left-open now has the distinct urgent treatment the task asked for**: found that `WaitingForDoorClose(warningExpired = true)` (door-left-open) previously rendered with the **exact same** `StatusTone.ATTENTION` look as the ordinary "still waiting, nothing wrong yet" `WaitingForDoorClose(warningExpired = false)` state — no visual escalation existed despite the text/assist-chip already changing. Fixed by adding a new `strongAttention: Boolean = false` param to `SoftWaitPanel` (defaults preserve the exact prior look for every other call site) — when `true`, adds a `colors.warning`-bordered card, reusing the existing warning tone (same one `HintSeverity`/`SoftAssistChip` use), not a new color. `TerminalKeyTakeScreen` passes `strongAttention = (stage as? TakeStage.WaitingForDoorClose)?.warningExpired == true` — a pure presentational read of a boolean the untouched state machine already computes, not a new condition.
  - **Flagged, not fixed, per the hard constraint's "stop and flag it" instruction**: `SoftWaitPanel`'s default card look (`elevation = 0.dp`, no border) has the same "may blend into the light-mode background" issue Phase 9A found and fixed in `SoftScanTile` — but `SoftWaitPanel` is also used by `TerminalKeyReturnScreen` (Return Flow, explicitly off-limits this pass) plus `TerminalNfcCardLoginScreen`/`TerminalFingerprintLoginScreen`. Fixing the *default* look would touch Return Flow's rendering, so it was left as-is; only the new opt-in `strongAttention` param was added, affecting nothing else. (Fixed in a later pass — see Phase 9F below.)
  - **`TerminalCloseToDeadlineScreen`**: Manual-entry now has its own `SoftCard` (title + field + button), matching Emergency's existing card shape — before this it was a bare field+button floating on the page next to Emergency's fully-cased choice, reading as an afterthought rather than an equally deliberate option (same imbalance shape as Phase 9C's Vendor Passkey Approve/Reject fix, judged to genuinely apply here too). "Use this return time" is now `IconActionButton(ActionButtonType.ACCEPT)` — a clean fit, the screen's one normal-path confirm action. **"Mark as Emergency" deliberately stays a plain `Button`, not `IconActionButton`** — flagged rather than forced: `ACCEPT`'s success/green tone would misrepresent an emergency declaration as a calm positive confirmation, and `IconActionButton` has no warning/emergency-toned type today (only `CANCEL`/`ACCEPT`/`ADD`) — adding one would be a component-surface change beyond this pass's visual/theme scope. Instead it's tinted with the existing `warning` token (`LocalEkmsColors.current.warning`) instead of the default primary color, so the two paths read as distinct (normal = accept/green, urgent = warning/amber) without forcing a mismatched component or inventing a new one.
  - Verify: `:terminalApp:compileDebugKotlin`, `:terminalApp:assembleDebug`, and `:shared:testDebugUnitTest` all pass. **Needs the user's on-device screenshot of both screens in both light and dark** — specifically the door-left-open warning border on `TerminalKeyTakeScreen` (never seen on real hardware) and the two-card layout + warning-tinted Emergency button on `TerminalCloseToDeadlineScreen`.
- **Phase 9F: applied the Phase 9 pattern to `TerminalKeyReturnScreen` (stricter pass than 9E — the `attemptId`-keyed abandonment-race fix and the wrong-slot detection sweep here are both still only "believed correct by code review, not hardware-reverified") + `ReturnSessionScreen`, and fixed `SoftWaitPanel`'s default look (flagged, not fixed, in Phase 9E).** Visual/theme only. **No `LaunchedEffect` key, `attemptId` comparison, polling/timing logic, `CardUidResolver` call, `SessionIdle`/`SESSION_IDLE_TIMEOUT_MILLIS` condition, `resolveReturningKey`'s behavior, `TerminalCheckoutStore` read/write, or any `AudioFeedbackController`/beep-loop/hardware-command code was touched** — confirmed via `git diff` after every edit, same rigor as Phase 9E: `TerminalKeyReturnScreen.kt`'s diff is imports, a KDoc block, and two `Card`-block edits below the render `Box`, all after every `LaunchedEffect` in the file; zero lines changed inside the `onBeginReturnFlow`/`onPollInsertion`/`onWaitForDoorClose` callback tree or the beep-loop `LaunchedEffect`. `ReturnSessionScreen.kt` has no timer/hardware logic at all to touch (confirmed by reading it — the idle timeout is owned entirely by `TerminalAdminApp.kt`, untouched this pass). No hardware run — compile/assemble only, per instruction.
  - **`SoftWaitPanel` default fix (Part 1 of the task)**: found via grep — 4 real call sites plus the definition: `TerminalKeyTakeScreen`, `TerminalKeyReturnScreen`, `TerminalNfcCardLoginScreen`, `TerminalFingerprintLoginScreen`. All 4 checked and confirmed correct after the change (none passed a custom `elevation`/`border` before, so all 4 uniformly gain a visible border — no call site regresses). Exact fix: default `elevation` `0.dp → 3.dp`, default `border` `null → BorderStroke(1.dp, MaterialTheme.colorScheme.outlineVariant)` (same recipe `SoftScanTile` already uses) — this is the DEFAULT now, not another opt-in param, per the task's explicit instruction. `strongAttention` (Phase 9E) stays deliberately stronger than this new default: `4.dp` elevation / `2.dp` `colors.warning` border, vs. the default's `3.dp` / `1.dp` neutral border — Take Flow's door-left-open state still reads as more urgent than an ordinary wait, just no longer "more urgent than an invisible card."
  - **`TerminalKeyReturnScreen` states covered**: normal waiting-for-insertion and the door-close wait inherit the `SoftWaitPanel` default fix automatically (no per-state change needed, already theme-token-clean). Wrong-slot alarm now has the most urgent treatment on the screen — `2.dp` `colorScheme.error` border + `4.dp` elevation (same weight as Take Flow's `strongAttention`), reusing the already-established alarm/danger tone (`errorContainer`/`onErrorContainer`), not a new tone tier — this already was the strongest tone `StatusTone` has (`ALARM`), so no new tone tier was added, matching the task's fallback instruction. "Wrong key" vs. "Unrecognized item" now also gets a distinguishing icon (`Icons.Filled.VpnKey` vs. `Icons.AutoMirrored.Filled.HelpOutline`) alongside the wording that already differed — both keep the identical alarm tone and card treatment, per the established "equally anomalous" design intent; nothing about the underlying `confirmedKey` resolution was touched. Abandoned/Failed states inherit `SoftWaitPanel`'s `ALARM`-tone treatment, already correct. `checkoutSummary`'s card (the "Checked out by X · Ym ago" display) gained the same light `1.dp` `outlineVariant` border for light-mode visibility — was bare before.
  - **Flagged, not implemented, per the hard constraint's "stop and flag it"**: the task asked for a "5s-escalation state" reskin (reuse warning tone once `PLEASE_INSERT_THE_KEY` fires) analogous to Take Flow's door-left-open — but no such visual state exists in this screen's code. `beepLoud` (the 5s flag) only ever drives `audio.beep(loud = ...)`; it never changes `stage`/title/message/assistText, unlike Take Flow's `warningExpired`, which was already a dedicated pre-existing stage field with its own render branches before any Phase 9 pass touched it. Making the 5s mark visible here would mean adding a brand-new render-time condition where none exists today — judged a real (if small) behavior addition, not a reskin, and left untouched.
  - **No dedicated Success render state exists** — matching Key Take Flow's identical shape (see Phase 9E), a successful return calls `onCompleted()` immediately with no intermediate success screen to reskin.
  - **Scope note, flagged**: `resolveReturningKey` itself is a pure `ManagedKey?` lookup with zero presentation — its actual on-screen control (the "Simulate key-card return" button) lives in `TerminalNfcCardLoginScreen.kt`, a different, unnamed screen. Left untouched (out of this task's named scope) — it was already theme-clean (`SoftWaitPanel`/`SoftTextButton`) and incidentally still benefits from this pass's `SoftWaitPanel` default fix. `SessionIdle`/the Done button live in `ReturnSessionScreen.kt`, a separate file not part of `TerminalKeyReturnScreen`'s own `ReturnStage` — treated as in-scope anyway since the task's checklist named these items explicitly and the file is tightly coupled to the same feature; "Done" is now `IconActionButton(ActionButtonType.ACCEPT)` in place of `SoftPrimaryButton`, a clean fit (the one confirm action ending the session).
  - Verify: `:terminalApp:compileDebugKotlin`, `:terminalApp:assembleDebug`, and `:shared:testDebugUnitTest` all pass. **Needs the user's on-device screenshot of `TerminalKeyReturnScreen` and `ReturnSessionScreen`, in both light and dark** — the wrong-slot card's new border/icon and the `SoftWaitPanel` default-border fix (which also now affects `TerminalNfcCardLoginScreen`/`TerminalFingerprintLoginScreen`, neither directly edited this pass) have never been seen on real hardware.
- **Digital Key hardware-feasibility test tooling (terminalApp side)**: pairs with the mobileApp-side test tooling (full entry lives in `CLAUDE_MOBILE.md`) via a not-yet-built terminalApp-side raw-reader-frame diagnostic screen (scoped in a prior session). Later fully reverted along with the mobileApp prototype — see the mobileApp clean-start entry in `CLAUDE_MOBILE.md`; terminalApp's revert was confirmed clean via `git diff`, and terminalApp was separately audited read-only during that pass: no natural Digital Key UI slot exists or is needed there; if ever built it would arrive via the existing NFC Card login tile, not a new one.
- **Passkey feature — terminalApp real login wiring** — full entry (Region schema, mobileApp screens) lives in `CLAUDE_MOBILE.md`. terminalApp: `TerminalPasskeyLoginScreen`'s previously-disabled UI shell now calls the real backend (`POST /v1/terminal/passkey-login`, see `CLAUDE_BACKEND.md`); on success resolves a normal `TerminalSession` via the same `authenticateByUserId` mechanism Fingerprint/Face already use, then routes straight into the take flow via a refactored-out `startMultiKeyQueue` helper shared with Key Menu's own multi-select path (confirmed via diff: zero behavior change to Key Menu itself). New `DueDateSource.PASSKEY_REQUEST` added (flagged/reasoned: AUTO/MANUAL would misrepresent this due-time's actual provenance, since it was fixed at request-approval time, not decided at take time) — purely additive, no exhaustive `when` broke. No other login method touched. Verify: terminalApp compiles+assembles clean; not deployed at the time.
- **Animated boot splash, replacing the always-visible `StartupDiagnosticsScreen`.** New `TerminalBootSplashScreen.kt`, shown at the exact same trigger point `StartupDiagnosticsScreen` used to own directly — same `rememberSaveable`-gated `showStartupDiagnostics` scope in `TerminalAdminApp`, untouched. The 5 diagnostic checks (cabinet/fingerprint/camera/NFC-reader/network) were **not rewritten** — `StartupDiagnosticsScreen.kt`'s `private` per-subsystem functions are unchanged; only a new `internal fun startupDiagnosticChecks(...)` was added there (factoring out the `listOf(...)` assembly `StartupDiagnosticsScreen`'s own composable now also calls) so both screens drive identical detection logic with nothing duplicated. **Discovery-phase correction to the task's own framing**: the screen that follows today is not a paired-vs-unpaired branch — the `isPaired` pairing gate always returns early *before* `showStartupDiagnostics` is ever reached, so diagnostics (and now the splash) only ever renders once already paired, and always falls through to the same `route` state (`SuperAdminRoute.LOGIN`) afterward. This meant no destination needed to be threaded through at all: `onContinue = { showStartupDiagnostics = false }` already reveals "whatever's next" exactly as before.
  - Logo: `res/drawable/cavotec_logo.png` (placed by the user before this pass; confirmed via search this was the only Cavotec logo asset anywhere in `terminalApp/res/` — no duplicate).
  - Animation (v1, flagged as an iterable placeholder): entrance scale 0.7x→1x + fade-in (`animateFloatAsState`, spring), then a continuous 1.0x–1.05x breathing pulse via `rememberInfiniteTransition` for as long as the splash is showing — reuses `SoftScanTile`'s existing pulse technique rather than a new one. No hardcoded background — relies on the surrounding Scaffold's theme background, same as every other full-screen composable (`TerminalPairingScreen`, etc.).
  - Navigates once BOTH conditions hold: every check is `OK`, and ≥1 second of splash time has elapsed (`MIN_SPLASH_DURATION_MILLIS`, a placeholder floor) — fast success still gets a full loop, slow success isn't cut short. Mode (splash vs. warning) is derived fresh from live check state every recomposition rather than latched into separate state, so a successful Retry or auto-recheck clears the warning card on its own.
  - On any `FAIL`: loop stops, `BootWarningCard` replaces the animation (not a separate screen/route). Plain-language summary (`checks.filter { FAIL }.joinToString { it.title }` — titles like "Cabinet link"/"Network", not raw error codes), a collapsed "View details" that reveals `ConnectionHintCard` per check (the exact existing row component, not rebuilt), a "Retry" button that re-issues the same guarded `connect()` calls the original startup probe made, and — only when the network check specifically is the failure — an "Open Network Settings" button launching `Settings.ACTION_WIFI_SETTINGS` via `Intent`, deliberately **not** a custom in-app Wi-Fi config screen (same precedent as `NetworkStatusController`'s existing Wi-Fi bring-up — see Known issues' network bring-up entry).
  - **Product decision surfaced and confirmed with the user before implementing**: the task's warning-card spec (Retry + Open Network Settings only) would have silently dropped `StartupDiagnosticsScreen`'s own documented "never blocks sign-in" invariant — e.g. a camera-less device would hit `FAIL` on every single cold launch forever, with Retry unable to fix a permanently absent camera, permanently stranding the terminal. User chose to keep a bypass: a small, deliberately lower-emphasis "Continue anyway" text link sits below Retry/Open Network Settings on the warning card.
  - Auto-recheck on resume: `LocalLifecycleOwner` + `LifecycleEventObserver` re-issues the same guarded `connect()` calls on `ON_RESUME` (covers returning from the Wi-Fi settings hand-off without a manual Retry tap); harmless no-op when hardware is already connected/busy.
  - `StartupDiagnosticsScreen` composable itself is unchanged in behavior/logic but now has **zero call sites** (only reachable pieces of it — `startupDiagnosticChecks` and `ConnectionHintCard` — are reused); left in place rather than deleted, since deleting a whole screen was beyond this task's "reuse, don't rebuild" scope — flag if it should be removed as dead code in a follow-up.
  - One pre-existing Compose API used here (`androidx.compose.ui.platform.LocalLifecycleOwner`) is flagged by the Compose Multiplatform 1.8.2 toolchain as deprecated in favor of `androidx.lifecycle.compose.LocalLifecycleOwner` (a different Gradle dependency, `lifecycle-runtime-compose`, not currently in the version catalog) — compiles clean, functionally correct, but flagged rather than silently pulling in a new dependency to chase the warning; see Known issues.
  - Verify: `:terminalApp:compileDebugKotlin`, `:terminalApp:assembleDebug`, and `:shared:testDebugUnitTest` all pass (the latter confirming `shared/` was genuinely untouched, per the task's own instruction to flag any shared/backend touch — there was none). **No hardware run in the same session this shipped** — compile/assemble-verify only, matching every other Phase 9x visual/flow change in this project. A follow-up session did install and launch it on a real device (`adb`-reported model `A133P`) — see the dedicated Known Issues entry below for exactly what that confirmed vs. what's still open (the happy path only; the warning card/Retry/Open Network Settings/bypass link were never exercised since every check passed on that device).
- **Boot splash sizing/timing follow-up: bigger logo, longer floor.** `TerminalBootSplashScreen.kt` only — failure-path behavior (warning card/Retry/Open Network Settings/Continue anyway) untouched. (1) `MIN_SPLASH_DURATION_MILLIS` `1_000L → 3_000L` (constant's doc comment updated to match, still flagged as an easy-to-tune placeholder, not a spec'd value — same "keep looping past the floor if checks take longer" logic, unchanged). (2) Logo went from a fixed `.size(140.dp)` box to `Modifier.fillMaxWidth(0.65f).aspectRatio(839f/147f)` — a **fraction of available width**, not a new fixed dp number, deliberately: this project's own screenshots/measurements (see the Part 2 audit note below) show the F7G18P/A133P actually renders **portrait** (confirmed 600x1024dp, not the 1024x600dp landscape the panel's native pixels would suggest), so a width-fraction scales correctly regardless of which of those two numbers turns out to be "the" screen width in a given context, where a hardcoded dp guess risked being right for one and wrong for the other. `839x147` is `cavotec_logo.png`'s real checked pixel size (a wide, short wordmark, not a square mark) — `aspectRatio` keeps it from ever stretching. On the confirmed 600dp-wide portrait panel this renders at ~390dp wide (65% of 600dp), up from the old ~140dp — a real, meaningful size increase, not just a config tweak.
  - Verify: `:terminalApp:compileDebugKotlin` and `:terminalApp:assembleDebug` both pass. **Not yet hardware-verified** — the on-device confirmation described in the Known Issues entry below was captured *before* this sizing/timing change (old 140dp logo, old 1s floor); nothing about the new ~390dp logo or the new 3s floor has been seen on the real device yet.
- **App-wide immersive mode (system bars hidden, edge-swipe reveal).** `MainActivity.kt` only. `applyImmersiveMode()` (private helper) sets `WindowInsetsControllerCompat.systemBarsBehavior = BEHAVIOR_SHOW_TRANSIENT_BARS_BY_SWIPE` and calls `hide(WindowInsetsCompat.Type.systemBars())` — platform behavior used as-is, no custom show/auto-hide timer logic. Called from both `onCreate` (after `super.onCreate`, before `setContent`) and `onResume` — the latter is load-bearing, not a defensive extra: Android clears the hidden-bars state on window focus loss/regain, and this app now has a real, already-shipped path that does exactly that (the boot splash's "Open Network Settings" button, added last session, launches the system Settings app; the user's return would otherwise leave bars permanently visible without the `onResume` reapply). **Device-config check done first, per instruction**: `com.hzmct.enjoyConfig` (EnjoyKit, v2.0.22) is confirmed installed on the real device, but empirically — with `com.ekms.terminal/.MainActivity` in the foreground, checked via `adb shell dumpsys window` — both the status bar (`TYPE_TOP_BAR`) and nav bar (`TYPE_BOTTOM_TAPPABLE_ELEMENT`) reported `visible=true` before this change. So EnjoyKit is not currently suppressing bars for this app; the in-app mechanism is not redundant with an existing device-level one. No `WindowCompat.setDecorFitsSystemWindows(window, false)` call added — deliberately not in the task's explicit instruction list, and not required for the legacy `View.SYSTEM_UI_FLAG_*` compat path `WindowInsetsControllerCompat` falls back to on this device's Android 10 (API 29, confirmed via `adb shell getprop ro.build.version.release`) — that's a newer (API 30+) platform-`WindowInsetsController` concept. Flagged in case a future higher-API device needs it revisited.
  - Verify: `:terminalApp:compileDebugKotlin` and `:terminalApp:assembleDebug` both pass; `androidx.core.view.WindowInsetsControllerCompat`/`WindowInsetsCompat` resolved cleanly as an existing transitive dependency (via `androidx.activity:activity-compose`) — no new Gradle dependency added. **Not yet hardware-verified** — the edge-swipe reveal, the auto-hide timeout feel, and specifically the Settings-app round-trip path (the whole reason `onResume` matters) have not been walked through on the real device.
- **App-wide UI click sound on button/toggle presses.** New `AudioFeedbackController.playClick()` — **originally** `SoundPool` playing a custom WAV with its own `AudioAttributes` pair; **superseded the same week** by the platform's native click sound (see the follow-up Completed entry directly below) — the coverage/wiring work described in this entry is otherwise still exactly how the sound reaches every call site today, only `playClick()`'s own internals changed.
  - **Asset check before wiring anything (per instruction)**: `res/raw/` held only `beep.wav` + 3 voice lines before this pass, confirmed via directory listing — no pre-existing generic click sound. The user-placed `cavotec_click.wav` (122190 bytes, mono/16-bit/44100Hz PCM, confirmed via `file`) was then checked against the stated target (~50-150ms, peak ~-6dB): **it did not meet either target** — measured duration was **~1.385s** (9-14x longer than intended) and peak was **0.00 dBFS with 161 samples (0.26%) at/near the digital ceiling** (real, if much milder, clipping — the same class of issue as the original `beep.wav` clipping incident). **Moot now**: this asset and the whole duration/clipping question it raised were sidestepped entirely by the native-sound switch below, not fixed — the file itself was deleted (see that entry).
  - **Coverage strategy**: a full inventory grep across every interactive tap surface in `terminalApp/.../ui/` was run before writing any code (`Button`/`OutlinedButton`/`TextButton`/`IconButton`/`IconActionButton`/`SoftScanTile`/`Switch`/`Modifier.clickable(`, plus `StatusRingCard`/`SoftCard`/`SoftNavTile`/`SoftHeroAction`/`SoftSegmented` once their own delegation chains were traced). New `LocalAudioClick` `CompositionLocal` (`ui/theme/Theme.kt`, alongside `LocalEkmsColors`, same `staticCompositionLocalOf` pattern) — a plain `() -> Unit`, not the whole `AudioFeedbackController`, provided from inside `EkmsTerminalTheme` itself so all three of `TerminalAdminApp`'s separate `EkmsTerminalTheme { ... }` call sites (pairing gate, boot splash, main scaffold — each its own early-return branch) get click support with zero changes to any of those call sites. **Trade-off flagged, not silently accepted**: because those three are different composition-tree positions, the backing `AudioFeedbackController` is recreated at the pairing→boot-splash and boot-splash→main transitions — a minor, once-per-launch inefficiency, not a correctness bug (even less of one now that there's no click sample to reload — see below), not worth the larger refactor of hoisting one instance above `TerminalAdminApp`'s early-return branches.
  - **9 shared components wrapped centrally** (each touched once, covering every call site through it): `SoftCard` (covers `SoftCard` itself + `SoftScanTile`, 6 call sites — the 5 login-method tiles + Key Menu's grid tile — + `SoftNavTile`, 10 call sites — the whole Super Admin dashboard grid), `SoftPrimaryButton` (8 call sites), `SoftTextButton` (4 call sites), `SoftHeroAction` (1 call site — "Take keys" dashboard hero), `IconActionButton` (9 call sites, gained a new `playSound: Boolean = true` param), `StatusRingCard` (3 interactive call sites — Key Retrieval's and Key Menu's List Display rows), `SoftSegmentOption` backing `SoftSegmented` (the Layout/List Display toggle, shared by both screens), `AdminMenuToggle`'s `Switch` (`TerminalAdminMenuScreen.kt`, covers all 3 Admin Menu toggle fields), `ThemeModeToggle`'s `Switch` (`TerminalLoginScreen.kt`, the dark/light corner toggle). ~46 individual tap/toggle surfaces now sound, through those 9 touch points. **None of these 9 wrap points or their call sites needed any change** for the native-sound switch below — confirmed via diff, exactly as expected from an internal-implementation-only swap.
  - **Exclusion decided, then reversed the same week** (see the "Close-to-Deadline click sound" Completed entry below for the reversal): `TerminalCloseToDeadlineScreen.kt` — the one screen from the "Take Flow, Return Flow, Close-to-Deadline" list that actually has any interactive buttons (`TerminalKeyTakeScreen.kt`/`TerminalKeyReturnScreen.kt` have **zero** buttons of any kind during their active state, confirmed via grep — nothing to exclude there because there was nothing to wrap). Its `IconActionButton` ("Use this return time") initially passed `playSound = false` explicitly; now uses the default `true` — see below.
  - **Raw call sites NOT covered this pass — flagged as a separate list, per instruction, not silently left out**: `CardEnrollmentScreen.kt` (6), `FaceEnrollmentScreen.kt` (4), `FingerprintEnrollmentScreen.kt` (3), `TerminalFaceLoginScreen.kt` (2), `TerminalFingerprintLoginScreen.kt` (1) — all raw `Button`/`OutlinedButton`; `TerminalKeyMenuScreen.kt:106` and `TerminalKeyRetrievalScreen.kt:96` — raw back/sign-out `TextButton`; `TerminalBootSplashScreen.kt` — its own raw "Open Network Settings" `OutlinedButton` and "Continue anyway" `TextButton` (custom-styled, not routed through `SoftPrimaryButton`/`SoftTextButton`); `TerminalAdminMenuScreen.kt` — 5 raw `OutlinedButton` (Modify password, Bootstrap, Push, Read, Download — deliberately not `IconActionButton`-wrapped per the existing Phase 9D "standalone action trigger" precedent); `TerminalAdminApp.kt` — the largest gap, roughly 25 raw `Button`/`OutlinedButton`/`TextButton` call sites across the real Super Admin CRUD screens (Access Grants, Personnel, Keys, Recycle Bin, password-change save). None of these were touched this pass — central-wrapper coverage was prioritized per the task's explicit preference, and this list is the "report before rolling out further" checkpoint. Still an open gap as of this week's follow-up — untouched by either the native-sound switch or the Close-to-Deadline reversal.
  - **Incidental finding, not part of this task's scope**: `SiteTerminalAdminScreen.kt` (18 raw button call sites, would otherwise have been in the list above) has **zero call sites anywhere in the app** — confirmed via grep for `SiteTerminalAdminScreen(`, only the function definition itself matches. It's dead/unreachable code, effectively the same category as the `terminalApp/reference/` scaffolds even though it isn't physically under that folder. Excluded from the coverage list entirely rather than reported as a gap; flagged here as a candidate for deletion in a future pass, not acted on in this one.
- **Click sound switched from custom WAV to the platform's native click.** `AudioFeedbackController.playClick()` rewritten: was `SoundPool` playing `cavotec_click.wav` with its own `clickAudioAttributes` pair; now `AudioManager.playSoundEffect(AudioManager.FX_KEYPRESS_STANDARD)` on `STREAM_SYSTEM` — the same click sound Android's own on-screen keyboard/UI uses. Every call site, all 9 shared-component wrap points, and the `LocalAudioClick` `CompositionLocal` wiring are **byte-for-byte unchanged** — confirmed via diff, exactly matching the task's own framing ("internal-implementation swap only, not a re-wire"). Removed as now-dead: `clickAudioAttributes`, `clickSoundPool`/`clickSoundId`/`clickSoundLoaded` state, `ensureClickSoundPoolReady()`, `CLICK_VOLUME`/`CLICK_MAX_STREAMS` constants, the `SoundPool` import, and the click-pool cleanup in `release()` (a system-owned `AudioManager` has nothing for this class to release). `cavotec_click.wav` deleted from `res/raw/` — no remaining reference anywhere, no reason found to keep it.
  - **This sidesteps the custom-asset clipping/duration defect entirely** (there is no asset — the ~1.385s/0dBFS-clipping problem flagged in the entry above is now moot, not fixed) **and is a genuinely different code path from beep/voice-line** — no app-owned `SoundPool`/`MediaPlayer`/`AudioAttributes` instance is involved in a click at all, `STREAM_SYSTEM` rather than this app's own custom `AudioAttributes` pairs — which plausibly also sidesteps the `awplayer`/CedarX HAL stream-grouping bug that's hit beep/voice twice. **That "plausibly" is reasoning, not a confirmed result** — still needs the same real-hardware Take/Return Flow interference check as the WAV-based version would have; nothing about this swap changes what still needs verifying, only which implementation is being verified.
  - **`SOUND_EFFECTS_ENABLED` investigated on the real device, per instruction, not assumed**: `adb shell settings get system sound_effects_enabled` → `1` (enabled) — this device plays sound effects by default, so `playSoundEffect()` is not currently gated off. `STREAM_SYSTEM` confirmed unmuted, at max volume (7/7), routed to the speaker (`adb shell dumpsys audio`). The app does **not** hold `WRITE_SETTINGS` (absent from the manifest, confirmed via `dumpsys package`/`appops get` — no grant, default mode) and **none was added** — per instruction, not to add a new permission speculatively when the setting is already enabled and there's nothing currently to toggle. Flagged, not solved: `SOUND_EFFECTS_ENABLED` is a device-wide system setting, not app-scoped — if a technician or Super Admin ever disables "Sound effects" in system Settings, `playClick()` would silently no-op app-wide (a real, if unlikely, Android behavior, not a crash) with no code-level way to detect or recover without `WRITE_SETTINGS`; left as a known, accepted gap unless the user decides otherwise.
  - **`TerminalCloseToDeadlineScreen` click-sound exclusion reversed (confirmed decision)**: its `IconActionButton`'s `playSound = false` override removed — the call site now uses the component's `true` default, same as every other `IconActionButton` call site. `IconActionButton`'s `playSound` param itself is unchanged/kept (still defaults `true`, doc comment updated to reflect it's currently unused by any call site) as a general opt-out escape hatch for any future flagged case, not removed outright.
  - Verify: `:terminalApp:compileDebugKotlin` and `:terminalApp:assembleDebug` both pass; `:shared:testDebugUnitTest` unaffected (`shared/` untouched). **No hardware test this pass, per instruction.** The Take/Return Flow audio-interference check flagged in the entry above **carries over unchanged** — it now needs to be run against this native-sound-effect implementation instead of the deleted custom WAV, not skipped because the code path changed.
- **Face/fingerprint login: auto-start, bigger preview, head-turn guide UI.** Scope was UI/trigger-timing only — `FaceCameraController`'s matching logic, `FACE_MATCH_SIMILARITY_THRESHOLD`, the embedding/cosine-match algorithm, and `identifyFingerprint()`'s R503 protocol calls are all untouched, confirmed via diff.
  - **Part 0 discovery (reported before implementing, per instruction)**: `ActiveHeadTurnLivenessChallenge` picks ONE random direction per attempt (confirmed correct, matching the task's assumption) but exposed only a coarse `{ state, message }` `Update` — the continuous `headTurnScore` passed into `consume()` was computed by `FaceCameraController` and fully discarded after the pass/fail comparison, never returned; the chosen direction was `private`, only reachable by parsing English text out of `message`. `TerminalFaceLoginScreen` saw even less — just `FaceLoginPhase.Liveness(message: String)`, the same string, once per frame. Reported this gap and asked before touching the challenge class's internals, per instruction; user chose to add minimal new state (see below) over a string-parsing hack or skipping the guide UI.
  - **`ActiveHeadTurnLivenessChallenge` additive changes**: `HeadTurnDirection` enum un-privated (was `private enum class`, now public) so a direction is a typed value, not text to parse. `Update` gained `direction: HeadTurnDirection` (populated on every `Update`, not just while waiting) and `progress: Float = 0f` (new `progressToward()` helper — `headTurnScore / signedThreshold`, clamped `0f..1f`; dividing by the *signed* threshold rather than its magnitude makes both directions' sign cancel out correctly in one expression with no separate LEFT/RIGHT branch). **`consume()`'s own pass/fail comparison (`headTurnScore >= LEFT_HEAD_TURN_MIN_SCORE` / `<= RIGHT_HEAD_TURN_MAX_SCORE`) is byte-for-byte unchanged** — `progress` is purely a new, additional, display-only value computed alongside it, confirmed via diff not to feed back into the decision.
  - **`FaceLoginPhase.Liveness` widened** (`direction`, `progress`, both forwarded straight from `Update`) — **`FaceEnrollmentPhase.Liveness` deliberately left untouched** (still just `message: String`), since only the login screen was in scope this pass; `FaceEnrollmentScreen.kt` was not opened for editing at all, confirmed via `git status`.
  - **`TerminalFaceLoginScreen` auto-start**: removed the old manual "Start facial recognition" first tap — `LaunchedEffect(hasCameraPermission)` now calls the same `startLogin()` local function automatically. Keyed on `hasCameraPermission` alone, deliberately — the one piece of state this effect actually needs to react to (a permission-grant callback flipping it false→true), not `loginPhase` or anything else the body touches, matching the same "key on the minimal state that should actually restart" lesson the beep-continuity `LaunchedEffect` re-keying fix and the `CardEnrollmentScreen` `PublicCardReaderController` fix already established in this project's history. Fires once on entry if permission's already granted (the common case after the first-ever grant); if not, requests it, then fires exactly once more when the callback flips the value. The retry button (`OutlinedButton`, relabeled "Try again" after `NoMatch`/`Failed`) stays — auto-start only replaced the *first* manual trigger, not post-failure retry.
  - **Camera preview enlarged**: `height(220.dp)` → `height(screenHeightDp * 0.4f)`, `screenHeightDp` read from `LocalConfiguration` — **not** `Modifier.fillMaxHeight(fraction)` as the task's own example suggested, because this screen's content sits inside `TerminalPage`'s `LazyColumn`, which gives children an unbounded max-height for scrolling; `fillMaxHeight(fraction)` has no bounded parent height to take a fraction of there (unlike the boot splash logo's `fillMaxWidth(fraction)`, which sits in a bounded `Box.fillMaxSize()`). Reading `LocalConfiguration.current.screenHeightDp` sidesteps that entirely — same "fraction stays correct on this confirmed-portrait 600x1024dp panel, a fixed dp is a guess" reasoning as the boot splash logo, just computed differently because of the different container. `0.4f` (~410dp on the real device) is flagged as an easy-to-retune placeholder, not a measured value.
  - **Head-turn guide UI**: new `HeadTurnGuide` composable — a direction arrow (`Icons.AutoMirrored.Filled.KeyboardArrowLeft`/`Right`, chosen over the deprecated non-`AutoMirrored` versions once the compiler flagged them) plus a `LinearProgressIndicator`, both bound to `FaceLoginPhase.Liveness.direction`/`.progress`, color animating from the theme's warning tone toward success green via `lerp`+`animateColorAsState` as progress approaches 1f. Shown only while `loginPhase is FaceLoginPhase.Liveness` — disappears naturally once the phase moves on, no extra guard needed.
  - **`TerminalFingerprintLoginScreen` auto-start**: `LaunchedEffect(Unit)` calls `startScan()` once on entry. `Unit` is the deliberately-correct key here (not a placeholder for "didn't think about it") — unlike face login, there's no reactive permission-grant signal to wait for; fingerprint hardware needs no runtime permission, and `identifyFingerprint()` handles its own sensor-connection state internally, untouched by this pass. A fresh screen entry is a fresh composition instance, so this still fires fresh every re-entry, same "once per entry" property as the face screen's effect. Retry button unchanged in behavior.
  - **Fingerprint panel size bump, judgment call flagged**: on this confirmed-portrait 600dp-wide device, the panel's existing `widthIn(max = 640.dp)` cap already didn't bind (640 > 600 — already full width, confirmed by the earlier layout audit), so there was no horizontal "enlarge" lever left, and `SoftWaitPanel` itself is a shared component used by Take/Return Flow and NFC login — changing its own size/padding was judged out of scope (wrong blast radius for a "modest... supporting-panel size bump"). Instead added a new 88dp `Icons.Filled.Fingerprint` icon, tone-colored via the existing `statusToneColor()` helper (`StatusRingCard.kt`), above the panel — local to this screen only, directly satisfies the task's own "and/or its 'place your finger' illustration" alternative without touching the shared component. Flag back if a literal panel-size increase (not the icon) was actually intended.
  - Verify: `:terminalApp:compileDebugKotlin` and `:terminalApp:assembleDebug` both pass; `:shared:testDebugUnitTest` unaffected (`shared/` untouched — all changes are in `terminalApp/`). **No hardware test this pass, per instruction** — neither the auto-start timing nor the head-turn guide's actual on-screen readability/color transition has been seen against a real camera/device; this is exactly the kind of thing that can't be meaningfully faked without live hardware, per the task's own framing.
- **New post-login landing chooser for Super Admin / Regional Admin — "Take Key" vs. "Admin Page."** Technician/Vendor's routing is untouched: `postLoginRoute()`'s `else -> SuperAdminRoute.KEY_MENU` branch is byte-for-byte the same line as before, confirmed via diff.
  - **Part 0 discovery (reported before implementing, per instruction)**: `postLoginRoute()` (one function, 5 call sites — every login method routes through it) previously sent `isAdminTier` sessions straight to `DASHBOARD`. `SuperAdminRoute` already had separate `KEY_RETRIEVAL` (Super Admin's unfiltered grid) and `KEY_MENU` (Technician/Vendor's filtered landing) entries — confirmed Regional Admin's new "Take Key" reuses `KEY_MENU` verbatim, not a fork. The dashboard's grid is actually **10 tiles**, not 8 (Office hours/Vendor passkey exist too) — the task's own summary undercounted; noted, didn't change the approach. **One gap found and fixed while checking the back/sign-out pattern, not explicitly asked for but a direct consequence of this change**: `KEY_RETRIEVAL`'s back button was already role-conditional ("Back to dashboard" for admin-tier, "Sign out" otherwise); `KEY_MENU`'s was hardcoded to "Sign out" — correct before this pass since only Technician/Vendor ever reached it. Left hardcoded, a Regional Admin reaching `KEY_MENU` via the new chooser would be forced to sign out just to get back, unlike Super Admin's equivalent `KEY_RETRIEVAL` path. Fixed by mirroring `KEY_RETRIEVAL`'s exact conditional pattern at the `KEY_MENU` call site — Technician/Vendor's own observed behavior is unchanged (still exactly "Sign out" → `signOut()`, confirmed since `isAdminTier` is `false` for both roles either way).
  - **New `SuperAdminRoute.LANDING_CHOICE`** + new file `TerminalLandingChoiceScreen.kt` (two `SoftHeroAction`s — "Take Key" / "Admin Page" — reusing the exact component the dashboard's old hero used, not a new one, plus a `SoftTextButton("Sign out")` matching the dashboard's own existing affordance so the chooser isn't a dead end for a wrong-account sign-in). `postLoginRoute()`'s `isAdminTier` branch now points here instead of `DASHBOARD` directly. "Take Key" branches by role at the `TerminalAdminApp` call site (not inside the new screen, which only reports "which button was tapped"): Super Admin → `KEY_RETRIEVAL`, Regional Admin → `KEY_MENU`, both via the existing `openAdmin()` guard rather than a raw `route =` assignment. "Admin Page" → `DASHBOARD` via the same `openAdmin()` path.
  - **No round trip back to the chooser** (confirmed assumption, not changed): every admin sub-screen's back button — including `KEY_RETRIEVAL`/`KEY_MENU`'s now-shared conditional pattern — returns to `DASHBOARD`, never to `LANDING_CHOICE`. `DASHBOARD` keeps its own (now-demoted, see below) Take Keys entry, so there's no need to route back through the chooser after the initial post-login choice.
  - **Consequence found and fixed, not explicitly asked about**: `passwordChangeReturnRoute`'s default (used by the forced first-login password-change gate, distinct from its `ADMIN_MENU` override for the voluntary in-Admin-Menu password change, which is untouched) pointed straight at `DASHBOARD`, bypassing the new chooser entirely for a brand-new Super Admin's very first successful login. Changed the default to `LANDING_CHOICE` — the forced password change is a required detour before normal post-login routing, not a separate landing decision, so it should land the same place a login without that gate would.
  - **Part 2 — "Take keys" demoted from `SoftHeroAction` to a normal `SoftNavTile`**, same shared component as the other tiles, first position (top-left) per instruction. Adding an 11th tile to a previously-even 10-tile 2-per-row grid leaves one row odd — rather than let the trailing tile (`Vendor passkey`, now last) stretch to fill its row (which would recreate the exact "looks different from everything else" problem being fixed), it keeps `weight(1f)` matching every other tile and a blank `Spacer(Modifier.weight(1f))` balances the row instead. Every existing tile's row-pairing shifted by one position to make room (e.g. Card enrollment now pairs with Take keys instead of Permission Settings) — cosmetic reshuffling only, no tile's own destination/label/behavior changed.
  - Verify: `:terminalApp:compileDebugKotlin` and `:terminalApp:assembleDebug` both pass; `:shared:testDebugUnitTest` unaffected (`shared/` untouched). **No hardware test this pass, per instruction** — this is a real navigation change to the primary landing flow for two roles; see the dedicated Known Issues entry below.
- **Personnel Management reworked into a list → per-user detail view; the 3 standalone Card/Fingerprint/Face enrollment tiles removed from the Super Admin dashboard grid.** Personnel Management is now the only path to any of the three. Terminal-side personnel **delete** stays web-only (Discovery confirmed the pre-existing screen never had a delete control here to begin with — there was nothing to remove; boundary #5's soft-delete/Recycle Bin flow already lives only on `web/`).
  - **Part 0 discovery (reported before implementing, per instruction)**: the old "Personnel Management" tile actually opened `EnrollUserScreen` — a create-personnel form with a flat, non-navigable `Card` list of existing personnel bolted on underneath (name/username/role only, no detail, no delete). `CardEnrollmentScreen`/`FingerprintEnrollmentScreen`/`FaceEnrollmentScreen` already each take a `users: List<TerminalUser>` and self-select via their own internal "· change" cycle button — none of them had (or needed) a dedicated "pick a target user" step, so scoping them to one person is a re-point (pass a singleton list), not new plumbing, exactly as anticipated. `TerminalUser` has no `staffId`/"No." and no "Additional number" field — both omitted from the detail view rather than invented, per instruction. `personnelCardStore`/`fingerprintTemplateStore`/`faceProfileStore` each already expose a real per-user enrollment-status query (`enrollmentFor`/`load`), so the detail view's status rows are backed by real data, not placeholders. `AdminMenuReadOnlyField` (`TerminalAdminMenuScreen.kt`) was `private` (file-scoped) — bumped to `internal` to reuse it, no other change. **Regional Admin scoping gap found, reported, not changed (per instruction)**: `personnelForScreens` (which Personnel Management also consumes) is not filtered by site/role anywhere in `terminalApp` — a Regional Admin session sees the exact same full personnel list a Super Admin does. This is a permission-boundary question, deliberately left to the user to decide separately — see Known issues.
  - **Real gap found during discovery, not anticipated by the task brief, surfaced and resolved with the user before implementing**: `CardEnrollmentScreen` conflates *personnel*-card and *key*-card NFC enrollment behind one Personnel/Key category toggle. Its only entry point was the dashboard's "Card enrollment" tile — removing that tile per Part 1 and replacing it with a per-user Personnel Detail row would have left Key-card enrollment (used by Return Flow's card-swipe/wrong-slot detection) with **no UI entry point at all**, since `EnrollKeyScreen` enrolls the electromagnet fob, a different thing entirely. User chose to add a small "Enroll a key card" sub-entry inside `EnrollKeyScreen` (Key enrollment) rather than dropping Key-card enrollment or leaving the toggle exposed on the personnel-scoped screen.
  - **`CardEnrollmentScreen` changes (entry/scoping only — the actual scan/enroll/revoke logic is untouched, confirmed via diff)**: gained `initialCategory: CardEnrollmentCategory = PERSONNEL` and `lockCategory: Boolean = false` params; when `lockCategory` is true the "Card kind" toggle button is hidden and `category` starts (and stays) at `initialCategory`. `CardEnrollmentCategory` un-privated (was file-scoped) so `TerminalAdminApp.kt` can reference it. Personnel Management's User Card row opens this screen with `users = listOf(thatUser)`, `keys = emptyList()`, `initialCategory = PERSONNEL`, `lockCategory = true`. The new Key enrollment sub-entry opens it with `users = emptyList()`, the real `keys` list, `initialCategory = KEY`, `lockCategory = true`.
  - **`FingerprintEnrollmentScreen`/`FaceEnrollmentScreen`: zero changes** — confirmed via diff. Both are re-pointed purely by what `TerminalAdminApp.kt` now passes as `users`: a singleton list containing only the Personnel Detail record they were opened from (Vendor still hard-excluded at this call site too, on top of Personnel Detail simply not rendering a clickable row for Vendor — belt and suspenders, matching the existing "hard-excluded, not just discouraged" rule).
  - **New file `PersonnelManagementScreen.kt`**: `PersonnelListScreen` (every known personnel record as a tappable `StatusRingCard` row — the same reusable list-row pattern `TerminalKeyMenuScreen`'s `KeyMenuList` already uses — plus an "Add personnel" button routing to the existing create form) and `PersonnelDetailScreen` (read-only `AdminMenuReadOnlyField` rows for Name/Identity-Role/Password, plus tappable status rows for User Card/Fingerprint entry/Face Registration). Password row is a fixed `"••••••"` placeholder only — never the real hash, never editable, per boundary #7. No Timetable/Multi-Authentication User Group rows exist, per instruction. Vendor users see the Fingerprint/Face rows rendered as non-interactive `AdminMenuReadOnlyField`s reading "Not available for Vendor role" rather than a live tappable status, since Vendor may never use either method.
  - **`TerminalAdminApp.kt` wiring**: new `SuperAdminRoute.PERSONNEL_LIST`/`PERSONNEL_DETAIL`, new `selectedPersonnelId` state (which record `PERSONNEL_DETAIL` and the 3 scoped enrollment routes apply to; `null` when `CARD_ENROLLMENT` is instead entered from the new key-card sub-entry). `EnrollUserScreen`'s now-redundant trailing personnel list was deleted (Personnel Management's own list screen replaces it) along with its now-unused `users` parameter; its title changed from "Personnel Management" to "Add personnel" since it's now a sub-screen reached via Personnel Management's "Add personnel" button, not the top-level destination. Dashboard grid dropped from 11 tiles (with an odd-row `Spacer` balancing hack) to a clean 8 tiles / 4 rows once the 3 enrollment tiles were removed — the `Spacer` hack is gone.
  - **`BackButton` gained an optional `label` param** (defaults to the exact prior hardcoded string, so all ~20 existing call sites are unaffected) — used by the two new screens to say "Back to Personnel Management" instead of the generic default where that's what's actually true.
  - Verify: `:terminalApp:compileDebugKotlin` and `:terminalApp:assembleDebug` both pass. **No hardware run this pass, per instruction** — see Known issues.
- **Door-closed card probe (temporary, fully removed) — answered the empirical question blocking Key node binding: does 0x17 work with the door closed?** Built a throwaway standalone `DoorClosedCardProbeActivity` (adb component-name launch, no launcher icon — same reachability pattern as the earlier `SoundTestActivity` precedent), ran it against the real F7G18P/A133P, then deleted the file and its manifest entry (confirmed via `git diff` — manifest byte-identical to HEAD, file absent). **Result: confirmed safe.** With the door closed (0x22 = `0xFF`), `testMicroSwitchAndCard` (0x17) returned a stable, distinct 4-byte UID for all 8 probed nodes, identical across 5 reads each, 2s apart — no unlock/eject needed, no instability. This directly enabled Part 3's background auto-scan design below (silent capture with the door shut). Incidental finding, unrelated to the probe itself and not acted on this session: a `TerminalKey` was found locally enrolled at node address 0 (the door) via the current guided-enrollment flow — flagged for Part 0 below.
- **Key node binding ("Key Attachment") — closes the previously-flagged gap that web-registered keys had no physical cabinet node binding.** Spans `shared`/`backend`/`web`/`terminalApp`; backend/web halves live in `CLAUDE_BACKEND.md`/`CLAUDE_WEB.md`. Compile/assemble-verified only this whole pass, explicitly flagged as needing a real hardware run more than most items in this project — see Known issues.
  - **Part 0 (isolated fix, done first)**: `EnrollKeyScreen`'s node-address field accepted `0` (the door) — found a real `TerminalKey` already enrolled there on the test device because of it (see the door-closed probe entry above). Fixed: bound to `KeySlotAccessPolicy.MIN_KEY_NODE_ADDRESS..MAX_KEY_NODE_ADDRESS` (was `0..MAX_KEY_NODE_ADDRESS`), labels/error text updated to say so explicitly.
  - **Part 1 discovery (reported before implementing Parts 2-5, per instruction)** — full findings shared across all three doc files; terminal-specific ones: (1) old Guided Key Enrollment's real sequence is session-level `ejectDoor()` once, then per-key `blueLightOn`+`engageElectromagnet`(unlock)+`readCard`(read what's already there)+operator-verifies-at-a-separate-reader+save+`blueLightOff`+`redLightOn`+poll-`testMicroSwitchAndCard`-for-return+`releaseElectromagnet`(lock)+light-off — built around verifying/re-securing an already-present fob, not attaching a new one. (2) Confirmed `KeySlot` has no UID field (id/terminalId/nodeAddress/managedKeyId/lifecycle only) — raw UIDs stay terminal-local via a **third, separate instance of the existing, unmodified `EncryptedUidEnrollmentStore`** (`storeName = "managed_key_fob"`), keyed by `ManagedKey.id` rather than `KeySlot.id` — a node reused for a different key is then automatically "not yet enrolled" for the new key's id, no extra bookkeeping needed for that specific case. (3) Confirmed the only single-node light commands anywhere are `KeyCabinetLink.blueLightOn`/`blueLightOff`/`redLightOn`/`redLightOff(nodeAddress)` — always called individually in a loop (e.g. `beginMultiKeyRedLightSequence`), no batch command exists or was invented here.
  - **Real gap found during Part 1 discovery, surfaced and resolved with the user before implementing**: `SuperAdminRoute.KEY_RETRIEVAL`/`KEY_MENU` (the actual, real physical Take/Return Flow, both Super Admin's own grid and Key Menu) already run entirely on `retrievalKeys`/`retrievalSlots` — `ManagedKey`/`KeySlot` — confirmed via reading the route wiring directly, not assumed. `TerminalKey` (the old schema) is used by nothing in real Take/Return; only by `EnrollKeyScreen` itself, `CardEnrollmentScreen`'s Key-card category, and the key-card-swipe return trigger (`managedKeyAndSlotFor`'s bridge) — the latter two explicitly off-limits this pass ("don't touch the personnel-card enrollment side of the old shared enrollment screen"). Since `EnrollKeyScreen` was the *only* place a new `TerminalKey` could ever be created, repointing its dashboard tile to the new schema would have stranded that creation path for a feature this pass isn't allowed to touch. **User's explicit decision: fully retire it** — `TerminalKey` creation is dead now that real Take/Return has moved on; the old screen's code and data are **not deleted** (`EnrollKeyScreen`, `GuidedKeyEnrollmentPhase`, `TerminalAdminStore.createKey`, `SuperAdminRoute.ENROLL_KEY` all still compile and are still fully wired in the `when(route)` block — confirmed via reading the diff, only the dashboard's own tile/param was repointed), just unreachable from any dashboard tile going forward. Same "compile-safe, zero call sites, left in place" precedent this file already used for `StartupDiagnosticsScreen` after the boot splash replaced it.
  - **Part 3 — background auto-scan-and-save, silent, no screen.** New `CabinetHardwareController.autoScanKeyFobs(nodeAddresses, onNodeResult, onSweepComplete)`: for each node, `blueLightOn` → `testMicroSwitchAndCard` (0x17) → hold `FOB_SCAN_FLASH_MILLIS` (600ms, flagged easy-to-retune constant) → `blueLightOff` — no unlock, no eject, confirmed safe with the door closed per the probe above. `TerminalAdminApp.triggerKeyFobAutoScan(keySlots)`: a node is a scan candidate if its `KeySlot` has a `managedKeyId` and the new `managedKeyFobStore` has no stored UID for that specific key id yet (covers both "never scanned" and, for free, "node reused for a different key" — the new key's id was never enrolled). **New `KeySlotAssignmentTracker`** (tiny local-only `nodeAddress → last-known managedKeyId` SharedPreferences map, a real gap found while reasoning through the design, not in the original brief): without it, a node reassigned to a different key but not yet physically re-fobbed would collide with `EncryptedUidEnrollmentStore`'s own `AlreadyAssigned` check (the same physical UID still "owned" by the old, now-unlinked key id) and never capture for the new key; the tracker detects the assignment change and revokes the stale entry first. On a successful capture, reports completion via the new `apiClient.completeKeyFobEnrollment()` (`POST .../fob-enrollment/complete`, see `CLAUDE_BACKEND.md`) in its own coroutine — a report failure is logged locally (`AuditEventType.KEY_FOB_ENROLLMENT_SYNC_FAILED`, new enum value, same non-blocking-sync shape as `KEY_CHECKOUT_SYNC_FAILED`) and **never retried automatically**, since the local store already considers that key enrolled once captured — flagged gap, see Known issues. Wired into the Admin Menu's Bootstrap and Download buttons only (`response.snapshot?.keySlots?.let(::triggerKeyFobAutoScan)`) — **deliberately not** the pairing screen's own best-effort bootstrap (a fire-and-forget call very early in `TerminalAdminApp`'s source order, before `triggerKeyFobAutoScan` is even declared — Kotlin's local-`fun`-resolves-by-source-order rule already documented elsewhere in this file would make that a compile error without a larger restructure judged out of scope this pass). Wrong-slot detection itself was **not touched** this pass. **Correction (found during this session's later "physical attachment" rework, see Completed below): the claim directly above — that wrong-slot detection "should simply start working correctly now that real UIDs are actually captured and stored" — was wrong and should not have been asserted.** Re-checked directly against `pollForKeyInsertion`'s `resolveKeyFobUid` parameter and its caller: wrong-slot detection resolves a swiped UID exclusively through `CardUidResolver` against `personnelCardStore`/`keyCardStore` (the old TerminalKey-schema stores) — it has never read `managedKeyFobStore` and still doesn't. `autoScanKeyFobs` capturing real UIDs into `managedKeyFobStore` has no effect on wrong-slot detection at all; the two systems are unrelated. This is left as-is, not fixed, since fixing it is out of scope for the pass that found it — flagged again in Known issues.
  - **Part 4/5 — `KeyAttachmentScreen.kt` (new file), replacing Guided Key Enrollment as the dashboard's key-fob tile.** Renamed dashboard tile "Key enrollment" → "Key Attachment" (`SuperAdminDashboardScreen`'s `onEnrollKey` param → `onOpenKeyAttachment`), new `SuperAdminRoute.KEY_ATTACHMENT`.
    - **Attachment sequence (Part 4)**: new `CabinetHardwareController.beginKeyAttachment(nodeAddress, onAttached, onTimeout, onFailure)` — adapted reuse of the old sequence's primitives for the opposite intent (attach a NEW key, not verify/return an existing one): `blueLightOn` + `engageElectromagnet` (unlock the empty slot) → poll `testMicroSwitchAndCard` up to `ATTACHMENT_TIMEOUT_MILLIS` (60s, flagged first-pass guess, no spec/hardware timing to derive it from) accepting whatever UID first appears (not requiring a match, unlike the old return-poll) → `releaseElectromagnet` (lock) + light-off once captured. New `cancelKeyAttachment(nodeAddress)` re-locks/lights-off the same way a timeout does — an unlocked, unattended slot is never left that way. New `attachmentMonitoring` guard flag, extends `stopMonitoring()`.
    - **Live cabinet lighting (Part 5)**: new `CabinetHardwareController.lightKeyAttachmentOverview(needsAttachmentNodes, alreadyAttachedNodes, onReady, onFailure)` lights every `KeySlot`-assigned node on screen entry — blue (no stored UID) / red (already attached) — individually, one command per node in one worker-thread pass, same never-a-batch-command reasoning as `beginMultiKeyRedLightSequence`; unassigned nodes are never touched, stay unlit. New `clearKeyAttachmentOverview(nodeAddresses)` is best-effort exit-path cleanup, called from the screen's `DisposableEffect.onDispose` regardless of how the screen was left, so it never leaves a stray light on.
    - **Blue→red live transition on completion**: the screen tracks `justAttachedKeyIds` locally (Compose state) so a completed attachment's on-screen card flips immediately, combined with a new `onLightAttachedNode` callback (`hardwareController.redLight(nodeAddress, true)`) that physically flips the real LED to match in the same moment — no polling needed for this case, it's a direct result of the action just taken, per instruction.
    - **Reverse-case polling (a key deleted on web while this screen is open)**: a screen-local `LaunchedEffect` polls every `NODE_POLL_INTERVAL_MILLIS` (3s, flagged easy-to-retune constant, picked mid-range of the instructed 2-5s) calling `apiClient.download()` **directly** — deliberately not `syncCoordinator.downloadFromServer()`, which would also mutate app-wide `retrievalKeys`/`retrievalSlots` state and could re-trigger Part 3's background sweep every 2-5s for no reason. Starts/stops with the screen's own composition lifecycle only — not a general app-wide change. **Load/performance note requested by the task**: this is one extra `POST /v1/terminal/sync/download` roughly every 3s, only while a Super Admin has this one screen open (not background/always-on) — judged low-risk relative to everything else already on this device's network path (Take/Return checkout sync, personnel refreshes), but genuinely untested against real load; flagged, not measured.
  - Verify: `:shared:compileDebugKotlinAndroid`, `:terminalApp:compileDebugKotlin`, `:terminalApp:assembleDebug`, and `:shared:testDebugUnitTest` all pass. **No hardware run at all this pass, per explicit instruction** — flagged as needing a real hardware pass more than most items in this project, given how much here is genuinely new (web API wiring, terminal local storage, lighting, polling) rather than incremental. See Known issues.
- **Key-card enrollment entry point fix — a regression from the Key node binding pass above, caught by the user, not self-caught.** Retiring `EnrollKeyScreen` (leaving it unreachable-but-present, per that pass's own confirmed decision) also silently stranded its "Enroll a key card" sub-entry — the *only* reachable path to `CardEnrollmentScreen`'s Key-card NFC category (the same gap a prior session had specifically found and fixed by adding that sub-entry in the first place). Nothing pointed at `SuperAdminRoute.ENROLL_KEY` to reach it any more; the button's own destination (`CARD_ENROLLMENT`, `selectedPersonnelId = null`, `CardEnrollmentCategory.KEY` locked) was never broken, just unreachable.
  - **Fix**: added the same "Enroll a key card" entry point to `KeyAttachmentScreen` instead — new `onOpenKeyCardEnrollment: () -> Unit` param, wired to the exact same navigation (`hardwareController.stopMonitoring()`, `selectedPersonnelId = null`, `openAdmin(SuperAdminRoute.CARD_ENROLLMENT)`) that already existed and worked. Rendered in its own `SoftCard` section below the node-attachment grid, with its own heading ("Key card enrollment") and description explicitly stating it's a separate mechanism from fob attachment — not blended into the node grid, per instruction, since NFC card-swipe enrollment and physical fob-to-node binding are genuinely different underlying mechanisms that only share this screen for reachability.
  - **Given the entry point moved and the old screen had zero remaining purpose, this pass fully deleted (not just left unreachable) `EnrollKeyScreen`, `SuperAdminRoute.ENROLL_KEY`, `GuidedKeyEnrollmentPhase`, the private `KeyCard` composable, and `sameFobUid`** — a deliberate escalation from the prior pass's "leave in place, unreachable" precedent, per this session's explicit instruction to confirm zero remaining references. `CardEnrollmentScreen`'s own `onBack` destination (when entered via the key-card path, `cardScopedUser == null`) was updated from `SuperAdminRoute.ENROLL_KEY` to `SuperAdminRoute.KEY_ATTACHMENT` — the only other place the retired route was referenced. `TerminalAdminStore.createKey`/`TerminalKey` data themselves were not touched (out of scope, per instruction — this was purely a navigation fix). Unused imports (`KeySlotAccessPolicy`, `MessageDigest`) removed as a result.
  - **Confirmed via repo-wide grep**: zero remaining references to `EnrollKeyScreen`, `SuperAdminRoute.ENROLL_KEY`, `GuidedKeyEnrollmentPhase`, `sameFobUid`, or the private `KeyCard` composable anywhere in any `.kt` file, except one historical mention in `KeyAttachmentScreen.kt`'s own doc comment describing the removal.
  - Verify: `:terminalApp:compileDebugKotlin`, `:terminalApp:assembleDebug`, and `:shared:testDebugUnitTest` all pass. **No hardware run** — this is a pure navigation/reachability fix with no hardware-sequence change, but the whole Key Attachment screen it now lives on remains entirely hardware-unverified (see the Key node binding entry's Known Issues above).
- **Key Attachment 4-state rework + new-key-registration-from-terminal.** Adds a genuinely new capability (registering a brand-new `ManagedKey`/`KeySlot` starting from a physically-present, previously-unknown fob) on top of last session's node-attachment screen. Compile/assemble-verified only, no hardware run — flagged as needing its own dedicated end-to-end pass, same as the rest of Key Attachment, given this screen's design has now needed correction twice in one session.
  - **Part 0 discovery (reported before implementing, per instruction) — the token finding matters more than a footnote.** `TerminalApiClient.accessToken`/`refreshToken` is one flat slot, not separate device-vs-operator storage. It's populated by `pairWithCode()` (TERMINAL_DEVICE-scoped) **or** by `TerminalSyncCoordinator.authenticate()`'s server branch (`api.login()`, real per-user JWT) — and that server branch is reached from **exactly one** login method: password. NFC card, fingerprint, face, and vendor-passkey logins all go through `store.authenticateByUserId(...)`, a purely local lookup that never touches `apiClient` at all. `refreshAccessToken()` has zero callers anywhere — nothing proactively refreshes an expired token. Net effect: whether a real, role-adequate token is currently held has nothing to do with which admin-tier role is locally active — it depends entirely on *how that session signed in*. `POST /v1/admin/keys`/`/key-slots` are not in `TERMINAL_DEVICE_ALLOWED_ROUTES`, so a TERMINAL_DEVICE-scoped token would get a real `403`, not just a vague failure. Reused the existing `session?.serverAuthenticated`/`serverLinked` flag (already used elsewhere in this app for the same "is this session really server-backed" question) as the gate — it's set precisely and only by that one password-login server branch, so it's accurate for the currently-active session, not just a coarse guess. **Practical consequence, not a rare edge case**: any admin who signed in via NFC/fingerprint/face (3 of 5 login methods) will have new-key registration disabled here even though they're a legitimate Super Admin locally — surfaced as a clear inline message, not a silent block.
  - Confirmed `TerminalApiClient` had zero `createKey`/`createKeySlot`-equivalent methods; added both using the shared, previously-unused `KeyUpsertRequest`/`KeySlotUpsertRequest`/`KeyDto`/`KeySlotDto` and existing `ApiPaths.ADMIN_KEYS`/`ADMIN_KEY_SLOTS` paths — same route/request shape as `web/src/api/client.ts`'s `createKey`/`createKeySlot`, no new backend surface.
  - Confirmed this terminal's own `siteId` needs no new resolution — `retrievalTerminal.siteId` (the already-synced `ManagedTerminalOption`) is exactly it; no site picker in the new-key dialog.
  - Confirmed the existing non-unlocking `testMicroSwitchAndCard` (0x17) primitive is reusable across the full `configuredSlotCount` range with no unlock needed. New `CabinetHardwareController.discoverAvailableFobs(nodeAddresses, onNodeResult, onSweepComplete)` — same door-closed-safe read as `autoScanKeyFobs`, deliberately **no** per-node light flash (this sweep runs with the screen already open; its result feeds straight into lighting the full picture once, not before). Timing estimate: real hardware data from the door-closed probe two sessions ago measured ~100ms per successful node read (8/8 nodes, log-timestamp-derived) — a `configuredSlotCount` sweep (24-48 in this project's demo configs) is therefore roughly 2.4-4.8s, not a noticeably long wait. Caveat: any address within `configuredSlotCount` with no node actually wired behind it would instead hit `KeyCabinetLink`'s built-in 500ms×3-attempt retry-and-fail (~1.5s) — untested at this scale, same caveat this file already carries for the wrong-slot sweep.
  - **Part 1 — 4-state rework**: new `NodeRowState` sealed interface (`NeedsAttachment`/`Attached`/`AvailableForRegistration`) replaces the old 2-state `NodeRow`. `lightKeyAttachmentOverview` gained a third `availableForRegistrationNodeAddresses` param — lights **both** blue and red for those nodes (one command each, still never a batch command). On-screen tone mapping: `NeedsAttachment`→`StatusTone.NORMAL` (blue), `Attached`→`StatusTone.ALARM` (red) — both unchanged from last session — `AvailableForRegistration`→`StatusTone.ATTENTION` (amber), the only tone left distinct from the other two on a single-tone `StatusRingCard`; not a severity judgment, just a stand-in for "both cabinet lights on" on a screen that can only render one tone per row.
  - **Part 2 — full-range discovery**: on screen entry, sweeps every node in `1..configuredSlotCount` **not** already covered by a `KeySlot.managedKeyId` assignment via `discoverAvailableFobs`, before the first light command fires (sequenced with `suspendCancellableCoroutine`, not fire-and-forget) — so the initial overview lighting already reflects any newly-discovered "available" nodes, not just the web-assigned ones. A node with nothing readable in this sweep renders and lights as before: OFF, no row at all.
  - **Part 3 — new "available" node tap flow**: tap → `AlertDialog` prompting for a single `displayName` (no site picker, per Part 0.3) → confirm → `onRegisterNewKey(nodeAddress, uidHex, displayName)`: `POST /v1/admin/keys` (siteId auto-filled) → `POST /v1/admin/key-slots` **pinned to the exact tapped node** (not lowest-unused-node logic — that's web's own registration-flow behavior; here the node is fixed by where the physical fob already is) → the already-known `uidHex` (from Part 2's sweep — no re-scan) saved into `managedKeyFobStore` and reported via the existing `completeKeyFobEnrollment` route immediately → then proceeds into the **exact same** `onBeginAttachment` sequence BLUE nodes use, so the operator can thread the actual physical key onto the now-registered fob, ending in the same `Done`/red state. **Real bug found and fixed while wiring this reuse**: the standard `onSaveAttachment` callback (which that reused sequence calls again at its end) would try to re-`enroll()` the exact uid/key pair already saved moments earlier, getting `UidEnrollmentResult.AlreadyEnrolledToSelectedRecord` instead of `Saved` — previously treated as failure. Fixed by treating both as success (the blue-node path only ever produces `Saved` in practice, so this is additive, not a behavior change there). A partial failure (key created, slot-binding POST fails) is surfaced as its own clear message naming the created key and telling the operator to check the web portal — not silently retried or rolled back.
  - **No offline queue, per explicit instruction**: `onRegisterNewKey`'s try/catch surfaces any failure (network or otherwise) as `RegisterNewKeyResult.Failed` with a message that says this requires an internet connection — the operator dismisses (`Failed` phase's existing "OK" button) and the node simply stays `AvailableForRegistration`, so re-tapping it is the retry path; nothing is queued into `TerminalSyncOutbox` or any other offline mechanism.
  - Verify: `:terminalApp:compileDebugKotlin`, `:terminalApp:assembleDebug`, and `:shared:testDebugUnitTest` all pass. **No hardware run at all** — flagged explicitly, needs its own dedicated end-to-end pass. No backend changes this pass (scope confirmed: reuses existing routes only).
- **Key Attachment — uidKnown/physicallyAttached split, door handling, and missing-fob detection.** Corrects a real modeling gap from the 4-state rework above and adds mechanics (door pop, exit-time missing-fob scan) never built for this screen at all. Compile/assemble-verified only, no hardware run — this screen has now needed correction three times in one session and needs its own dedicated hardware+backend end-to-end pass more than anything else built this session.
  - **The gap**: `managedKeyFobStore.enrollmentFor(id) != null` ("is this fob's UID known locally") was being read as "fully attached" (RED), but that fact can become true with zero human action at all — Part 3's background auto-scan sets it silently, on a schedule tied to Bootstrap/Download, not to any operator action on this screen.
  - **Fix — two separately tracked facts.** New `terminalApp/.../data/PhysicalAttachmentTracker.kt`: a tiny SharedPreferences-backed `managedKeyId -> Boolean`, deliberately its own class rather than extending `EncryptedUidEnrollmentStore`/`KeySlotAssignmentTracker` — a different concern (human action history) from either. `isUidKnown` (still `managedKeyFobStore.enrollmentFor(id) != null`) and `isPhysicallyAttached` (`physicalAttachmentTracker.isAttached(id)`) are now two separate params into `KeyAttachmentScreen`, replacing the old single `isAttached`. RED now requires `isPhysicallyAttached`; a node that's assigned but not yet physically attached renders BLUE regardless of whether its UID happens to already be known (an interpretive call, not explicitly specified by the task — preserves the pre-existing rule that every assigned node always renders as either "needs attachment" or "attached," never neither). `onSaveAttachment` (both the direct blue-node tap and the reused tail end of new-key registration) now calls `physicalAttachmentTracker.markAttached(managedKeyId)` on success, in addition to the existing `managedKeyFobStore.enroll(...)` call — the only place either flow ever sets the second fact.
  - **Part 0 discovery (reported before implementing, per instruction).** (1) `managedKeyFobStore` is read/written exclusively inside `TerminalAdminApp.kt` — splitting its single conflated boolean has no other consumer to break. (2) Confirmed wrong-slot detection (`pollForKeyInsertion`'s `resolveKeyFobUid`) never reads `managedKeyFobStore` at all — it resolves swiped UIDs through `CardUidResolver` against `personnelCardStore`/`keyCardStore` exclusively; this session's own earlier documentation claiming otherwise was wrong (corrected above, in the 4-state rework's own Completed entry). (3) Door primitives reused verbatim, no new cabinet command: `KeyCabinetLink.checkDoorStatus()`/`ejectDoor()`, called directly (bypassing `runCommand`'s curated-string-state wrapper, same as `beginKeyTakeInternal`/`beginKeyReturnFlow` already do) since the new door methods need real boolean results. (4) `testMicroSwitchAndCard`'s CRC-validated retry means "genuinely missing" (0xFF, a successful read) and "transient hiccup" (an exception after 3 failed attempts) are cleanly distinguishable at the API level — a `KeyFobScanResult.Failed` is never treated as proof of absence, only `NothingPresent` is.
  - **Door pop on node selection (new `CabinetHardwareController.ensureDoorOpen(onReady, onFailure)`)**: checks door status (0x22) first, ejects (0x23) only if not already open, confirms open afterward. Called at the start of `KeyAttachmentScreen`'s `startAttachment()` — i.e. before every guided attach, both the direct blue-node tap and the post-registration reuse — so both paths get it for free from one call site. Deliberately does not track/auto-close the door afterward; it's meant to stay open across multiple attachments in one screen visit.
  - **Exit check, triggered from the Back button (new `checkDoorStatusOnly`/renamed `scanNodes`)**: `CabinetHardwareController.discoverAvailableFobs` renamed to `scanNodes(nodeAddresses, onNodeResult: (Int, KeyFobScanResult) -> Unit, onSweepComplete)` — unifies what was two near-identical reads (Part 2's discovery sweep, this pass's missing-fob sweep) into one method, since both are the same underlying 0x17 read differing only in caller-side interpretation. New read-only `checkDoorStatusOnly(onResult, onFailure)` (never ejects, unlike `ensureDoorOpen`). `KeyAttachmentScreen`'s Back button now runs `attemptExit()` instead of calling `onBack` directly: checks the door, then sweeps every node with either `isUidKnown` or `isPhysicallyAttached` true (both blue and red nodes are in scope) via `scanNodes`. A `NothingPresent` result on any swept node is logged immediately via a new `AuditEventType.KEY_FOB_MISSING` event (`shared/.../AdminModels.kt`) — persisted the moment it's detected, before any dialog is shown, so it's recorded regardless of what the operator does next. If either the door is open or a fob is missing, one combined dialog shows both findings (a single dialog with up to two sections, not two separate stacked modals — the task said "notifications" plural but didn't require them literally separate, and one dialog reads cleaner); its single "Continue to dashboard" button is the only way to dismiss it, and dismissing always proceeds to the dashboard — **explicitly non-blocking**, per instruction. If neither finding applies, `onBack()` fires immediately with no dialog at all.
  - **Missing-fob blink (new `setMissingFobBlink(nodeAddresses)`)**: a node with a missing fob blinks red regardless of its other steady state — a transient overlay, not a 5th steady state, per instruction. Deliberately **not** a blocking loop on `worker` (the single-threaded executor every other cabinet command shares) — that would starve Take/Return Flow and background sync for as long as anything blinked. Timing is driven by `mainHandler.postDelayed` (main thread) at `MISSING_FOB_BLINK_INTERVAL_MILLIS` (600ms, flagged easy-to-retune guess, no spec/hardware basis), with each tick submitting only a quick on/off command per blinking node to `worker`.
    - **Stop condition, corrected the same session (see the follow-up entry directly below): the blink stops only on explicit operator acknowledgment**, via the exit dialog's "Continue to dashboard" button. There is no automatic re-scan or timeout that clears it on its own. The **first version of this pass** turned the node's light fully off on acknowledgment (reasoned as "the locally-known state is stale, showing a steady light would be dishonest") — the user caught this as wrong: OFF already means "unassigned" everywhere else in this model, and the node is still assigned, just possibly stale. See the follow-up entry for the corrected behavior (re-read-and-relight, not go-dark).
  - Verify: `:terminalApp:compileDebugKotlin`, `:terminalApp:assembleDebug`, and `:shared:testDebugUnitTest` all pass. **No hardware run at all** — flagged explicitly, and reiterated per instruction: Key Attachment needs its own dedicated hardware+backend end-to-end pass before being trusted, more than anything else built this session. No backend changes this pass.
- **Key Attachment — missing-fob acknowledgment correction: re-read-and-relight instead of going dark.** User-caught correction to the entry directly above, same session. Turning both lights off on acknowledgment was wrong because OFF already means "no assignment exists" elsewhere in this model — the node is still assigned, just in a possibly-stale state, and going dark misrepresented that. Compile/assemble-verified only, no hardware run.
  - **Fix**: `CabinetHardwareController.clearMissingFobBlink(nodeAddress)` replaced by `reconcileMissingFobBlink(nodeAddress, wasPhysicallyAttached, onResult: (stillPresent: Boolean) -> Unit, onFailure)`. On acknowledgment this stops the blink, then does one more `testMicroSwitchAndCard` (0x17) read of that specific node (reusing the same classification `scanNodes` already uses, not a new primitive) and relights it based on the fresh result: `NothingPresent` (the same and only authoritative "gone" signal used everywhere else in this screen) means the fob really is gone — reports `stillPresent = false` and lights the node BLUE ("needs re-attachment"); anything else readable (`CardRead`/`BoltPresentNoCard`) means the earlier alert was a transient blip — reports `stillPresent = true` and restores whichever steady light `wasPhysicallyAttached` implies (RED if true, BLUE if false), no fact reset. A read `Failed` (an exception, not a `NothingPresent` response) is deliberately treated the same as still-present — consistent with this screen's existing "a Failed read is inconclusive, never proof of absence" rule, and it also avoids stranding the node blinking forever with no further trigger able to clear it, since acknowledgment is the only path that ever calls this method.
  - **`KeyAttachmentScreen`**: new `onReconcileMissingFobBlink` param replaces `onClearMissingFobBlink`; new `onResetMissingKeyState: (managedKeyId: String) -> Unit` param, called only when `stillPresent = false` — wired in `TerminalAdminApp.kt` to `managedKeyFobStore.revoke(managedKeyId)` + `physicalAttachmentTracker.clear(managedKeyId)`, so a genuinely-gone fob actually resets both tracked facts (not just the light), and the node correctly renders BLUE the next time this screen is opened rather than still claiming a UID/attachment that's no longer true. The exit dialog's confirm action fires this off per missing node without awaiting the result (matching the existing non-blocking-navigation rule) before calling `onBack()`.
  - Verify: `:terminalApp:compileDebugKotlin`, `:terminalApp:assembleDebug`, and `:shared:testDebugUnitTest` all pass. **No hardware run** — flagged explicitly; folds into the same "needs a dedicated hardware+backend pass" flag as the rest of Key Attachment.
  - **Superseded later this same session (see "Key Attachment — door-closed and missing-fob resolution both made mandatory" below): the dismissible acknowledge-and-relight dialog this entry built was removed entirely** once the exit gate became a mandatory resolve-or-explicitly-override design with no dismiss action. `reconcileMissingFobBlink`/`onReconcileMissingFobBlink` no longer exist; `onResetMissingKeyState` survived but is now called from the override path instead.
- **Key Attachment — two real bugs found by actual hardware testing (the first real hardware run this screen has ever had), both now fixed.** This is the fifth significant correction to this screen in one session. Compile/assemble-verified only for the fixes themselves — **both need re-verification on the same real hardware that caught them** before being trusted; the previous hardware pass already proved this screen's design had two non-obvious defects code review alone hadn't caught.
  - **BUG 1 — fob could not be physically removed after tapping an amber ("available for registration") node.** Root cause, confirmed by tracing the exact command sequence: `CabinetHardwareController.beginKeyAttachment` (shared by both a plain blue node and the amber-then-registered node — genuinely the same function call, no divergence) unconditionally polled "wait until a readable fob is present" before re-locking. That condition is correct for an empty blue node (nothing is there until the operator clips a new fob on) but was trivially, instantly true for an amber node — its fob was already physically resting there, which is how it was discovered as available in the first place. The unlock (0x13) was torn down by a re-lock (0x14) within a single 0x17 round-trip (~100ms), far too fast for a human to react. Door-level eject (0x23) and the per-node electromagnet (0x13/0x14) were both confirmed independent, as assumed — nothing hardware-surprising there, the bug was purely in the poll's success condition.
    - **Fix**: `beginKeyAttachment` now does an immediate presence check (0x17) right after unlock and branches. Not present (0xFF): unchanged, waits for a NEW fob to be clipped on. Already present (the common case for this hardware): a two-phase wait instead — (a) wait for the existing fob to be physically REMOVED (0x17 → 0xFF), then (b) wait for it to be REINSERTED (0x17 readable again) — only re-locks once (b) succeeds, so the slot stays genuinely unlocked for the whole real physical task (pull fob out, attach the real key, put it back). Each phase gets its own full `ATTACHMENT_TIMEOUT_MILLIS` (60s) budget rather than a split half-each — flagged as an easy constant to retune, chosen because removing+reattaching a key is a longer physical task than clipping on a brand-new fob (what that constant was originally sized for).
    - **Included, since it was straightforward**: on reinsertion, the fresh UID is compared against the one read during the initial presence check; a mismatch (a different physical fob came back) doesn't block completion but sets a new `uidChangedFromPrevious` flag on `beginKeyAttachment`'s `onAttached` callback, surfaced on-screen as an explicit warning rather than silently accepted as identical.
    - **`beginKeyAttachment` signature changed**: gained `onSlotState: (occupied: Boolean) -> Unit` (fired once, right after the presence check, purely for on-screen guidance — no behavior depends on it) and `onRemovalConfirmed: () -> Unit` (the occupied case's phase (a)→(b) transition); `onAttached` gained the `uidChangedFromPrevious: Boolean` second parameter. `KeyAttachmentScreen`'s `onBeginAttachment` param and `AttachmentUiPhase.WaitingForFob`/`Done` were updated to match — `WaitingForFob` now carries a new `AttachmentWaitGuidance` (`CHECKING`/`EMPTY_SLOT`/`OCCUPIED_PULL_AND_REINSERT`/`OCCUPIED_REINSERT`) driving distinct on-screen wording for the occupied case ("pull out the fob, attach your key, then reinsert it" / "Fob removed — reinsert it now") vs. the unchanged empty-slot wording. The existing cancel-attachment button and Done/Failed OK button needed no changes — both already worked generically off `WaitingForFob`/`Done`'s `nodeAddress` field, unaffected by the new fields added alongside it.
  - **BUG 2 — a registered key's name vanished on screen re-entry, though the backend genuinely had the record.** Root cause, confirmed by tracing where state is read from: `KeyAttachmentScreen`'s `keys`/`initialSlots` params come from `TerminalAdminApp`'s app-wide `retrievalKeys`/`retrievalSlots` state, which is only ever reassigned by `refreshSnapshot()` — and `onRegisterNewKey` called `apiClient.createKey`/`createKeySlot` **directly**, bypassing `syncCoordinator` entirely, so `refreshSnapshot()` was never triggered by a successful registration. The screen's own `locallyRegisteredKeys` local Compose state made the just-registered key render immediately, but only within that one composable instance — disposed the moment the operator navigated back to the dashboard, so re-entering rendered from the still-stale pre-registration snapshot. Re-tapping the node re-created a **new**, duplicate `ManagedKey` (no dedup in `createKey`) and only then failed at `key-slots.js`'s existing duplicate-node-per-terminal check (`backend/src/routes/keySlots.js:67-73`, a real `SELECT` against the live `key_slots` table) — proving the original record was genuinely persisted server-side; the terminal's own re-render was the only thing wrong. A secondary consequence also confirmed while tracing: each such retry left an orphaned, never-slotted `ManagedKey` behind on the backend.
    - **Fix**: after both `createKey`/`createKeySlot` succeed, `onRegisterNewKey` now calls `syncCoordinator.downloadFromServer()` (the exact same call the Admin Menu's own Download button uses) then `refreshSnapshot()` (the same function every other sync entry point already uses to repopulate `retrievalKeys`/`retrievalSlots`) — best-effort, wrapped in its own try/catch, since the key+slot already exist server-side regardless of whether this refresh succeeds; a failure here just means the operator may need to hit Download manually later, same recovery as any other missed sync.
    - **Bonus fix (requested alongside this bug)**: if `createKeySlot` fails after `createKey` succeeded, the handler now calls a new `TerminalApiClient.deleteKey(keyId)` (`DELETE /v1/admin/keys/:id`, same route/shape as `web/src/api/client.ts`'s `deleteKey`, soft-deletes per boundary #5, not a hard purge) before surfacing the error — best-effort; if the delete itself also fails, the failure message says so explicitly rather than silently leaving the orphan unmentioned.
    - **`locallyRegisteredKeys` evaluated, kept — not removed.** Reasoning: `retrievalKeys`/`retrievalSlots` updating now fixes the *cross-visit* bug (a fresh screen entry now reads correct data), but within the *same* screen visit, `KeyAttachmentScreen`'s own `slots` state is seeded once from `initialSlots` via a plain `remember { mutableStateOf(...) }` and is not re-seeded just because the parent's `retrievalSlots` changes underneath it — it only picks up a change via the screen's own pre-existing ~3s polling loop (`onPollSlots`). Without `locallyRegisteredKeys`, a freshly registered node would still flash "unregistered" for up to ~3s in the same visit before that poll caught up. `locallyRegisteredKeys` was never the source of the cross-visit bug (that was the missing `syncCoordinator` call) — it still earns its keep for same-visit immediacy, so it stays.
  - Verify: `:terminalApp:compileDebugKotlin`, `:terminalApp:assembleDebug`, and `:shared:testDebugUnitTest` all pass, clean on first try for both fixes. **No hardware run this pass** — flagged explicitly; both fixes need re-verification on the same real hardware/backend that caught the original two bugs before either is trusted. No other backend changes beyond the new `deleteKey` client method (calls an existing, unmodified route).
- **Key Attachment — door-closed and missing-fob resolution both made mandatory at the exit gate, replacing the previous dismissible notice.** Confirmed design change, not a bug fix. Compile/assemble-verified only, no hardware run — flagged explicitly; needs re-verification alongside the rest of this screen's hardware pass given its track record this session (this is its sixth significant correction).
  - **Grepped every exit path from this screen before changing anything, per instruction — there is exactly one.** `BackButton` (wired to `attemptExit()`) is the only way to leave `KeyAttachmentScreen`; the app has no `BackHandler`/`OnBackPressedCallback`/Activity-level `onBackPressed` override anywhere (grepped both this screen and app-wide) — the hardware system back button/gesture is not intercepted at all in this app, consistent with a kiosk-style terminal. Nothing else needed updating.
  - **Change 1 — door-closed, no override.** New `checkDoorOpenSuspend()` failure semantics changed as part of this: the prior dismissible-warning version treated a failed door-status read as "assume closed" (harmless there, since the operator could always click through anyway); now that this gate has no override, that same default would have silently let an operator leave with a possibly-open door on a hardware read failure, so it now treats a failed read as "still open" and keeps polling instead. `proceedPastMissingFobStep()` (the door-closed step, deliberately last — see ordering below) checks once; if open, shows a non-dismissible dialog and polls at `NODE_POLL_INTERVAL_MILLIS` (reused as instructed, for consistency with this screen's existing 3s poll) until closed, then auto-dismisses and calls `onBack()` — no manual "check again" button, since closing the door is always the available fix.
  - **Change 2 — missing-fob resolution, mandatory with a logged override.** New `CabinetHardwareController.resolveMissingFob(nodeAddress, wasPhysicallyAttached, onResolved, onTimeout, onFailure)`: engages that node's electromagnet (0x13) — naturally serialized to one node at a time by the existing `engageElectromagnet`/`ElectromagnetConcurrencyException` guard, reinforced at the UI level too (the screen only lets one node's "Unlock" button be active at once) — then reuses Fix 1's hardware-tested "wait for reinsertion" pattern (`isReadableFobData`, the same `ATTACHMENT_TIMEOUT_MILLIS`/`ATTACHMENT_POLL_INTERVAL_MILLIS` budget) rather than the old instant-success check that pass fixed. On success: re-locks (0x14), relights the node's correct steady state via a new shared `relightSteadyState` helper (factored out of the now-removed `reconcileMissingFobBlink`, also reused here), reports resolved. New `cancelResolveMissingFob(nodeAddress)` re-locks and restores the blink (the fob is still genuinely missing after a cancel). A timeout also restores the blink rather than leaving the node dark or falsely steady. **Gap found and fixed later the same session, see the dedicated entry below**: this originally engaged the electromagnet without first confirming the door was open at all.
    - **Override**: implemented **per-node** (not a single whole-list action — the task left this open; per-node was chosen because it gives each audit entry an unambiguous single node/key, and a whole-list version would have needed to log one event per node internally anyway). New `AuditEventType.KEY_FOB_MISSING_OVERRIDDEN` (shared/AdminModels.kt), distinct from the automatic `KEY_FOB_MISSING` detection event — logged via the existing `store.logEvent(eventType, actorUserId, ...)` shape, whose `actorUserId` + auto-stamped `occurredAtEpochMillis` already satisfy "who and when" with no new `AuditEvent` fields needed. Overriding also resets both tracked facts (`managedKeyFobStore.revoke` + `physicalAttachmentTracker.clear`, via the surviving `onResetMissingKeyState` param) since the fob really is still gone — but deliberately does **not** clear the node's blink; overriding unblocks navigation, it does not mean the physical problem was fixed.
  - **Ordering, per instruction**: missing-fob resolution (`attemptExit()`) runs first; the door-closed gate (`proceedPastMissingFobStep()`) runs last, immediately before actually leaving — resolving a missing fob via the unlock action may need the door to still be open, so gating on door-closed first would have blocked that.
  - **Old dismissible mechanism removed, not left unreachable** (matching this session's established precedent for superseded code — see the `EnrollKeyScreen` retirement earlier in this file): `CabinetHardwareController.reconcileMissingFobBlink` and `KeyAttachmentScreen`'s `onReconcileMissingFobBlink` param are deleted outright, since replacing their only call site left them with zero remaining references — confirmed via repo-wide grep. `onResetMissingKeyState` survived (still meaningfully used, now by the override path instead).
  - Verify: `:terminalApp:compileDebugKotlin`, `:terminalApp:assembleDebug`, and `:shared:testDebugUnitTest` all pass, clean on first try. **No hardware run** — flagged explicitly, folds into the same "needs a dedicated end-to-end pass" flag the rest of this screen already carries. No backend changes this pass beyond the new shared `AuditEventType`.
- **Key Attachment — missing-fob resolution now confirms the door is open before engaging the node's electromagnet.** Small, contained gap fix: `CabinetHardwareController.resolveMissingFob` engaged the electromagnet (0x13) directly, with nothing upstream of it ever guaranteeing the door was actually open first — unlike the attach flow, which always runs `ensureDoorOpen` via `startAttachment` before `beginKeyAttachment`. Compile/assemble-verified only, no hardware run.
  - **Fix**: `KeyAttachmentScreen.resolveMissingFobAt` now wraps its call to `onResolveMissingFob` in `onEnsureDoorOpen(...)` first, mirroring `startAttachment`'s exact existing pattern (check-then-conditionally-eject, then proceed only once the door reports open) — the same already-wired `ensureDoorOpen` function, not a reimplementation. On a door-open failure, `resolvingNode` resets to `null` (silently reverting to the button state), matching the existing (already silent) treatment of a general `onResolveMissingFob` failure — no new failure-message UI was added, to keep this change contained to the one gap it was asked to close. `CabinetHardwareController.resolveMissingFob`'s doc comment was updated to state this door-opening responsibility explicitly (matching the note `beginKeyAttachment` already carries), though the method itself needed no code change.
  - **Interaction with Change 1's door-closed exit gate — checked, not assumed.** Reopening the door during a resolve (if it had been closed) is expected and already accounted for: Change 1's `proceedPastMissingFobStep()` deliberately runs last, only once every missing node is resolved or overridden, and simply re-checks door status at that point regardless of why it's open — a door reopened by this fix is caught the same way a door reopened by the attach flow, or left open by an operator, already was. No conflict found; `ensureDoorOpen` is also idempotent (only ejects if not already open), so repeated resolves across multiple missing nodes don't re-eject unnecessarily.
  - Verify: `:terminalApp:compileDebugKotlin`, `:terminalApp:assembleDebug`, and `:shared:testDebugUnitTest` all pass, clean on first try. **No hardware run** — flagged explicitly, same as everything else on this screen this session.

- **Passkey login no longer requires local personnel cache (Haikal, Jul 2026).** See `CLAUDE_MOBILE.md` — builds session from passkey-login response profile so Only B exception techs work at cabinets outside standing assignments.

- **Standing login blocked off-site (Haikal, Jul 2026).** Technician/Vendor password/NFC/fingerprint/face must match `retrievalTerminal.siteId` ∈ `assignedSiteIds`. Passkey login remains the Only B exception path (server site check). Rebuild terminal APK.

### Known issues / not yet resolved

- **Only B live smoke (Jul 2026): Apply → RA Approve → Technician access works; Key Attachment / key enrollment still not fixed** — passkey session reaches KEY_MENU but approved keys are not reliably node-bound for take. Portal Key Access grant/revoke OK (see `CLAUDE_MOBILE.md` / `CLAUDE_WEB.md`). Fix Attachment before treating passkey→take as done.
- **Key Attachment's mandatory door-closed/missing-fob-resolution exit gate (see Completed, "door-closed and missing-fob resolution both made mandatory") is compile/assemble-verified only, zero hardware run.** Specifically unverified: `resolveMissingFob`'s unlock-wait-for-reinsertion-relock sequence actually working in person (only reasoned through against the same 0x17 classification `beginKeyAttachment`'s Fix 1 already validated in code, never run as this exact new method); the one-electromagnet-at-a-time guard's `ElectromagnetConcurrencyException` never actually surfacing in normal use (the UI is designed to prevent triggering it by only allowing one node's resolve at a time, but a real concurrent-attempt edge case has not been tried); the door-closed poll actually detecting a real door close and auto-dismissing promptly rather than lingering; the changed door-check failure default (now "assume still open" instead of "assume closed") not causing an operator to get stuck if the read genuinely keeps failing for an unrelated reason (there is no operator-facing distinction between "door genuinely open" and "door status can't be read" in this dialog — both look identical); and that overriding a missing fob correctly resets `isUidKnown`/`isPhysicallyAttached` while leaving the node visibly still blinking, exactly as designed, on a real device.
- **Key Attachment's two hardware-tested-and-fixed bugs (see Completed) need re-verification on the same real hardware/backend that caught them — do not report either as resolved until that happens.** The first real hardware pass this screen ever got found two real defects code review had missed: (1) `beginKeyAttachment`'s unlock being torn down by an instant re-lock for any already-occupied slot, now fixed with an occupied/empty branch and a two-phase remove-then-reinsert wait; (2) `onRegisterNewKey` never updating `retrievalKeys`/`retrievalSlots`, now fixed with a `syncCoordinator.downloadFromServer()` + `refreshSnapshot()` call. Specifically still unverified: that the two-phase wait's removal detection and reinsertion detection actually work in person (only reasoned through against the same 0x17 classification used elsewhere, never run); that the new UID-mismatch warning correctly fires on a genuinely different reinserted fob and stays quiet on the same one; that the new occupied-case guidance text (`AttachmentWaitGuidance`) reads clearly and appears at the right moments; that `syncCoordinator.downloadFromServer()` genuinely repopulates `retrievalKeys`/`retrievalSlots` with the just-created key/slot fast enough that a real back-out-and-back-in no longer shows it as unregistered; and that the bonus `deleteKey` cleanup call actually removes the orphaned key on a real slot-creation failure without itself failing silently.
- **Key Attachment's uidKnown/physicallyAttached split, door pop, and missing-fob detection (see Completed) is compile/assemble-verified only, zero hardware run.** Nothing about `ensureDoorOpen`, `checkDoorStatusOnly`, the renamed `scanNodes`, or the missing-fob blink has run against a real cabinet. Specifically unverified: `ensureDoorOpen`'s check-then-conditionally-eject actually behaving correctly whether the door starts open or closed; the door genuinely staying open across multiple attachments in one visit with no accidental re-close; the exit-time `scanNodes` sweep's real timing over every uidKnown-or-physicallyAttached node (not just unassigned ones, which is the only range `discoverAvailableFobs` was ever timed against); and the missing-fob blink actually being visually distinct from steady red/blue on the real LEDs, and not starving other cabinet commands in practice (reasoned through in code, never observed under real concurrent load). **The exit dialog and acknowledgment mechanism this entry originally described no longer exist** — both were replaced by the mandatory resolve-or-override gate (see the dedicated Completed/Known-issues entries above); do not test against this entry's original dialog design.
- **Wrong-slot detection is confirmed independent of `managedKeyFobStore`/`isUidKnown`/`isPhysicallyAttached` — a real correction to this session's own earlier documentation, not a new gap.** It resolves swiped UIDs exclusively through `CardUidResolver` against `personnelCardStore`/`keyCardStore`. Nothing built across any of this session's Key Attachment passes (background auto-scan, 4-state rework, or this pass's physical-attachment split) changes wrong-slot detection's behavior at all — do not expect Key Attachment work to have any effect on it.
- **Key Attachment's 4-state rework + new-key registration: the "reused attach-flow-after-registration" concern flagged here was confirmed real by actual hardware testing (see Completed, "two real bugs found by actual hardware testing") — both bugs found there are now fixed, pending re-verification (see the dedicated entry above).** The rest of this entry's scope is still compile/assemble-verified only, zero hardware run: `discoverAvailableFobs`'s real timing at `configuredSlotCount` scale (the ~100ms/node figure is real data, but only from 8 nodes, all genuinely wired — an address within `configuredSlotCount` with nothing behind it has never been swept and could be much slower per the retry-and-fail caveat above); both lights actually turning on together correctly for an "available" node and both turning off correctly on exit/registration; and the `serverAuthenticated`-gated message actually showing correctly for an NFC/fingerprint/face-signed-in admin on real hardware (reasoned through via code, never seen on screen).
- **New-key registration is only reachable for admins who signed in via password against the real server — not a rare edge case.** NFC card, fingerprint, face, and vendor-passkey logins never populate a real per-user token (see the Completed entry's Part 0 discovery), so any admin using those methods sees registration disabled with an inline message rather than being able to use it, even though they're a fully legitimate Super Admin/Regional Admin locally. No workaround exists this pass; flagged for a future decision (e.g. having those login methods also attempt a background server token refresh) rather than silently worked around.
- **`onRegisterNewKey`'s partial-failure case (key created, key-slot binding fails) has no compensating action** — the operator is told to check the web portal for the resulting unbound key, but nothing here deletes it, retries just the slot half, or surfaces it anywhere else in the terminal UI. A rare failure mode (both calls hit the same request in quick succession), but unhandled beyond the message.
- **Key node binding ("Key Attachment") is compile/assemble-verified only, zero hardware run — flagged as needing a real hardware pass more than most items in this project.** Nothing about the background auto-scan, the guided attachment sequence, the overview lighting, or the reverse-case polling has ever run against a real cabinet. Specifically unverified: `autoScanKeyFobs`'s blue-flash-per-node timing/visibility in person; `beginKeyAttachment`'s unlock→poll→lock sequence end to end (only its individual primitives — blueLightOn/engageElectromagnet/testMicroSwitchAndCard/releaseElectromagnet — have been separately hardware-verified in other flows, never this exact new sequence); the 60s `ATTACHMENT_TIMEOUT_MILLIS` guess; `lightKeyAttachmentOverview`/`clearKeyAttachmentOverview` actually lighting/clearing the correct nodes with no stray lights left on; the blue→red live transition's timing against the real LED; and the reverse-case polling loop actually catching a real web-side delete within its 3s interval. Also never run: the whole chain end-to-end against the live backend (auto-assign a node on web → background-sync bootstrap/download on a real terminal → real hardware capture → completion report round-trips back to `ManagedKey.fobEnrollmentReference` visible on web).
- **`KeySlotAssignmentTracker`'s stale-entry revocation is reasoned-through but never exercised on real hardware**: the scenario it exists for (a node reassigned to a different key without the physical fob having been swapped yet) has not been physically reproduced.
- **Key fob enrollment completion reports have no retry mechanism** — if `apiClient.completeKeyFobEnrollment()` fails (offline, timeout, server error) after a successful local capture, the local `managedKeyFobStore` already considers that key enrolled, so no later sync attempt re-triggers the report; only a local `KEY_FOB_ENROLLMENT_SYNC_FAILED` audit event records that it happened. `ManagedKey.fobEnrollmentReference` could stay `null` on the backend indefinitely in this case. Flagged as an accepted first-pass gap, not fixed.
- **Old TerminalKey-based Guided Key Enrollment (`EnrollKeyScreen`, `SuperAdminRoute.ENROLL_KEY`) has since been fully removed (see Completed, "Key-card enrollment entry point fix" below) — this entry is superseded, not just historical.** It was first made unreachable-but-present, then a regression was found (its "Enroll a key card" sub-entry was the only reachable path to Key-card NFC enrollment, and going unreachable stranded that too) and fixed by moving the sub-entry onto `KeyAttachmentScreen` and deleting the old screen/route/enum entirely. `TerminalAdminStore.createKey`/local `TerminalKey` data are untouched; `CardEnrollmentScreen`'s Key-card category and the key-card-swipe return trigger (`managedKeyAndSlotFor`) still read existing `TerminalKey` records exactly as before. No new `TerminalKey` is expected to ever be created again.
- **New post-login landing chooser (`TerminalLandingChoiceScreen`, see Completed) is compile/assemble-verified only — needs a real hardware pass before being called done, given this is a navigation change to the primary landing flow for Super Admin and Regional Admin.** Specifically unverified: that a Regional Admin's "Take Key" choice actually lands on the correctly-filtered `KEY_MENU` (scoped to their own access grants) rather than the unfiltered grid — the role branch is a two-line `if (activeSession.isSuperAdmin)` at the call site, never exercised through a real Regional Admin login (same live-login blocker this file has flagged for Regional Admin work before — see the Phase 7/Regional Admin entries); that Super Admin's "Take Key" still correctly reaches the unfiltered `KEY_RETRIEVAL` grid unchanged; that the demoted "Take keys" tile on the dashboard renders correctly at its new position (first, paired with Card enrollment) and that the trailing `Vendor passkey` + blank-`Spacer` row actually looks like "one tile with empty space beside it" rather than something visually broken; the two `SoftHeroAction`s on the new chooser screen stacking correctly in both light and dark theme (compile-verified only, no screenshot); and — most importantly, since this touches shared navigation state — that **Technician/Vendor's login flow is genuinely unaffected**, i.e. still lands directly on `KEY_MENU` with no chooser in between and an unchanged "Sign out" back button, which was reasoned about via diff (the `postLoginRoute()` `else` branch and `KEY_MENU`'s `isAdminTier`-false path are unchanged) but never re-confirmed by an actual Technician/Vendor login on real hardware. The `KEY_MENU` back-button fix (now role-conditional, see Completed) and the `passwordChangeReturnRoute` default change (a brand-new Super Admin's forced first-login password change now also lands on the chooser instead of `DASHBOARD`) are both new consequences of this pass, neither hardware-tested — the latter specifically needs a genuinely fresh/reset Super Admin bootstrap login to exercise at all, not just any admin-tier login.
- **Personnel Management rework (list → detail, 3 enrollment tiles removed, key-card sub-entry — see Completed) is compile/assemble-verified only, not hardware-run at all.** Specifically unverified: `PersonnelListScreen`'s tappable `StatusRingCard` rows actually navigating to the right record; `PersonnelDetailScreen`'s live enrollment-status rows (User Card/Fingerprint entry/Face Registration) actually reflecting real `personnelCardStore`/`fingerprintTemplateStore`/`faceProfileStore` state on a real device; all three scoped sub-flows (Card/Fingerprint/Face enrollment entered from a specific Personnel Detail row) actually enrolling/revoking against the intended single user, not silently falling back to the wrong one; and the new "Enroll a key card" button inside Key enrollment correctly locking `CardEnrollmentScreen` to the Key category with the real keys list. None of this has been seen on the real F7G18P.
- **Regional Admin site-scoping gap in Personnel Management — found during this rework's discovery, pre-existing (not introduced by this pass), deliberately not fixed per instruction.** `personnelForScreens` is not filtered by site/role anywhere in `terminalApp` — a Regional Admin session's Personnel Management list shows every locally-known personnel record, identical to what a Super Admin sees, rather than being scoped to the Regional Admin's own assigned site(s) (the row-level scoping pattern used elsewhere, e.g. Admin Menu item 14). This is a permission-boundary decision reserved for the user to make explicitly in a future pass, not something this rework changed.
- **Two small, deliberately-unfixed cosmetic wrinkles from the Personnel Management rework (see Completed), both traded off against not touching the enrollment screens' own files beyond the new lock params:**
  - `CardEnrollmentScreen`/`FingerprintEnrollmentScreen`/`FaceEnrollmentScreen`'s own personnel "· change" cycle button still renders even when scoped to a single user from Personnel Detail — with a 1-item list the button is inert (cycles back to itself) but still visually present, which could read as "there are other people to pick from" when there aren't.
  - Those same 3 screens' `BackButton` still shows the generic "Back to Super Admin dashboard" label even though they now actually return to Personnel Detail (or Key enrollment, for the key-card sub-entry) — `BackButton` gained a `label` param this pass (see Completed) but it was only threaded into the 2 brand-new Personnel Management screens, not into the 3 pre-existing enrollment screens.
- **App-wide immersive mode (`MainActivity`, see Completed) is compile/assemble-verified only, not yet confirmed on the real F7G18P.** Specifically unverified: bars actually hide on launch; an edge swipe actually reveals them and they auto-hide again on the standard system timeout (no custom logic was added, so this is purely trusting platform behavior, but that trust itself hasn't been checked on this specific device/Android build); and — the path this change exists for — that returning from the boot splash's "Open Network Settings" button doesn't leave bars permanently visible. EnjoyKit (`com.hzmct.enjoyConfig`) is installed but confirmed not currently suppressing bars for this app (see Completed); if a future EnjoyKit config change starts suppressing bars at the device level, this app-level mechanism would become redundant (harmless, not conflicting, but worth re-checking then).
- **App-wide UI click sound (`AudioFeedbackController.playClick()`, now the native `AudioManager.playSoundEffect(FX_KEYPRESS_STANDARD)` implementation — see Completed) is compile/assemble-verified only — still needs a real hardware pass specifically checking for audio interference during Take/Return Flow before being called done.** This requirement **carries over unchanged from the WAV-based version**, just against different code: this device has hit the exact "streams sharing `AudioAttributes` get grouped/preempted by the `awplayer`/CedarX HAL" bug twice already (see beep-continuity investigation); the native sound effect uses `STREAM_SYSTEM` with no app-owned `AudioAttributes`/`SoundPool`/`MediaPlayer` instance at all, which plausibly sidesteps that HAL behavior entirely, but that's reasoning, not a hardware-confirmed result. Also unverified: the click actually plays audibly and promptly on tap, including rapid repeated taps. The custom-asset duration/clipping concern from the WAV version (`cavotec_click.wav`, ~1.385s/0dBFS-clipping) no longer applies — that file is deleted and no longer referenced. `Settings.System.SOUND_EFFECTS_ENABLED` is confirmed `1` on the real device (see Completed) so nothing should currently block playback, but that's a device-wide setting an operator could still disable later with no in-app detection — flagged, not solved, no `WRITE_SETTINGS` requested. `TerminalCloseToDeadlineScreen` now plays the click sound (confirmed decision, `playSound = false` override removed) — also not yet hardware-verified for interference, same as every other click-sound call site.
- **Credentials are terminal-local (policy A, Jul 2026)** — full policy explanation lives in `CLAUDE_BACKEND.md`. Terminal-side implication: local NFC/fingerprint/face enrollment still works; nothing is uploaded.
- **Pairing-code flow: code + portal UI + cabinet settings sync deployed; live e2e on a fresh F7G18P still required** — see `CLAUDE_BACKEND.md` for the deploy-status framing and `CLAUDE_WEB.md` for the portal-side registration steps. Run: portal register (including Cabinet Settings) → copy 6-digit code → fresh terminalApp enter code → confirm bootstrap downloads unit data **and** timers/toggles.
- **Beep-continuity bug: two code-only fixes applied, neither hardware-reverified against the original concurrent scenario.** Fixes: (1) `LaunchedEffect` re-keying in both Take/Return screens (`beeping` alone, `beepLoud`/`wrongSlotNodeAddress` via `rememberUpdatedState`); (2) distinct `AudioAttributes` for beep vs. voice line in `AudioFeedbackController` — see Completed "Beep-continuity investigation". A later hardware test (`SoundTestActivity`, 15 beep reps at 1/sec) ran clean, but it never ran the beep loop *concurrently* with a voice line — do not treat that as reverification. Check first whether Take/Return redesign has superseded point-fixing this further.
- **Boot auto-launch confirmed failing on a real reboot test** (the app had been manually opened before the test, so this isn't the "stopped app" `BOOT_COMPLETED` exemption). `BootCompletedReceiver`/manifest were re-verified line by line and found correct (see Completed "Boot auto-launch diagnosis") — no code bug found. Diagnostic logging (tag `BootAutoLaunch`) is in place but has not yet been exercised by another reboot.
- **`beep.wav` clipping:** earlier PCM analysis found severe clipping on a prior asset; audio assets were later replaced (see Completed beep work). Re-confirm on device if chirp persists after the replacement files.
- **Return Flow "double abandonment" event — believed structurally fixed by the Return Flow rework, not yet hardware-reverified.** Originally: a single card-swipe/no-insert test produced two separate `KEY_RETURN_ABANDONED` events ~28 seconds apart, cause never conclusively answered. The rework's `attemptId`-keyed, live-re-checked abandonment race (see Completed "Return Flow rework" above) closes the specific TOCTOU gap traced in the old code, but this has **not been re-tested on a real cabinet** — treat as fixed-by-construction/code-review, not fixed-and-confirmed, until a repeat of the original test happens.
- **Return Flow's success/insertion path has never been hardware-tested at all** — no run of insertion-within-5s, the `PLEASE_INSERT_THE_KEY` voice line, or the Door-Close Warning Time countdown has happened on real hardware yet (Part 1 only exercised the abandonment path, and even that was ambiguous — see above).
- **Animated boot splash (`TerminalBootSplashScreen`, see Completed) — partial hardware verification done, real gaps remain.** Installed and launched via `adb` on a real device (`adb` reports `ro.product.model`/`ro.product.device` = `A133P`; the same physical unit this project's other hardware-verification sessions call the F7G18P — names differ, unit not independently re-confirmed as identical). **Confirmed on-device**: the splash renders with no crash (`Zygote`→`MainActivity` cold start, no `FATAL EXCEPTION`/`AndroidRuntime` crash in logcat across two separate launches); the Cavotec logo asset decodes and displays correctly (not broken/missing/wrong-colored); dark theme applies correctly to the splash with no hardcoded background bleeding through; the `CircularProgressIndicator` renders; cabinet (`/dev/ttyS1`), fingerprint (`/dev/ttyS0`), and public NFC reader (`/dev/ttyS2`) serial ports all opened successfully during the splash's underlying checks (confirmed via `serial_port`/`SerialPortManager` logcat lines); and the splash correctly auto-navigated to `TerminalLoginScreen` once checks passed (confirmed via screenshot — device landed on the login screen unattended). **Still not confirmed**: the entrance scale/fade-in and breathing-pulse *motion* itself (only static screenshots captured, not video — a single mid-splash frame confirms the logo is present/positioned/colored correctly, not that the animation curve looks right in motion); light-mode splash/warning card (device was in dark mode this pass); the warning-card path, "View details" expansion, Retry button, "Open Network Settings" button, and the "Continue anyway" bypass link were **never exercised** — every check passed on this device, so `anyFailed` was never true; the `ON_RESUME` auto-recheck was never meaningfully exercised either (no Wi-Fi-settings round trip attempted). One unexplained, likely-unrelated event during this pass: the device dropped off `adb` entirely for ~15s mid-session and came back with a new process (fresh `Zygote` start, new PID) — no crash trace on either side of the gap, consistent with a USB re-enumeration/device-side event rather than anything in the new code, but not conclusively ruled out either; if this recurs, check `logcat -b all` around the drop for a cause. `StartupDiagnosticsScreen` itself (reached only via "View details," never opened this pass) remains unverified on hardware. `androidx.compose.ui.platform.LocalLifecycleOwner` compiles clean but is flagged deprecated by the current Compose Multiplatform toolchain (moved to `androidx.lifecycle.compose.LocalLifecycleOwner` / `lifecycle-runtime-compose`, not in this project's version catalog) — a real but non-blocking toolchain-drift item, not a functional bug; worth migrating in a future pass that's willing to add the new dependency. **Superseded by the sizing/timing follow-up above as of this same session**: everything "confirmed on-device" in this entry was captured against the *old* 140dp logo / 1s floor build — the new ~390dp logo (65% of the confirmed 600dp portrait width) and 3s floor have not themselves been seen on hardware yet; re-verify before trusting that the bigger logo actually looks right (not cropped/overflowing/too large) and that 3s doesn't feel sluggish in person.
- **Phase 9 design-system rework (colors/dark-mode/`IconActionButton`) is compile/assemble-verified only, not hardware-run at all.** Dark-mode contrast/legibility on the real F7G18P display, the `Switch`-based corner toggle's touch target, and `IconActionButton` in the one wired dialog have never been seen on real hardware. Also open: `SoftAssistChip`/`ConnectionHintCard` still use fixed light-mode-only container colors (not theme-aware) — will look inconsistent in dark mode until a follow-up sweep fixes them; the new `info` semantic token isn't wired into `HintSeverity`'s color mapping yet; and the rest of terminalApp's screens (Take/Return Flow, Key Menu, Office Hours, Vendor Passkey, and every other Admin Menu screen) still use the old single-light-scheme tokens/raw `Button()`/`TextButton()` calls — this pass was foundation + a 2-screen pilot only, not a full sweep.
- **Phase 9A's two hardware-bug fixes (`TerminalLoginScreen` blank/mistinted icon badges + invisible light-mode cards — see Completed "Phase 9A hardware-bug fix") are compile/assemble-verified only, not yet confirmed on the real F7G18P.** The user is re-screenshotting the device to confirm both are actually resolved — do not report either as closed until that lands. If the re-screenshot still shows a problem, check first whether `material-icons-extended`'s icon names (`Password`/`Fingerprint`/`Face`/`Nfc`/`VpnKey`) render as expected at runtime (a clean compile only proves the symbols resolve, not that the glyphs look right) and whether the `1.dp` `outlineVariant` border reads clearly enough against the terminal's actual display (contrast that looks fine in a compile-time mental model can still be too subtle on real glass).
- **Phase 9A's light-mode fill polish (see Completed "Phase 9A follow-up polish") is compile/assemble-verified only, not yet confirmed on the real F7G18P.** The `#EBF1F8` resting fill (8% `CavotecBlue` over white) and the deliberate contrast dip on the description text (≈4.78:1 → ≈4.21:1, computed not measured on-device) both need the user's next screenshot to confirm: (1) the tint actually reads as "matching dark mode's depth" rather than too subtle/too strong in person, (2) the description text is still comfortably legible on real glass despite the computed dip, and (3) — the specific thing this pass was told must not regress — dark mode's tile fill genuinely didn't move, since `EkmsColors.isDark`/`restingContainerColorFor` now gate that branch in code where a single shared expression handled both modes before.
- **Phase 9B (`SoftAssistChip`/`ConnectionHintCard` theme fix + `TerminalKeyMenuScreen` rework, see Completed "Phase 9B") is compile/assemble-verified only, not yet confirmed on the real F7G18P — needs a screenshot in both light and dark.** Specifically unverified: the new dark-mode success/warning/info soft-chip fills (`SoftSuccessContainerDark`/`SoftWarningContainerDark`/`SoftInfoContainerDark`) have **never been seen on real hardware in dark mode at all** — no prior dark-mode screenshot of `SoftAssistChip`/`ConnectionHintCard` exists to compare against, unlike `SoftScanTile` which at least had one hardware round already; `ConnectionHintCard`'s `CHECK` severity now shows a blue info tint instead of the old neutral gray — confirm this doesn't read as an error/warning by mistake; `TerminalKeyMenuScreen`'s new `SoftScanTile`-based grid tiles, `selected` state's `2.dp` primary border, the dimmed/disabled look for `taken` keys (`Modifier.alpha(0.55f)`), the new empty state, and the `IconActionButton(ACCEPT)` confirm button have no hardware run at all — this screen was never part of any earlier Phase 9 hardware pass. Also still true from Phase 9A and unaffected by this pass: `TerminalKeyMenuScreen` itself remains otherwise live-unverified (no Technician/Vendor account/access grant existed on the live backend to reach this screen through a real login) — confirm that blocker is resolved (see the Regional Admin entry below) before expecting an on-device screenshot of this screen to be possible at all.
- **Phase 9C (`TerminalOfficeHoursScreen`/`TerminalVendorPasskeyScreen` visual rework, see Completed "Phase 9C") is compile/assemble-verified only, not yet confirmed on the real F7G18P.** Needs a screenshot of both screens, in both light and dark. Office Hours specifically needs verification under **both** roles — Super Admin's edit-mode `SoftCard` + `IconActionButton` and Regional Admin's view-only rows have never been compared side by side on real hardware, only judged adequate by reading the code (see Completed entry for the reasoning). Vendor Passkey's `SoftAssistChip` status tones and the new equal-weight `IconActionButton(ACCEPT)`/`IconActionButton(CANCEL)` pair are unverified on device — same live-login blocker as `TerminalKeyMenuScreen` applies here too (no real Regional Admin/Technician/Vendor account exists on the live backend yet to reach either screen's Regional Admin view through an actual login). Also flagged in the Completed entry and repeated here: this pass read the task's "both roles see the same approve/reject UI" instruction as "both actions" (Approve/Reject) needing equal visual weight, not literal Super-Admin/Regional-Admin parity — confirm that reading is correct.
- **Phase 9D (`TerminalAdminMenuScreen` 12-field + Backend Sync card visual rework, see Completed "Phase 9D") is compile/assemble-verified only, not yet confirmed on the real F7G18P.** Needs a screenshot in both light and dark, under **both** roles — Super Admin's full-edit view (all 12 fields + Backend Sync's all-four-buttons + `IconActionButton` Save) and Regional Admin's partial view (4 fields genuinely absent, the rest view-only, Backend Sync missing Push/Read) have never been compared side by side on real hardware; the HIDDEN-fields-stay-absent behavior was verified by reading the code, not by looking at an actual rendered Regional Admin screen. Same live-login blocker as the rest of the Regional Admin work applies (no real Regional Admin account exists on the live backend yet).
- **Phase 9E (`TerminalKeyTakeScreen`/`TerminalCloseToDeadlineScreen` visual rework, see Completed "Phase 9E") is compile/assemble-verified only, not yet confirmed on the real F7G18P.** The new door-left-open `strongAttention` warning border has never been seen on a real cabinet in either theme — this is the state Take Flow's known unresolved timing bug lives in (see the beep-continuity/Take Warning Time entries above, both still open and untouched by this pass), so confirm the new border doesn't visually clash with or obscure whatever the still-buggy beep/timing behavior actually looks like live. `TerminalCloseToDeadlineScreen`'s two-card layout and warning-tinted Emergency button are also unverified on device — this screen has never had a hardware pass at all (Phase 5's original entry already flagged zero hardware testing for the whole close-to-deadline flow, unchanged by this visual-only pass).
- **Phase 9F (`TerminalKeyReturnScreen`/`ReturnSessionScreen` visual rework + `SoftWaitPanel` default fix, see Completed "Phase 9F") is compile/assemble-verified only, not yet confirmed on the real F7G18P.** The wrong-slot card's new `2.dp` error border + distinguishing icon has never been seen on a real cabinet — this is the Return Flow rework's wrong-slot sweep, itself still only "believed correct by code review, not hardware-reverified" (see the entries above), so confirm the new visual treatment doesn't obscure or misrepresent whatever the still-unverified detection actually does live. `SoftWaitPanel`'s now-visible default border affects `TerminalNfcCardLoginScreen` and `TerminalFingerprintLoginScreen` too, even though neither file was directly edited this pass — both need a hardware check to confirm the border reads correctly there as well, not just on the two screens this pass directly touched. `ReturnSessionScreen`'s `IconActionButton` Done button and the checkout-summary/"Returned this session" card borders are also unverified on device.
- **Take/Return hardware verification / redesign** — hold per product; not part of the pairing/registration rework.
- **Fingerprint and face enrollment: device-local hardware behavior is hardware-verified.** Server credential sync is **disabled by policy A** (not a bug — see `CLAUDE_BACKEND.md`). Remaining narrow gaps: fingerprint "misaligned/dirty finger" failure case never literally tested; face liveness not tested against a deliberately manipulated spoof. **Face LOGIN's core pipeline is now hardware-confirmed** (see the dedicated entry below — head-turn liveness pass, capture, and cosine-match comparison all ran live on a real F7G18P/camera, correctly producing a `NoMatch` result). **Fingerprint LOGIN's actual identify outcome remains unconfirmed** — the sensor was never physically touched during the automated test that confirmed auto-start, so `identifyFingerprint()`/`autoIdentify`'s `Matched`/`NoMatch`/`Failed` paths are still compile/assemble-verified only. `FACE_MATCH_SIMILARITY_THRESHOLD = 0.40f` remains an uncalibrated placeholder guess, not a tuned value, regardless of this session's `NoMatch` result (a `NoMatch` doesn't exercise or validate the threshold the way a genuine same-person match attempt would).
- **Face/fingerprint login auto-start + head-turn guide UI (this session, see Completed) — real hardware pass done, largely confirmed working.** Installed and launched on the real device (`adb`-reported model `A133P`); no crash/ANR across the whole session (same process PID throughout). **Confirmed on-device**: both screens auto-start with zero manual tap — fingerprint shows "Place a finger on the sensor…" with the progress spinner already active on screen entry, face shows a live camera preview with real-time face detection (`OpenCvFaceEngine`'s bounding box + confidence, e.g. "Face 93%") already running on entry; the enlarged `screenHeightDp * 0.4f` camera preview renders as a large, proportionate live feed, not cramped; the head-turn guide UI displayed the **correct** direction arrow matching the actual challenge instruction ("Turn your head right" text paired with the right-pointing arrow) with a visibly non-zero, responding progress bar; a real head turn **passed the liveness challenge**, capture proceeded automatically, and the (untouched) cosine-match comparison ran and correctly produced `FaceLoginPhase.NoMatch` ("Face not recognized. Try again or choose a different method.") with the "Try again" retry button correctly reappearing; the fingerprint screen's new 88dp icon rendered correctly, sized and tone-colored as intended, above the panel. **Still unconfirmed**: the progress bar's color-transition/fill *smoothness* in motion (only static screenshots captured, not video — frame noise at the 350ms sampling interval could still look jittery in person); fingerprint's actual scan-to-outcome path (`Matched`/`NoMatch`/`Failed`) — the sensor was never physically touched this pass, only auto-start itself was confirmed (see the entry above); and light-mode appearance of either screen (device was in dark mode throughout this test). The immersive-mode work from an earlier session also incidentally looked correct throughout this same test run (no status/nav bars visible in any screenshot) — not this task's scope, noted for completeness.
- **Regional Admin role (Phase 1 revised + follow-ups):** backend deployed; **RA login verified on web / mobile / terminal** (Jul 2026). Remaining: Only B Apply→approve→PIN e2e and Adi passkey→take; full 17-item Admin Menu matrix still needs a dedicated walkthrough.
- **Phase 5 checkout sync / close-to-deadline logic: backend is deployed and confirmed live, including the 3-route allowlist gap (see `CLAUDE_BACKEND.md`) — the only remaining gap is that it has never been hardware-run at all.** `TerminalCloseToDeadlineScreen` (manual-entry/Emergency), the office-hours-fetch-once-per-session gate, and the backend create/close-out sync calls have still never run against a real cabinet. `CLOSE_TO_DEADLINE_THRESHOLD_MINUTES = 60` and `EMERGENCY_WINDOW_HOURS = 3` remain unvalidated reasonable-default guesses, not confirmed product values.
- **Face enrollment ships RGB-only active liveness — reworked this session from blink+head-turn to head-turn-only** (see Completed above for why: blink detection was unreliable due to a polling-rate/blink-duration mismatch, not a threshold-tuning issue). Confirmed live: reliably passable by a real person, correctly times out against a static photo. Still a deliberate, user-confirmed v1 choice with a real, weaker anti-spoof tier than the vendor manual's RGB+IR spec (section 4.8.3/4.8.4, not available in this repo) — the rework improved reliability, not the security tier. A deliberately-manipulated spoof (rotating/tilting a photo to mimic head-yaw) was not tested. Explicit plan to upgrade later; do not treat this as final or silently "fix" the anti-spoof tier without revisiting the tradeoff.
- **`terminalApp/src/main/assets/models/`'s ~41 MB of binary face-model assets are permanently in git history as raw blobs** (commit `ff332f5`) — Git LFS was set up afterward (commit `3b4c8dd`, `.gitattributes` + the three files re-added as LFS pointers), so nothing new grows the problem, but the historical ~41 MB itself was deliberately **not** removed via a history rewrite (`git filter-repo`/BFG/`git lfs migrate import`) — that would change every commit hash from `ff332f5` onward, forcing the concurrently-active `web/` developer to hard-reset/re-clone instead of a normal `git pull`, which given this repo's prior silent-revert merge incident was judged a materially worse risk than the one-time bloat. Treat a history rewrite here as a **future, deliberately coordinated maintenance action** (both developers idle, remote refs cleaned, everyone re-clones after) — never do it as a routine session action. Everyone working in this repo needs `git lfs install` run once locally (see `CLAUDE_SHARED.md` Toolchain) or these files silently resolve to unusable pointer stubs.
- **Key Take Flow is hardware-verified for its core path (door sequence, 20s abandonment ceiling, removal-triggered countdown, success path)** — see Completed "Combined hardware verification session" above. **Key Return Flow, post-rework, is entirely unverified on real hardware** — the whole state machine changed (continuous session, wrong-slot sweep, `attemptId` rework) since the last hardware run, so even the previously-confirmed real card-swipe node resolution needs re-confirming, not just the new pieces. Do not assume anything about Return Flow's hardware behavior from before the rework still applies.
- **Return Flow rework (continuous session, wrong-slot detection, checkout record) is entirely unverified on real hardware** — see Completed "Return Flow rework" and its follow-up above for exactly what shipped. Specifically untested: a real wrong-slot insertion actually lighting red/warning/beeping and clearing on correction; the identity-vs-unresolved wording distinction; a continuous multi-key session actually staying active across multiple scans and ending via Done or the 20s idle timeout; `TerminalCheckoutStore`'s "Checked out by X · Ym ago" display; and `testMicroSwitchAndCard`'s real sweep timing on a cabinet with several configured slots.
- **Standing Super Admin / mobile alerts** for take door-left-open and return abandoned-return are not built (events may exist locally on terminal; no portal/mobile delivery UI — see `CLAUDE_WEB.md`/`CLAUDE_MOBILE.md` for the delivery-side gap).
- Orphaned terminal workflow scaffolds remain reference-only under `terminalApp/reference/` — do not merge as production UI.
- Theme polish: terminal red/alarm status tone was never confirmed live on hardware (deferred by user during Cavotec pass).
- **Boot receiver (`BootCompletedReceiver`) is compile/assemble-verified only — never confirmed by an actual reboot.** This is the one piece of the auto-launch/diagnostic/network work that categorically cannot be verified any other way; do not report auto-launch-on-boot as working until a real power-cycle test happens.
- **Network bring-up's Ethernet-preferred path is untested** — the test device only ever had Wi-Fi available, so `NetworkStatusController` preferring `TRANSPORT_ETHERNET` over `TRANSPORT_WIFI` has never actually been exercised with both present. The fully-offline degraded state (no network at all) is also untested — Wi-Fi wasn't deliberately disabled during testing, to avoid disrupting this device's live adb connection.
- **Key Menu & multi-key sequential Take Flow is compile/assemble-verified only, not live-verified at all.** See Completed above for the full breakdown. The live backend (`kms-cvt.com`) this device is paired to has zero access grants and no Technician/Vendor personnel, so `KEY_MENU` has never actually been reached through a real login — only a regression check on the pre-existing Super Admin grid was done. Creating a throwaway test technician + grant to close this gap was offered and declined; do this before reporting Key Menu as working. Completely unverified: the screen's own render (multi-select, Layout/List toggle, empty state), the red-light batch sequence, the red→blue transition, and multi-node queue advancement.
- **Digital Key hardware feasibility test tooling** — full entry (design options, decision status) lives in `CLAUDE_MOBILE.md`. terminalApp side: pairs with a not-yet-built terminalApp-side raw-reader-frame diagnostic screen; neither this nor the mobileApp prototype should be reported as "the" Digital Key approach until the user has run the real-hardware test.
- **Passkey feature (Region schema + mobileApp request/approval + terminalApp real login wiring) is compile-verified and locally-smoke-tested only — never run end-to-end on real devices against the live deployed backend.** Full unverified-scenario list lives in `CLAUDE_MOBILE.md` (primary owner of this entry). terminalApp-side specifically unverified: entering a passkey at a real F7G18P terminal and the take flow proceeding with the correct pre-set due time (`DueDateSource.PASSKEY_REQUEST`) rather than the manual-entry/Emergency prompt.

### Next steps (in order)

- **[Haikal] Only B phone smoke** then **[Adi] passkey→take** — see `CLAUDE_MOBILE.md` Next steps (primary).
- **Default server URL** is production (`https://kms-cvt.com`) for Admin Menu `serverAddress` + API client; blank legacy prefs migrate. Rebuild terminal APK after pull.
- **Hardware-verify Key node binding end to end** — the highest-priority item in this session's work: add a key on web (auto-assigns a node) → Bootstrap/Download on a real terminal → confirm the background auto-scan lights blue-flashes and captures a real fob's UID with the door closed → open Key Attachment, confirm overview lighting (blue/red) matches reality → attach a genuinely new key via the guided flow → confirm blue→red live transition and the completion report lands on `ManagedKey.fobEnrollmentReference` (visible on web) → delete a key on web while Key Attachment is open and confirm the node goes unlit within the polling interval. **Extended scope from the 4-state rework**: also place a bare, unregistered card at a node with no web assignment, confirm it discovers and lights both-on, register it as a new key from the terminal (as a password-signed-in admin), confirm the reused attach sequence completes and the node goes solid red, and confirm the new key/slot are visible on web afterward. Also confirm the `serverAuthenticated`-gated disabled state actually renders correctly for an admin signed in via fingerprint/face/NFC instead of password. **Further extended scope from the uidKnown/physicallyAttached split**: confirm a node whose UID the background auto-scan captured (but which no human has tapped through this screen's attach flow yet) renders BLUE, not RED; confirm tapping that BLUE node pops the door and, on completion, flips it RED; confirm the door stays open across a second attachment in the same visit; confirm leaving the screen with the door still open shows the door-open notice; physically remove an already-attached key's fob, leave and re-enter (or just leave) the screen, and confirm the exit scan detects it missing, blinks that node red, logs `KEY_FOB_MISSING`, and clears the blink only once the exit notice is acknowledged.
- **Regional Admin Admin Menu matrix walkthrough** — RA login already works; still verify the 17-item permission matrix, item-14 site names, Office Hours, Vendor Passkey, Key Menu with a Technician + grant.
- Hardware-test Phase 5's checkout sync + close-to-deadline screen on a real cabinet (manual-entry and Emergency paths, backend create/close-out sync, `KEY_CHECKOUT_SYNC_FAILED` logging on a simulated backend failure)
- Hardware-test Phase 3's fingerprint and face login (`identifyFingerprint`, cosine-match) on a real R503/camera; calibrate `FACE_MATCH_SIMILARITY_THRESHOLD` (currently an unvalidated `0.40f` guess) against real attempts
- Live e2e: portal set Cabinet Settings → pair fresh terminal → confirm Admin Menu / Take-Return timers match DB values after bootstrap
- Live e2e pairing checklist (unit data + pairing code) on a fresh F7G18P
- Re-run the reboot test with `BootAutoLaunch` diagnostic logging (`adb logcat -s BootAutoLaunch` after power-cycle)
- Hardware-reverify beep-continuity fixes against concurrent beep + voice line during a real Take/Return Flow
- Hardware-verify the Return Flow rework end to end on a real cabinet (duplicate-abandonment retest, wrong-slot, continuous session)
- Hardware-verify the Personnel Management rework end to end (list rows, per-user detail's live enrollment-status rows, all 3 scoped Card/Fingerprint/Face sub-flows, and Key enrollment's new key-card sub-entry) — untouched by any hardware pass so far
- Standing-alert UI on `web/` for door-left-open / abandoned-return (later) — see `CLAUDE_WEB.md`
- **Hardware-verify the whole Phase 9 design-system sweep (9/9A-9F) on a real F7G18P, in both light and dark, for both Super Admin and Regional Admin roles.** Nothing in the sweep below has been seen on real hardware yet — this is one on-device pass covering everything, not several separate ones. Compile/assemble-verified only, per every Phase 9x Completed entry: `TerminalLoginScreen` (tiles, theme toggle), `TerminalKeyMenuScreen`, `TerminalOfficeHoursScreen`, `TerminalVendorPasskeyScreen`, `TerminalAdminMenuScreen` (12 fields + Backend Sync), `TerminalKeyTakeScreen`, `TerminalCloseToDeadlineScreen`, `TerminalKeyReturnScreen`, `ReturnSessionScreen`, plus the shared `SoftAssistChip`/`ConnectionHintCard`/`SoftScanTile`/`SoftWaitPanel`/`IconActionButton` components underneath all of them.
- **Design-system sweep is otherwise substantially complete** for the screens explicitly in scope across Phase 9A-9F — do not re-open or redo styling on any of them without a new reason. Screens **not yet part of any Phase 9 pass** (no color/component audit done, status unknown either way): `TerminalPasswordLoginScreen`, `TerminalFaceLoginScreen`, `TerminalPasskeyLoginScreen`, `TerminalKeyRetrievalScreen` (Super Admin's unfiltered cabinet grid), `TerminalPairingScreen`, `StartupDiagnosticsScreen`, `CardEnrollmentScreen`, `FaceEnrollmentScreen`, `FingerprintEnrollmentScreen`, `SiteTerminalAdminScreen` — audit these before assuming they're either fine or broken.

## Reference

- Hardware protocol: `docs/Key Cabinet Communication Protocol.md` (note
  the spaces in that filename) is the authoritative vendor spec — read
  before any phase 8+ work. `docs/Key_Cabinet_Communication_Protocol.md`
  (underscored) is a project-level index onto it and defers to it on any
  conflict.
- Terminal UX baseline: Smart Key Cabinet User Manual V2.1 (baseline +
  defined enhancements — see "Terminal App UX Baseline (Production —
  baseline + defined enhancements)" section above for what's permitted to
  diverge and how)
