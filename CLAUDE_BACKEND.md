# CLAUDE_BACKEND.md

_Part of the split CLAUDE.md documentation set — `backend/`-specific. See [CLAUDE.md](CLAUDE.md) for the index, the "Production is already live" short warning, and the non-negotiable architectural boundaries list. See [CLAUDE_SHARED.md](CLAUDE_SHARED.md) for cross-cutting info (toolchain, common commands, module architecture, `shared/` contract locations)._

## Production is already live — do not reinvent the backend

**The API and Super Admin portal are already deployed on a VPS and reachable on the public internet.** Agents must not assume "there is no backend," invent a new deploy, or treat local-only Node as a requirement to use the API.

| URL | What |
|---|---|
| `https://kms-cvt.com/` | Live React portal (`web/` build served from the VPS) |
| `https://kms-cvt.com/v1/...` | Live Express API |
| `https://kms-cvt.com/health` | Health check |
| Terminal Admin Menu → server address | `https://kms-cvt.com` (no trailing slash; **not** an `api.` subdomain) |

Deploy/ops details live in `backend/DEPLOY.md` (Docker Compose prod, Caddy, Cloudflare DNS for `kms-cvt.com`). **Default agent scope:** work on `shared/`, `terminalApp/`, `web/`, and docs. Touch `backend/` only when the user explicitly asks for an API/schema/deploy change — and even then prefer updating the existing VPS deploy over standing up a parallel stack. A laptop without Node/Docker can still verify against production (browser → portal, terminal → `https://kms-cvt.com`); "no local Node" only means you cannot run `npm run build` / local Vite in that environment, **not** that the backend is unavailable.

## Terminal → live API (how terminalApp reaches the portal backend)

> **Pairing code path (preferred).** terminalApp's first screen is a 6-digit code entry
> (`POST /v1/terminal/pair-with-code`), then bootstrap downloads settings from the database.
> Super Admin creates the cabinet and code on the portal (**Registration** wizard or **Cabinet Management**, see `CLAUDE_WEB.md`).
> Backend pairing + `web/` registration UI are deployed together in the Jul 2026 rework — still
> verify end-to-end on a fresh terminal install before retiring the manual Admin Menu steps (see `CLAUDE_TERMINAL.md` for the terminal-side wire-up steps).

The terminal does **not** talk to the React UI. It talks to the **same backend** the portal uses (`https://kms-cvt.com/v1/...`). When the terminal creates personnel (server-linked), the portal's Personnel page sees it after refresh because both share MySQL on the VPS. **NFC / fingerprint / face enrollment stays terminal-local** and is not uploaded to the portal database (policy A, Jul 2026 — see "Credential policy (policy A)" below).

The terminal-side wire-up steps (Admin Menu manual pairing, client code location) live in `CLAUDE_TERMINAL.md` — this file keeps the endpoint table, auth model, and what shows on the portal after terminal actions, since those describe the backend contract itself.

**Endpoints terminalApp actually calls today** (base = `https://kms-cvt.com`):

| Purpose                               | Method | Path | Auth |
|---------------------------------------|---|---|---|
| Pair with 6-digit code (fresh device) | `POST` | `/v1/terminal/pair-with-code` | No |
| Sign in (store tokens)                | `POST` | `/v1/auth/login` | No |
| Refresh tokens                        | `POST` | `/v1/auth/refresh` | No (body refresh token) |
| First-link / bootstrap snapshot       | `POST` | `/v1/terminal/sync/bootstrap` | Yes |
| Push offline outbox                   | `POST` | `/v1/terminal/sync/push` | Yes |
| Read sync ack / revisi-ons            | `GET` (via client) | `/v1/terminal/sync/read` | Yes |
| Download server snapshot              | `GET` (via client) | `/v1/terminal/sync/download` | Yes |
| List units (sites) for Add Personnel  | `GET` | `/v1/admin/sites` | Yes |
| Resolve this cabinet's unit           | `GET` | `/v1/admin/terminals/{terminalId}` | Yes |
| List / create personnel               | `GET` / `POST` | `/v1/admin/users` | Yes |

Credential complete/revoke admin routes may still exist on the backend for older clients; terminalApp must not call them for biometric/NFC enrollment under policy A.

Auth header: `Authorization: Bearer <accessToken>` on every route except login/refresh/pair-with-code. Login sends `clientType: TERMINAL`. Canonical path strings live in `shared/.../api/ApiContracts.kt` (`object ApiPaths`) — the contract between all three apps and the backend.

**What shows on the web portal after terminal actions:**

| Terminal action | Portal place to check |
|---|---|
| Add personnel (server-linked) | **Personnel Management** — new user row; also **Activity Report** → Personnel registration |
| Card / fingerprint / face enroll | **On the terminal only** — portal shows "Terminal-local only" |
| Bootstrap / Download / Push | **Data Synchronization** (+ underlying users/keys/slots/grants lists); take/return audits also land in **Activity Report** after Push |

Full admin/report path catalog (portal-only and unused by terminal yet) is still in `ApiPaths` and `docs/API_HANDOVER_SUPER_ADMIN V4.md`. Do not invent a second base URL or `api.kms-cvt.com`.

### Credential policy (policy A)

`reportPersonnelCardEnrollment` / fingerprint / face complete+revoke in `TerminalAdminApp` no longer call `completeCredentialEnrollment` / `revokeCredentialEnrollment`. Local enrollment still works; portal does not store biometric/NFC enrollment rows.

## Project Status

### How to read this file (status truth — filtered to backend conflicts)

| Topic | Conflicting claims that used to confuse agents | Current truth |
|---|---|---|
| **Terminal pairing** | Manual Admin Menu was the only path; web registration UI was missing / "not yet deployed". | **Portal Key Cabinet Registration + pairing API + terminal pairing screen are implemented and deployed.** Preferred path: web register → 6-digit code → terminal enter code → bootstrap (includes `cabinetSettings`). Manual Admin Menu remains fallback until live e2e is confirmed on a fresh install. Full detail: see the entry in `CLAUDE_TERMINAL.md`'s status-truth table for the terminal-side wire-up; this file owns the deploy-status framing. |
| **Regional Admin backend deploy** | Multiple Completed/Known-issues entries said migration `008` + the Phase 1/Phase 5 backend changes (`UserRole.REGIONAL_ADMIN`, `key_checkouts`/`site_office_hours`/`vendor_passkey_requests`, the `REGIONAL_ADMIN_ALLOWED_ROUTES` allowlist) were reviewed line-by-line only and "not deployed" to `kms-cvt.com`. Phase 5's own Completed entry additionally flagged a specific gap: `TERMINAL_DEVICE_ALLOWED_ROUTES` had zero entries for `/key-checkouts`/`/sites/:id/office-hours` — the fix existed in code but its own deploy status was separately unconfirmed. | **Deployed, confirmed by the user — including that specific route-allowlist gap.** `GET /sites/:id/office-hours`, `POST /key-checkouts`, `PATCH /key-checkouts/:id` are live; `pairWithCode`/`createKeyCheckout`/`closeKeyCheckout` reach production. Deployment alone does **not** mean live-tested — see `CLAUDE_TERMINAL.md`'s Known Issues for what's still live-unverified (the 17-item Admin Menu matrix, item-14 site names, Office Hours/Vendor Passkey screens, Key Menu, Phase 5 checkout sync). `web/`'s `REGIONAL_ADMIN` option (see `CLAUDE_WEB.md`) should now work against production but hasn't been click-verified either. |

**Agent rule:** When deciding whether a bug is open, check **Known issues / not yet resolved** and **Next steps** first. Only use older Completed Phase notes for history.

### Completed

- **Card-UID / credential sync (backend half)**: as part of the wider "Personnel/credential sync + theme iteration" push (full entry — client wiring, theme rework — lives in `CLAUDE_TERMINAL.md`), `TerminalApiClient` gained user/site/credential list+create+complete+revoke methods (credential complete/revoke for NFC/fingerprint/face were later disabled under policy A, not a bug if unused now). Backend: `POST /users/:id/credentials/complete` + migration `005`, rejects hex-UID-shaped payloads server-side (boundary #2 enforced at the API layer, not just client convention).
- **Removed unauthenticated `/v1/debug/agent-log` + all scaffolding that fed it** — see `CLAUDE.md` boundary #8 for the incident and the permanent rule it produced. Deleted: backend route + `agentDebugLog.js`, 4 call sites in `users.js`, 1 in `sync.js`, 5 + `postAgentDebugLog` in terminalApp (see `CLAUDE_TERMINAL.md`), 1 in `web/src/pages/PersonnelPage.tsx` (found later, see `CLAUDE_WEB.md`'s PATCH rollout entry). Confirmed via grep that `backend/src/index.js` is the only file mounting Express routes, and the only unauthenticated routes are the 3 sanctioned ones.
- **PATCH/update support across `web/` (backend half)** — full entry (client.ts, ResourcePage.tsx) lives in `CLAUDE_WEB.md`. Backend side: 11 resources have real double-guarded `expectedRevision` enforcement (sites/terminals/users/keys/key-slots/access-grants individually; event-definitions/schedules/personnel-groups/key-groups/multi-authentication-rules/appointment-reasons via a shared `softDeleteRouter` factory in `phase4.js`).
- **Fingerprint + face enrollment (backend half)** — full entry (R503 protocol, `FaceCameraController`, etc.) lives in `CLAUDE_TERMINAL.md`. Backend: audit found `credentials.js`'s raw-material check didn't cover FINGERPRINT/FACE_RECOGNITION; fixed with a **raw-material guard** (`credentials.js`) — a recognized reference prefix (`cardref_`/`fptemplate_`/`faceref_`/`vendorref_`/`ref_`) always passes; otherwise a per-kind denylist applies (hex for NFC, unchanged; a ≥20-char base64/hex-blob pattern for FINGERPRINT/FACE_RECOGNITION — real templates/embeddings run far longer than any reasonable reference).
- **Key Cabinet + Personnel registration, pairing code — Part 1 (backend + shared)**. Terminal gained `vendorDeviceId`, structured `nodeRows`/`nodesPerRow` (server-validated 1..127 per protocol §7.1), `latitude`/`longitude`; `AdminUser`/`users` gained `staffId` — see `ApiContracts.kt` (`TerminalDto`, `UserDto`, `CreateAdminUserRequest`/`UpdateAdminUserRequest`, `TerminalUpsertRequest`) as the canonical field-shape source. One-time pairing code: 6-digit (`crypto.randomInt`, no modulo bias), 30-min expiry, single-use (transactional `affectedRows` guard against concurrent redemption), `pairing_attempts` DB-ledger rate limiter (10 failures/15min/IP — a real concern at only 1,000,000 possibilities; requires `app.set('trust proxy', true)` so `req.ip` reflects the real client through Caddy). New unauthenticated `POST /v1/terminal/pair-with-code` issues `TERMINAL_DEVICE`-scoped JWTs (`sub = terminals.id`). Regenerating a terminal's code **[confirmed with user]** revokes its existing refresh-token sessions first — intentional, so a lost/reset device's old session can't keep syncing once a replacement is paired. `requireSuperAdminOrAllowlistedRole` middleware (single `/v1/admin` mount point) allowlists exactly the routes `TerminalApiClient` actually calls (enumerated from every call site, not assumed) — everything else 403s for a terminal token; `/v1/audit` and `/v1/reports` untouched (`requireSuperAdmin`). Already-paired terminals (old Super-Admin-derived token) are unaffected — strictly additive, no forced re-pair. Migration `006` (idempotent). Verify: shared/terminalApp compile-verified (confirms new fields don't break terminalApp); backend JS reviewed line-by-line only (no Node in this dev environment), never executed. Not deployed at the time.
- **Portal-managed cabinet settings (backend half)** — full spec lives in `CLAUDE_WEB.md`'s "Web Portal — Registration Workflow" section (migration `007`, `GET`/`PATCH` route, bootstrap merge into terminalApp — see `CLAUDE_TERMINAL.md` for the terminal-side merge). Phase 1 extras (Regional Admin, checkout sync, office hours, vendor passkey) were deferred at that point — now delivered, see the entries below.
- **Phase 1 (Revised): Regional Admin role + checkout/office-hours/vendor-passkey data model** (backend + `shared` + terminalApp only — no `web/`/`mobileApp` this pass; terminalApp side is compile-verified only, see `CLAUDE_TERMINAL.md`). `UserRole` gained `REGIONAL_ADMIN` (multi-site oversight, same many-to-many `user_site_assignments` model as Technician/Vendor); every exhaustive `when` over the enum fixed. Vendor role is now **hard-excluded** from fingerprint/face enrollment at the call site, not just a UI warning. New tables: `key_checkouts` (`OVERDUE` derived at query time from `status`+`dueAtEpochMillis`, never stored/cron-flipped — same pattern as everything else in this codebase that avoids a stale-cache field), `site_office_hours` (per-site + timezone, defaults `Asia/Kuala_Lumpur` — this system is Malaysia-only, flag any site needing a real different value), `vendor_passkey_requests` (deliberately minimal placeholder — no expiry-duration field yet, flagged as an open product decision). Migration `008`. `AuditEventType` gained `KEY_CHECKOUT_CREATED`/`UPDATED`/`RETURNED`, `SITE_OFFICE_HOURS_UPDATED`, `VENDOR_PASSKEY_REQUESTED`/`APPROVED`/`REJECTED`. **Flagged, not silently decided:** access-grant scope for Regional Admin was interpreted as "grants only," not full personnel-record management — see the allowlist follow-up below for where this bites. Verify: shared/terminalApp compile-verified; backend JS reviewed line-by-line only (no Node in this dev environment); not deployed.
- **Regional Admin route allowlist follow-up**: `requireSuperAdminOrAllowedTerminalDevice` renamed to **`requireSuperAdminOrAllowlistedRole`** (`backend/src/middleware/auth.js`) and gained a second allowlist, `REGIONAL_ADMIN_ALLOWED_ROUTES` — cabinet-settings `GET`/`PATCH`, office-hours `GET`/`PATCH`, access-grants `GET`/`GET:id`/`POST`/`PATCH` (no `DELETE`), vendor-passkey-requests `GET`/`GET:id`/`POST`/approve/reject. **Two-layer pattern established here, reused by everything after**: the allowlist controls *which routes* are reachable at all; a second, per-route in-handler check (`isSiteAssignedToUser`/`assignedSiteIdsForUser` in `util.js`) controls *which rows* — a Regional Admin token passing the allowlist for someone else's site still gets **404** (not 403, to avoid confirming the record exists) from the row-level check. Deliberately excludes `/users` (personnel CRUD) and `DELETE /access-grants/:id` — consistent with the "grants only" interpretation flagged above; widen if that reading turns out wrong. Verify: compile-verified only for `shared`/terminalApp side-effects; backend JS reviewed line-by-line, not executed; not deployed.
- **Phase 7 follow-up: Site names for Regional Admin (backend allowlist half)** — full entry (terminal-side site-name resolution, Backend Sync card split) lives in `CLAUDE_TERMINAL.md`. Backend: widened `REGIONAL_ADMIN_ALLOWED_ROUTES` with `GET /sites` and `GET /sites/:id` (read-only; `sites.js`'s own handlers additionally scope both to the Regional Admin's assigned sites via the same row-level pattern established above).
- **Phase 5: Post-Take Logging, Real Checkout Sync (backend allowlist fix)** — full entry (terminalApp `CheckoutDeadlinePolicy`, `TerminalCloseToDeadlineScreen`, etc.) lives in `CLAUDE_TERMINAL.md`. **Confirmed critical finding, matching what this phase was asked to verify**: `TERMINAL_DEVICE_ALLOWED_ROUTES` had **zero** entries for `/key-checkouts` or `/sites/:id/office-hours` before this phase — Phase 1's own code comments claimed "TERMINAL_DEVICE-reachable" but the allowlist entries were never actually added. Added exactly 3: `GET /sites/:id/office-hours`, `POST /key-checkouts`, `PATCH /key-checkouts/:id` (no `GET`/list on checkouts — the terminal only ever closes out checkouts it created itself). `DueDateSource` gained `EMERGENCY`; `CreateKeyCheckoutRequest` gained `dueDateSource` (backend `POST` schema widened to match); the `PATCH` route's schema deliberately stays `AUTO`/`MANUAL`-only — emergency status is only ever decided once, at take time. Verify: backend JS reviewed line-by-line only (no Node in this dev environment); not deployed at the time.
- **Jul 2026 portal + pairing rework** shipped policy A + `web/` registration UI (see `CLAUDE_WEB.md`) + pairing backend together — see "Credential policy (policy A)" above and `CLAUDE_WEB.md`'s "Web Portal — Registration Workflow" section for the full current spec, not repeated here. Appointments removed from portal nav/routes and unmounted from `backend/src/index.js`.
- **Region concept + generalized key-access-request schema (migration 009).** `regions` table (name, `display_order`, `max_key_access_duration_minutes` default 1440, full revision/soft-delete shape matching `sites`); `sites.region_id` nullable FK (no NOT NULL+backfill — no sensible default region exists, unlike office-hours' timezone default); `user_region_assignments`, same many-to-many shape as `user_site_assignments`, additive/independent of it per explicit product decision (a Regional Admin's Region and Site assignments are not cross-validated for consistency — flagged, not enforced); `key_access_requests` + `key_access_request_keys` linking table (multi-key supported from the start, mirrors `access_grant_keys`); `key_access_login_attempts`, a dedicated rate-limit table kept separate from `pairing_attempts` so the two brute-force surfaces don't share a lockout counter. **Correction made mid-session**: the old `vendor_passkey_requests` table/route was initially assumed to be an unused placeholder and nearly deleted — it is NOT, it's a currently-deployed, working feature (terminalApp's Phase 7 `TerminalVendorPasskeyScreen`, see `CLAUDE_TERMINAL.md`); caught and reverted before shipping, `key_access_requests` ships additive, coexisting alongside the untouched original, terminalApp confirmed zero diff. Routing model: Regional Admin approval authority routes through Region assignment (`user_region_assignments`), NOT Site assignment; Technician/Vendor's own request creation routes through their existing Site assignment instead — a bug where creation was initially gated through the Region check (which would have 403'd every legitimate requester) was caught and fixed before shipping. New allowlist tier added: `TECHNICIAN_VENDOR_ALLOWED_ROUTES` (not originally scoped, but required for Technician/Vendor tokens to reach `/v1/admin/key-access-requests` at all). New unauthenticated `POST /v1/terminal/passkey-login`: validates the passkey + confirms it matches the terminal's own site (a deliberate addition beyond the literal spec — otherwise a passkey could be replayed at any terminal, not just the one tied to the approved request), issues a `KEY_ACCESS_SESSION`-scoped JWT. Verify: shared/terminalApp/mobileApp compile-verified; backend booted locally and smoke-tested live (not just reviewed line-by-line) — every new/modified route correctly 401s unauthenticated, passkey-login reaches its DB call cleanly. Not deployed at the time.
- **Passkey feature — backend half** — full entry (mobileApp screens, terminalApp wiring) lives in `CLAUDE_MOBILE.md`. Part 0 audit found and fixed 4 real backend gaps that would have made the feature non-functional even with migration 009's schema in place: (a) `sites.js` never exposed/accepted `region_id` despite the column existing since migration 009 — fixed; (b) Technician/Vendor had no route to their own keys/access grants to build a key picker from — added self-scoped `GET /keys` / `GET /access-grants`, server-side scoped, not client-trusted; (c) no way to learn a Region's duration ceiling before submitting a request — added a narrow `GET /key-access-requests/site-policy/:siteId` endpoint rather than opening full Region visibility to a lower-privilege role; (d) — the significant one — the passkey-visibility model inherited from the original vendor-passkey workflow only ever showed the generated passkey to the approver, never to the actual requester through any API call, meaning the feature as originally modeled would have been completely non-functional (the person who needs to enter the passkey at the terminal could never have received it). Fixed: `generatedPasskey` is now visible only to the request's own requester on any GET; approvers still never see the value itself. New `DueDateSource.PASSKEY_REQUEST` added on the terminalApp side (see `CLAUDE_TERMINAL.md`) — purely additive, no exhaustive `when` broke. Verify: backend booted locally and smoke-tested — every new/modified route correctly gated. Not deployed at the time — this is the combined push landing now (see Next steps).
- **Key node binding (Key Attachment feature) — backend half.** Full entry (discovery, web/terminal halves) lives in `CLAUDE_WEB.md`/`CLAUDE_TERMINAL.md`. This is the fix for the previously-flagged gap: web-registered keys had no `KeySlot` (physical node) binding at all.
  - **`keys.js` DELETE reworked**: previously hard-blocked (409) deleting a key with any active `KeySlot` reference — the opposite of "delete frees the node." Now unlinks instead: any active `key_slots` row with `managed_key_id` = the deleted key gets `managed_key_id = NULL` (row stays ACTIVE, revision bumped, `KEY_SLOT_UPDATED` audit logged per slot) as part of the same delete call, then the key itself soft-deletes as before. The active-`access_grant_keys` dependency check is **untouched** — still a hard 409 block, per explicit scope (AccessGrant logic confirmed correct in a prior session, not to be touched).
  - **New terminal-only completion route**: `POST /v1/admin/keys/:id/fob-enrollment/complete` (`ApiPaths.ADMIN_KEY_FOB_ENROLLMENT_COMPLETE`) — mirrors `credentials.js`'s `/complete` pattern exactly (same permissive-with-denylist opaque-reference check, reusing the same `cardref_`-etc. prefix allowlist). Accepts `{ enrollmentReference, terminalId }`, writes `managed_keys.fob_enrollment_reference`, logs `KEY_FOB_ENROLLED`/`KEY_FOB_REPLACED` (existing, previously-unused enum values — confirmed pre-scaffolded for exactly this). **This is a genuinely different mechanism from policy A** — policy A only disabled `credential_statuses` (personnel NFC/fingerprint/face) sync; `ManagedKey.fobEnrollmentReference` was always the sanctioned boundary-#2 completion path and was never part of that decision. Added to `TERMINAL_DEVICE_ALLOWED_ROUTES` (`auth.js`), same allowlist pattern as the personnel credentials-complete entry.
  - **`writeAudit()` (`util.js`) gained an optional `id` param and now returns `{ id }`** — purely additive (existing callers all ignore the return value, still compile/behave identically) — needed so the new completion route could report a real `auditEventId` back to the terminal (`FobEnrollmentResponse.auditEventId`), which the old `writeAudit()` had no way to surface.
  - **`FobEnrollmentCompleteRequest` added to `ApiContracts.kt`** next to the pre-existing (previously unused) `FobEnrollmentResponse`/`FobEnrollmentAuditPayload`/`KEY_FOB_ENROLLED` family — confirms these were scaffolded in anticipation of exactly this route, now finally wired.
  - **New `AuditEventType.KEY_FOB_ENROLLMENT_SYNC_FAILED`** — terminal-local-only event, same non-blocking-sync shape as `KEY_CHECKOUT_SYNC_FAILED`: the background auto-scan's local capture succeeds and is never undone even if this completion POST fails.
  - Verify: `node --check` syntax-clean on `keys.js`/`util.js`/`auth.js`; **not booted locally against a real MySQL instance this pass** (no local DB in this dev environment) and **not deployed** — see Known issues.
- **mobileApp login null `deviceId` (Haikal, Jul 2026).** `POST /v1/auth/login` Zod used `deviceId: z.string().optional()`, which rejects JSON `null`. mobileApp's kotlinx client sent `"deviceId": null` → `Expected string, received null`. Fixed to `z.string().nullish()`; mobile also sends `mobile-android` / `explicitNulls = false` (see `CLAUDE_MOBILE.md`).
- **mobile companion Terminals allowlist (Haikal, Jul 2026).** `GET /terminals` + `GET /terminals/:id` added to `REGIONAL_ADMIN` and `TECHNICIAN`/`VENDOR` allowlists; list/get scoped to `user_site_assignments`. Same site scoping extended to Technician/Vendor on `GET /sites`.
- **Key Attachment new-key-registration widened onto `TERMINAL_DEVICE_ALLOWED_ROUTES` (Jul 2026) — full entry (audit, decision, terminalApp side) lives in `CLAUDE_TERMINAL.md`'s "Key Attachment login-method gate" Completed entry.** `POST /keys` and `POST /key-slots` (`auth.js`) were previously excluded on purpose, requiring a real per-user Super Admin/Regional Admin JWT; widened by explicit product decision so a fingerprint/face/NFC/passkey-signed-in admin can also register a new key from the terminal, not just password logins. **Real attribution bug found and fixed alongside the widening**: `keys.js`'s and `keySlots.js`'s `POST /` routes both wrote `actorUserId: req.auth.sub` unconditionally — harmless while only real users could reach them, but would have written the terminal's own device UUID into `actorUserId` as if it were a real user once a TERMINAL_DEVICE token could reach them too. Fixed by adding the same `actorUserIdFor(req)` helper (`role === 'TERMINAL_DEVICE' ? null : req.auth.sub`) already used by `users.js`/`credentials.js`/`keyCheckouts.js`/`vendorPasskeyRequests.js`/`keyAccessRequests.js` to both files, applied only at the newly-widened `POST /` routes. **Known, accepted tradeoff**: a TERMINAL_DEVICE-created key/slot's `KEY_CREATED`/`KEY_SLOT_CREATED` audit event now has `actorUserId = null`, same as every other TERMINAL_DEVICE-reachable route — which specific locally-logged-in admin triggered a terminal-side registration is not recorded server-side. Verify: `node --check` syntax-clean on `auth.js`/`keys.js`/`keySlots.js`; not booted against a real MySQL instance this pass and **not deployed** — needs deployment to the live VPS before non-password logins can register new keys from a terminal, see Next steps.
- **Activity Report API (Aug 2026).** `GET /v1/reports/activity-logs` + `activity-summary` (four categories: KEY_TAKE / KEY_RETURN / CABINET_REGISTRATION / PERSONNEL_REGISTRATION); `POST /v1/reports/exports` kind `ACTIVITY_LOGS` PDF via `streamActivityLogsPdf`. Optional `cabinetScope=ACTIVE|DELETED` (default **ACTIVE** on activity + audit/report helpers) filters active vs removed cabinets (Activity Report vs Activity archive). Portal Location filter further hides locations with zero active cabinets. Personnel create emits `PERSONNEL_REGISTERED` (replaces create-path reliance on `USER_ACCOUNT_STATUS_CHANGED` + `USER_CREATED` for this report bucket). Cabinet registration continues to use existing `TERMINAL_CREATED` / `TERMINAL_PAIRED`. **Fault fixed in the same pass:** sync `ingestAuditEvents` previously stamped `occurred_at` with push-receive time — now preserves terminal `occurredAtEpochMillis` (and prefers client event `id`, skipping `ER_DUP_ENTRY` on re-push). Auth unchanged: `/v1/reports` remains `requireSuperAdmin`. **Deployed** with the Aug 2026 cascade/flush API rebuild — browser click-through still recommended.

- **Report PDF table layout fix (Aug 2026).** `backend/src/reportPdf.js` (KEY_OPERATIONS + ACTIVITY_LOGS) rewritten: ruled multi-column tables with scaled column widths, clipped/ellipsis cell text, striped rows, and repeating headers on page breaks — fixes overlapping/run-together export formatting.
- **Cascade cabinet soft-delete + Admin flush (Aug 2026).** `DELETE /v1/admin/terminals/:id` with body `{ cascade: true }` soft-deletes the terminal plus ACTIVE slots, keys bound to those slots, and grants referencing those keys (`cascadeDelete.js`, audited `CASCADE_CABINET_DELETE`). Non-cascade DELETE still 409s when active slots exist. New Super-Admin-only `GET /v1/admin/flush/preview?scope=` + `POST /v1/admin/flush` (`flush.js`) hard-wipes scopes `TERMINALS`/`KEYS`/`USERS`/`SITES`/`ACCESS_GRANTS`/`ALL` after preview-token + `confirmToken: I_UNDERSTAND_PERMANENT_FLUSH`; never deletes `SUPER_ADMIN`/`GOD_ADMIN`; audit events retained. Shared `ApiPaths.ADMIN_FLUSH` / `ADMIN_FLUSH_PREVIEW`. Portal: double-confirm Recycle + Flush data page (see `CLAUDE_WEB.md`). **Deployed** to `kms-cvt.com` (API rebuild + `web/dist`); flush preview returns 401 without auth (route live). Click-through of cascade Recycle / Flush scopes still recommended.

- **Checkout-deadline notification backend (Aug 2026, backend-only pass — mobile Firebase SDK wiring and web SSE client follow separately).** Implements the confirmed audit+design from the prior session as-is. Migration `012`: `key_checkout_notifications` (`checkout_id` FK, `notification_type ENUM('WARNING_15MIN','OVERDUE')`, `UNIQUE KEY (checkout_id, notification_type)` — the actual de-dup mechanism, registered in `migratePhase2.js`).
  - **`deadlineMonitor.js`**: an in-process `setInterval` (60s tick), started once from `index.js` before `app.listen()` — no OS cron, no queue/worker library, matching "prefer updating the existing VPS deploy" (this backend is a single Node process in Docker Compose with no existing scheduler infra at all — confirmed via `package.json`, no cron/queue dependency of any kind). Each tick runs two queries (`key_checkouts` `JOIN terminals`/`managed_keys`, `LEFT JOIN key_checkout_notifications` filtered to `notification_type IS NULL` for that row) — one for checkouts due within 15 minutes, one for checkouts already overdue — each already excluding checkouts with an existing dedup row, then inserts the dedup row via `INSERT ... ON DUPLICATE KEY UPDATE id = id` rather than a separate pre-check-then-insert (the UNIQUE KEY is the actual safety net against a slow tick overlapping the next one, not a second race-prone check). A `tickInProgress` guard additionally skips a tick outright if the previous one is still running.
  - **Recipient resolution, reusing existing pieces, not reimplementing**: technician/vendor via `key_checkouts.user_id` directly to `sendPushToUser`; covering Regional Admins via `notifyRegionalAdminsForSite` (`keyAccessRequests.js`) — **exported** (one `export` keyword added, function body untouched) so `deadlineMonitor.js` can import and reuse it exactly as it already existed for the Only-B/vendor-passkey flow. **A checkout at a region-less site (`sites.region_id IS NULL`) notifies zero Regional Admins — this is `notifyRegionalAdminsForSite`'s own pre-existing, already-documented behavior (migration `009`'s own comment: "routes to nobody until a Super Admin assigns the site to a region"), not a new gap introduced here.** Super Admins are **not** pushed via FCM at all for this feature — they get the SSE broadcast instead (below), per the feature's own recipient/channel split, not an oversight.
  - **`fcm.js` migrated from the legacy FCM HTTP API to `firebase-admin`'s HTTP v1 API** (`admin.messaging().send()`), authenticated via a service-account JSON whose path comes from new env var `FCM_SERVICE_ACCOUNT_PATH` (replacing `FCM_SERVER_KEY`, which was never actually referenced in any `.env*`/`docker-compose*` file to begin with — confirmed via grep, so there was no stale config residue to clean up, only the bare `process.env` read inside the old `fcm.js` itself). Firebase app init is lazy, cached, and **never throws** — missing/unreadable credential file logs and no-ops (`{ sent: 0, skipped: 'no_service_account' }`), exactly the same graceful-degradation contract the legacy key-based version had, since the user had not completed Firebase signup at the time of this pass. `registerPushToken`/`sendPushToUser`'s exported signatures are byte-for-byte unchanged — all 5 existing `sendPushToUser` call sites in `keyAccessRequests.js` needed zero changes, confirmed via `node --check` plus a direct runtime import test (`fcm.js` imports cleanly, `firebase-admin` resolves as a real installed dependency — added to `package.json`/`package-lock.json` via `npm install`).
  - **New SSE endpoint for the Super Admin live-popup channel** (`notificationsStream.js`, `notifications.js`): `POST /v1/notifications/stream/ticket` (normal `requireAuth`+`requireSuperAdmin`, mints a random single-use ticket scoped to that user, 30s TTL) and `GET /v1/notifications/stream` (`text/event-stream`, authenticated by *consuming* that ticket instead of a Bearer header — burned on lookup regardless of validity, never reusable). **Deliberate exception to "every route sits behind `requireAuth`"** (boundary #8), same class as `pair-with-code`/`passkey-login`'s existing exceptions, explicitly commented as such at both the route file and its `index.js` mount point — not an oversight. The workaround exists because the browser's native `EventSource` cannot set an `Authorization` header at all; the ticket is deliberately **not** the real JWT in the query string, to keep it out of server/proxy access logs. In-process connection registry (`Map<userId, Set<Response>>`) + `broadcastToSuperAdmins(eventType, data)` — plain `Map`/`Set`, no external pub/sub library, since this is a single-process deploy (confirmed sufficient before reaching for anything heavier). A 30s SSE comment-line heartbeat keeps each connection demonstrably active against any idle-connection timeout.
  - **`server.requestTimeout = 0`** set on the `http.Server` instance now captured from `app.listen()`'s return value (`index.js`) — this is a single, genuinely global setting; Express/the `http` module offer no per-route way to scope it, confirmed and flagged explicitly rather than silently applied. Accepted specifically because Caddy (`Caddyfile`) already sits in front of this process as the real internet-facing edge, providing its own layer against the slow-loris pattern this Node-level setting exists to mitigate.
  - Verify: `node --check` syntax-clean on every new/changed file. **No local MySQL in this dev environment** (same long-standing constraint as every other backend pass in this file) — so a full live tick against real `key_checkouts` rows was not run. What *was* run, for real, against the actual production code (not mocks): a direct runtime import of `fcm.js` (confirms `firebase-admin` resolves, `db.js`'s lazy pool doesn't block without a live DB); an isolated test of `notifications.js`'s ticket mint/single-use-consume/unknown-ticket and broadcast-deliver/unregister-stops-delivery logic; and a full HTTP-level smoke test booting a minimal Express app mounting the real `notificationsStream.js` router (hand-signed JWT to satisfy `requireAuth` without a DB, since neither this middleware nor these new routes touch it) — ticket mint, no-ticket-rejected (401), stream connect (`200`, `text/event-stream`), a real `broadcastToSuperAdmins` call actually reaching the open connection, and ticket-reuse-rejected (401) all passed. Confirmed via grep: `FCM_SERVER_KEY` appears exactly once left in the whole `backend/` tree, in a doc comment inside the new `fcm.js` explaining *why* it was replaced — not a live config reference or dead code path. **Not deployed.** **FCM sending is untestable end-to-end until the user completes Firebase signup and provides the real service-account file** (`FCM_SERVICE_ACCOUNT_PATH` is documented in both `.env.example`/`.env.production.example` and `docker-compose.prod.yml`, left unset/commented — the graceful no-op is what's actually been verified, not a real send). **SSE is boot-tested locally only** — needs a live check against Caddy's actual proxy timeout behavior post-deploy, per the "worth a quick live check" note from the design pass; the local smoke test above cannot exercise Caddy at all.
  - **Aug 2026 handoff update: all three pieces (backend here, mobile, web) are now code-complete and cross-checked against each other's actual contracts** (SSE event names `CHECKOUT_WARNING_15MIN`/`CHECKOUT_OVERDUE` and payload fields `checkoutId`/`keyId`/`dueAtEpochMillis` confirmed byte-for-byte matching between this backend's `notifications.js`/`deadlineMonitor.js` and web's `NotificationsContext.tsx`). **Still not deployed** — per explicit user instruction this is being handed to a separate dev team for a GitHub-push-triggered VPS deployment, with the Firebase service-account JSON handed to that team directly (outside this repo/chat). Full deploy sequence, Caddy SSE-timeout live-check, and post-deploy smoke test: `docs/DEPLOY_HANDOVER_checkout_notifications.md`. Do not duplicate those steps here — that doc is the single source for the deploy procedure.

### Known issues / not yet resolved

- **Activity Report / cascade-flush click verification.** API + portal were redeployed with cascade/flush (Aug 2026); Activity Report routes and Flush data nav ship with that same deploy. Remaining gap is manual click verification (create personnel/cabinet → report categories; terminal take/return + Push → KEY_TAKE/KEY_RETURN timestamps; cabinet cascade Recycle; Flush preview counts). mobile Logs tab still needs APK rebuild if not already shipped.
- **Key Attachment new-key-registration widening (see Completed above) is code-complete; API image redeployed Aug 2026 with cascade/flush.** Live route behavior (fingerprint/face/NFC/passkey admin creating keys via TERMINAL_DEVICE) still needs a real terminal e2e — do not treat deploy alone as verified.
- **Credentials are terminal-local (policy A, Jul 2026):** NFC / fingerprint / face enrollment no longer POSTs `complete`/`revoke` to the backend. The old `expectedRevision: null` zod rejection is therefore moot for those call sites. Do **not** re-enable server credential sync without an explicit product decision. (Terminal-side implication: see `CLAUDE_TERMINAL.md`.)
- **Pairing-code flow: code + portal UI + cabinet settings sync deployed; live e2e on a fresh F7G18P still required.** Run: portal register (including Cabinet Settings, see `CLAUDE_WEB.md`) → copy 6-digit code → fresh terminalApp enter code (see `CLAUDE_TERMINAL.md`) → confirm bootstrap downloads unit data **and** timers/toggles.
- **Pairing-code flow field verification outstanding** — same item as above, restated in the original doc as a short pointer. Portal registration UI and API are in place after the Jul 2026 rework; confirm live e2e on hardware.
- **Regional Admin role (Phase 1 revised + follow-ups): backend is now deployed to `kms-cvt.com` (confirmed by the user), but nothing has been live-tested through a real login yet.** Backend changes (`UserRole.REGIONAL_ADMIN`, `key_checkouts`/`site_office_hours`/`vendor_passkey_requests` tables, migration `008`, the `REGIONAL_ADMIN_ALLOWED_ROUTES` allowlist + row-level site scoping) were reviewed line-by-line only at the time they were written and are now confirmed live — that confirms deployment, not a fresh post-deploy code review. See `CLAUDE_TERMINAL.md` Known Issues for the terminal-side 17-item permission matrix, still never exercised through a real Regional Admin or Technician login (no such account has been created yet). Do not report any Regional Admin *behavior* as working until a real account + login test happens; only the deploy itself is confirmed. `web/`'s `REGIONAL_ADMIN` option (see `CLAUDE_WEB.md`) should now work against production since the backend is deployed — but this has not been click-verified either.
- **Phase 5 checkout sync / close-to-deadline logic: backend is deployed and confirmed live, including the 3-route allowlist gap — the only remaining gap is that it has never been hardware-run at all.** `GET /sites/:id/office-hours`, `POST /key-checkouts`, `PATCH /key-checkouts/:id` are live; `pairWithCode`/`createKeyCheckout`/`closeKeyCheckout` reach production. See `CLAUDE_TERMINAL.md` Known Issues for the terminal-side hardware-verification gap (`TerminalCloseToDeadlineScreen`, `CLOSE_TO_DEADLINE_THRESHOLD_MINUTES`/`EMERGENCY_WINDOW_HOURS` unvalidated guesses).
- **Appointment Authorization removed from portal + API mounts** (Jul 2026). Dead `phase4.js` appointment routers may still exist unmounted; ignore unless product restores appointments. (Portal-side nav removal: see `CLAUDE_WEB.md`.)
- **Key Slots:** backend CRUD exists and is now actually wired up from `web/` (auto-assign on key add, unlink on key delete — see the Key node binding entry above and `CLAUDE_WEB.md`); still no dedicated standalone Key Slots admin page — see `CLAUDE_WEB.md` Next steps.
- **Key node binding (Key Attachment) — RESOLVED, see `CLAUDE_TERMINAL.md`'s closure entry.** Backend half (DELETE-unlink, fob-enrollment complete, `TERMINAL_DEVICE` allowlist) has now been exercised end-to-end via live MySQL/terminal e2e and passed the full testing checklist.
- **Passkey feature (Region schema + mobileApp request/approval + terminalApp real login wiring) is compile-verified and locally-smoke-tested only — never run end-to-end on real devices against the live deployed backend.** Full unverified-scenario list lives in `CLAUDE_MOBILE.md` (primary owner of this entry); backend-side implication is that none of the new routes (`region_id` on sites, self-scoped keys/access-grants, site-policy lookup, passkey-login) have been exercised by a real device round-trip, only local smoke tests.

### Next steps (in order)

- **Browser-verify** cascade cabinet Recycle, Flush data preview, and Activity Report on production (API + `web/dist` redeployed Aug 2026).
- **This session's combined work (mobileApp foundation, migration 009/Region schema, mobileApp real backend client, full Passkey feature) is being pushed to GitHub now for the dev team to deploy.** Once live: real end-to-end device test of the full Passkey flow (mobile request → mobile approval → mobile passkey display → terminal passkey login → take flow with correct due time) is the highest-priority verification, since none of it has touched a real device yet. (Primary tracking in `CLAUDE_MOBILE.md`.)
- **Regional Admin + Phase 5 checkout-sync backend changes are deployed to `kms-cvt.com`, including the 3-route allowlist gap** (migration `008`, `auth.js` allowlist additions, `keyCheckouts.js`/`sites.js` route changes) — this step is no longer "deploy," it's "test." Create a real Regional Admin account + a Technician/Vendor + access grant on the live backend, then verify: the 17-item Admin Menu permission matrix end-to-end, item-14 site-name display, Office Hours/Vendor Passkey screens, and Key Menu multi-key take (see `CLAUDE_TERMINAL.md` for the terminal-side checklist this unblocks).
- Live e2e: portal set Cabinet Settings → pair fresh terminal → confirm Admin Menu / Take-Return timers match DB values after bootstrap (see `CLAUDE_WEB.md` and `CLAUDE_TERMINAL.md` for the respective portal/terminal steps).
- Live e2e pairing checklist (unit data + pairing code) on a fresh F7G18P (see `CLAUDE_TERMINAL.md`'s operator checklist).
